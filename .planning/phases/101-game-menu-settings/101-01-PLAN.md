---
phase: 101-game-menu-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/store/uiSettingsStore.ts
  - apps/web/src/ui/modals/GameMenu.tsx
  - apps/web/src/ui/modals/GameMenu.css
autonomous: true
requirements: [MENU-01, MENU-02, MENU-03, MENU-04, MENU-05]

must_haves:
  truths:
    - "GameMenu renders as a React Portal to document.body with dark backdrop"
    - "Audio sliders for Master, Music, Effects, Ambient wire to useAudioStore setters and update live on drag"
    - "Mute/unmute icon toggles exist for each audio category"
    - "Interface section has a toggle switch for Secondary Action Bar visibility"
    - "Settings tab and About tab switch content via tab navigation"
    - "Leave Game button shows confirmation dialog; confirming disconnects WebSocket and navigates to /character-select"
    - "useUiSettingsStore persists showSecondaryBar to localStorage via Zustand persist middleware"
  artifacts:
    - path: "apps/web/src/store/uiSettingsStore.ts"
      provides: "Persisted UI settings store with showSecondaryBar"
      exports: ["useUiSettingsStore"]
    - path: "apps/web/src/ui/modals/GameMenu.tsx"
      provides: "Tabbed game menu overlay component"
      exports: ["GameMenu"]
    - path: "apps/web/src/ui/modals/GameMenu.css"
      provides: "Game menu overlay styling"
  key_links:
    - from: "apps/web/src/ui/modals/GameMenu.tsx"
      to: "apps/web/src/store/audioStore.ts"
      via: "useAudioStore hook for volume sliders"
      pattern: "useAudioStore"
    - from: "apps/web/src/ui/modals/GameMenu.tsx"
      to: "apps/web/src/store/uiSettingsStore.ts"
      via: "useUiSettingsStore hook for secondary bar toggle"
      pattern: "useUiSettingsStore"
    - from: "apps/web/src/ui/modals/GameMenu.tsx"
      to: "apps/web/src/network/socket.ts"
      via: "gameSocket.disconnect() for Leave Game"
      pattern: "gameSocket\\.disconnect"
---

<objective>
Create the UI settings store and the full GameMenu overlay component with all content: audio sliders, interface toggle, tab navigation, and Leave Game flow.

Purpose: Provides the complete game menu UI that players interact with. This is the core deliverable — Plan 02 wires it into the game HUD.
Output: `uiSettingsStore.ts`, `GameMenu.tsx`, `GameMenu.css`
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-game-menu-settings/101-CONTEXT.md
@.planning/phases/101-game-menu-settings/101-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/src/store/audioStore.ts:
```typescript
interface AudioState {
  master: number;
  music: number;
  effects: number;
  ambient: number;
  setMaster: (v: number) => void;
  setMusic: (v: number) => void;
  setEffects: (v: number) => void;
  setAmbient: (v: number) => void;
}
export const useAudioStore = create<AudioState>()(persist(...));
```

From apps/web/src/network/socket.ts:
```typescript
export const gameSocket: Socket;
// gameSocket.disconnect() is the established pattern for leaving the game
```

From apps/web/src/screens/GameScreen.tsx (leave-game pattern, line 92):
```typescript
gameSocket.disconnect();
// Then navigate('/character-select')
```

CSS variables available (from existing HUD):
```css
--color-bg-dark, --color-bg-panel, --color-border, --color-text, --color-text-muted,
--color-accent, --color-accent-hover
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useUiSettingsStore with persisted showSecondaryBar</name>
  <files>apps/web/src/store/uiSettingsStore.ts</files>
  <action>
Create a new Zustand store at `apps/web/src/store/uiSettingsStore.ts` following the exact pattern used by `useAudioStore`:

```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface UiSettingsState {
  showSecondaryBar: boolean;
  setShowSecondaryBar: (v: boolean) => void;
}

export const useUiSettingsStore = create<UiSettingsState>()(
  persist(
    (set) => ({
      showSecondaryBar: true,
      setShowSecondaryBar: (v) => set({ showSecondaryBar: v }),
    }),
    {
      name: 'ui-settings',
      partialize: (state) => ({ showSecondaryBar: state.showSecondaryBar }),
    }
  )
);
```

- Default `showSecondaryBar` to `true` (secondary bar visible by default).
- Use `partialize` to persist only the boolean, not the setter.
- localStorage key: `'ui-settings'`.
  </action>
  <verify>
    <automated>grep -q "useUiSettingsStore" apps/web/src/store/uiSettingsStore.ts && grep -q "persist" apps/web/src/store/uiSettingsStore.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>uiSettingsStore.ts exists, exports useUiSettingsStore with showSecondaryBar boolean persisted to localStorage via Zustand persist middleware</done>
</task>

<task type="auto">
  <name>Task 2: Build GameMenu component and CSS</name>
  <files>apps/web/src/ui/modals/GameMenu.tsx, apps/web/src/ui/modals/GameMenu.css</files>
  <action>
Create the full GameMenu overlay component at `apps/web/src/ui/modals/GameMenu.tsx` and its styles at `apps/web/src/ui/modals/GameMenu.css`. Check if `apps/web/src/ui/modals/` directory exists; create it if not.

**GameMenu.tsx requirements:**

1. **Portal rendering:** Use `createPortal(content, document.body)` to render outside `.game-ui` stacking context.

2. **Props:** `{ onClose: () => void }` — called when backdrop clicked or close button pressed.

3. **Backdrop:** Dark semi-transparent overlay (`rgba(0, 0, 0, 0.7)`), covers full viewport, `onClick={onClose}`. Must have `pointer-events: all` to block clicks reaching the game canvas.

4. **Modal container:** Centered, ~60% screen width, max-width ~700px. `onClick={e => e.stopPropagation()}` to prevent backdrop close when clicking inside modal.

5. **Tab navigation:** Two tabs — "Settings" and "About". Use `useState<'settings' | 'about'>('settings')` for active tab. Style tabs to match existing HUD dark theme (use CSS variables). Claude's discretion on tab style (underline, pill, etc.).

6. **Settings tab — Audio section:**
   - Section header "Audio"
   - Four sliders: Master, Music, Effects, Ambient
   - Each slider: label on left, `<input type="range" min={0} max={1} step={0.01}>` in middle, mute/unmute icon button on right
   - Wire `value` and `onChange` to `useAudioStore` getters/setters: `const { master, music, effects, ambient, setMaster, setMusic, setEffects, setAmbient } = useAudioStore()`
   - `onChange={e => setMusic(Number(e.target.value))}` — fires on every drag tick (React onChange = live update)
   - Mute toggle: store previous value, set to 0 on mute, restore on unmute. Use local state `mutedValues` map or individual refs. When muted, slider shows 0 but previous value is remembered. Icon changes between speaker and muted-speaker.

7. **Settings tab — Interface section:**
   - Section header "Interface"
   - "Secondary Action Bar" label with a toggle switch (styled checkbox or button)
   - Wire to `useUiSettingsStore`: `const { showSecondaryBar, setShowSecondaryBar } = useUiSettingsStore()`
   - `onChange` toggles the boolean

8. **About tab:**
   - Game title "Into the Void"
   - Version: "v1.21" (hardcoded is fine)
   - Brief description: "A multiplayer 2D sci-fi survival MMO"
   - Claude's discretion on additional content (credits, links)

9. **Leave Game button:**
   - Always visible at the bottom of the modal, outside tab content area
   - Labeled "Leave Game"
   - On click: show inline confirmation — "Are you sure you want to leave?" with Cancel and Leave buttons
   - Use `useState(false)` for `confirming` state
   - On Cancel: `setConfirming(false)`
   - On Leave (confirm): call `gameSocket.disconnect()` then `navigate('/character-select')` using `useNavigate()` from react-router
   - Do NOT call `useAuthStore.logout()` — keep auth token intact per user decision
   - Import `gameSocket` from `../../network/socket` and `useNavigate` from `react-router`

10. **Close button:** An X button in the top-right corner of the modal that calls `onClose`.

**GameMenu.css requirements:**
- Use existing CSS variables (`--color-bg-dark`, `--color-bg-panel`, `--color-border`, `--color-text`, `--color-text-muted`, `--color-accent`)
- Dark theme matching existing HUD panels
- `.game-menu-backdrop`: fixed position, full viewport, z-index 10000 (above all game UI), flex center
- `.game-menu-modal`: background panel color, border, border-radius, padding, ~60% width, max-width 700px
- Style sliders to look integrated (accent color for thumb/track)
- Style toggle switch for secondary bar
- Style tabs, sections, Leave Game button, confirmation dialog
- Responsive: on small screens, modal should be wider (up to 90%)

**Anti-patterns to avoid:**
- Do NOT use `onMouseUp` for sliders — `onChange` fires live in React
- Do NOT render inside `.game-ui` div — must be Portal to `document.body`
- Do NOT forget `e.stopPropagation()` on modal container click
- Do NOT call `useAuthStore.logout()` on Leave Game
  </action>
  <verify>
    <automated>grep -q "createPortal" apps/web/src/ui/modals/GameMenu.tsx && grep -q "useAudioStore" apps/web/src/ui/modals/GameMenu.tsx && grep -q "useUiSettingsStore" apps/web/src/ui/modals/GameMenu.tsx && grep -q "gameSocket.disconnect" apps/web/src/ui/modals/GameMenu.tsx && grep -q "game-menu-backdrop" apps/web/src/ui/modals/GameMenu.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
GameMenu.tsx renders as a Portal to document.body with:
- Dark backdrop blocking game interaction
- Two tabs (Settings, About) with tab switching
- Audio section: 4 sliders (Master/Music/Effects/Ambient) wired to audioStore with mute toggles
- Interface section: Secondary Action Bar toggle wired to uiSettingsStore
- About tab with game info
- Leave Game button with confirmation dialog that disconnects WebSocket and navigates to /character-select
- Close button (X) and backdrop click to close
GameMenu.css provides dark-themed styling matching existing HUD
  </done>
</task>

</tasks>

<verification>
1. `apps/web/src/store/uiSettingsStore.ts` exists and exports `useUiSettingsStore` with `showSecondaryBar` persisted
2. `apps/web/src/ui/modals/GameMenu.tsx` exists and uses `createPortal`, `useAudioStore`, `useUiSettingsStore`, `gameSocket.disconnect()`, `useNavigate`
3. `apps/web/src/ui/modals/GameMenu.css` exists with `.game-menu-backdrop` and `.game-menu-modal` classes
4. No TypeScript compilation errors: `npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20`
</verification>

<success_criteria>
- GameMenu component is a complete, self-contained overlay ready to be mounted
- All four audio sliders wire to useAudioStore setters with live drag updates
- Each audio slider has a mute/unmute toggle
- Secondary bar toggle wires to useUiSettingsStore
- Leave Game shows confirmation and disconnects + navigates on confirm
- Tab navigation switches between Settings and About
- uiSettingsStore persists showSecondaryBar to localStorage
- All styling uses existing CSS variables for visual consistency
</success_criteria>

<output>
After completion, create `.planning/phases/101-game-menu-settings/101-01-SUMMARY.md`
</output>
