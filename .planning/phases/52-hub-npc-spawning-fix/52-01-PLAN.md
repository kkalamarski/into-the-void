---
phase: 52-hub-npc-spawning-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/zones/zones.service.ts
  - packages/world-gen/src/generation/hub.ts
autonomous: true

must_haves:
  truths:
    - "Player enters any faction hub — only NPCs appear, no creatures spawn"
    - "Hub zones show correct NPC types (Trader, Guard, Faction Rep, Ambient, Service)"
    - "Creature spawning logic skips hub zones entirely"
    - "Existing world creature spawning remains unchanged"
  artifacts:
    - path: "apps/game-server/src/zones/zones.service.ts"
      provides: "Hub zone loading with NPC spawning"
      contains: "spawnHubNpcs"
    - path: "packages/world-gen/src/generation/hub.ts"
      provides: "Hub configuration with NPC spawn positions"
      contains: "npcSpawns"
  key_links:
    - from: "apps/game-server/src/zones/zones.service.ts"
      to: "packages/world-gen/src/generation/hub.ts"
      via: "getHubConfig import"
      pattern: "getHubConfig\\(zoneId\\)"
    - from: "apps/game-server/src/zones/zones.service.ts"
      to: "@into-the-void/npcs"
      via: "NpcRegistry.get"
      pattern: "NpcRegistry\\.get\\(spawn\\.npcId\\)"
---

<objective>
Fix hub zone entity spawning so NPCs appear correctly instead of creatures.

Purpose: Hub zones (faction orbital stations) are safe areas that should only contain NPCs — no creatures. The bug causes creatures to appear in hubs, breaking the safe zone guarantee.

Output: Hub zones correctly spawn NPCs from their definitions, with no creature spawning in hub zones.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Relevant prior work
@.planning/phases/48-npc-definition-system-and-hub-spawns/48-03-SUMMARY.md

# Key files to understand
@apps/game-server/src/zones/zones.service.ts
@packages/world-gen/src/generation/hub.ts
@packages/npcs/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix hub NPC spawning issue</name>
  <files>apps/game-server/src/zones/zones.service.ts, packages/world-gen/src/generation/hub.ts</files>
  <action>
**Step 1: Verify the bug exists**
Add temporary debug logging to `loadHubZone()` in `zones.service.ts`:
- Log when `loadHubZone()` is called with zoneId
- Log the result of `getHubConfig(zoneId)` (should return HubConfig with npcSpawns)
- Log the result of `spawnHubNpcs(zoneId)` (should return Map with NPC entities)
- Log the size of the entities Map before returning ZoneState

**Step 2: Check NpcRegistry initialization**
The NpcRegistry auto-registers NPCs in `packages/npcs/src/index.ts` on module load. Verify that:
- `NpcRegistry` import in `zones.service.ts` triggers module initialization
- Add a log in `spawnHubNpcs()` showing `NpcRegistry.size` to confirm NPCs are registered

**Step 3: Fix identified issue**
Based on diagnosis, apply the fix. Common issues to check:
- **Import order issue**: If NpcRegistry is imported but module side-effects aren't running, ensure import is at top level
- **getHubConfig returning undefined**: Check `isKnownHub()` vs `getHubConfig()` consistency
- **Empty npcSpawns**: Verify hub.ts HUB_CONFIGS has npcSpawns arrays populated

**Step 4: Add defensive logging and guards**
Add permanent logging (at INFO level) for hub zone loading:
```typescript
console.log(`[ZonesService] Loading hub zone: ${zoneId}`);
console.log(`[ZonesService] Hub config: ${hubConfig ? `${hubConfig.displayName} with ${hubConfig.npcSpawns.length} NPC spawns` : 'NOT FOUND'}`);
```

If `getHubConfig` returns undefined for a valid hub zone, throw an error instead of silently returning empty entities.

**Step 5: Verify creatures cannot spawn**
Confirm that `generateHubChunk()` returns `spawnPoints: []` — this is the key guard against creature spawning. The spawn points array must be empty for hub zones.

**Step 6: Remove debug logging**
After confirming the fix works, remove any temporary debug logging added in Step 1. Keep only the INFO-level logging added in Step 4.
  </action>
  <verify>
Run the game server and connect with a client:
1. Enter any hub zone (use portal or recall)
2. Check server logs for hub loading messages
3. Verify zone:state event contains NPC entities (type: 'npc')
4. Verify no creature entities appear in hub zone

Manual test: `pnpm dev:game-server` and observe console output when hub zone loads.
  </verify>
  <done>
Hub zones load NPCs correctly. Server logs show NPC spawn count. Zone state contains NPC entities. No creatures spawn in hub zones.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add hub zone spawn validation tests</name>
  <files>apps/game-server/src/zones/zones.service.ts</files>
  <action>
Add runtime validation in `spawnHubNpcs()` to catch configuration errors early:

1. **Validate hub config exists**: If `getHubConfig(zoneId)` returns undefined, log error and return empty map (defensive, prevents crash)

2. **Validate NPC definitions exist**: For each spawn in `hubConfig.npcSpawns`, check if `NpcRegistry.has(spawn.npcId)` returns true. Log warning if NPC ID not found (allows game to continue with fallback).

3. **Log spawn summary**: After spawning all NPCs, log a summary: `[ZonesService] Spawned ${npcs.size} NPCs in ${zoneId}`

4. **Ensure NPC entity has all required fields**: Verify that each Npc entity created has:
   - `id`: string (unique)
   - `type`: 'npc' (literal)
   - `position`: { x, y, zoneId }
   - `name`: string (from NpcRegistry)
   - `active`: true
   - `npcId`: string
   - `npcType`: NpcType
   - `faction`: NpcFaction

These validations help catch future regressions without breaking the game.
  </action>
  <verify>
1. Start game server: `pnpm dev:game-server`
2. Trigger hub zone load (connect client, enter hub)
3. Verify log output shows: `[ZonesService] Spawned 5 NPCs in hub_verdant` (or appropriate hub)
4. No error or warning logs about missing NPC IDs
  </verify>
  <done>
Hub NPC spawning includes validation and logging. Missing NPC IDs produce warnings. Spawn count logged for each hub zone load.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Hub zones load NPCs**: Enter each faction hub (verdant, helix, nexus, neutral) and verify NPCs appear
2. **No creatures in hubs**: Confirm no creature entities in any hub zone
3. **NPC types correct**: Verify all 5 NPC types visible (Trader, Guard, Faction Rep, Ambient, Service)
4. **Server logs clean**: No error logs, only INFO-level hub loading messages
5. **Open world unchanged**: Enter open world zone (z_0_0) and verify creatures still spawn normally

Test command:
```bash
pnpm dev:game-server &
# Wait for server to start
# Connect client and test hub zones
```
</verification>

<success_criteria>
- Hub zones spawn exactly 5 NPCs each (one per type)
- Zero creatures spawn in hub zones
- Server logs show hub loading with NPC count
- Open world zones continue to spawn creatures normally
- Requirements FIX-01 and FIX-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/52-hub-npc-spawning-fix/52-01-SUMMARY.md`
</output>
