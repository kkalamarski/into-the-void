---
phase: 46-currency-and-hub-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema/characters.ts
  - packages/database/src/queries/characters.ts
  - packages/shared-types/src/core/player.ts
  - apps/game-server/src/game/player.service.ts
  - apps/web/src/ui/hud/HUD.tsx
  - apps/web/src/ui/hud/HUD.css
autonomous: true

must_haves:
  truths:
    - "Player HUD shows a credits balance (e.g., '1,000 cr')"
    - "New characters start with 1000 credits"
    - "Credits persist after logging out and back in"
  artifacts:
    - path: "packages/database/src/schema/characters.ts"
      provides: "credits column definition"
      contains: "credits.*integer"
    - path: "packages/shared-types/src/core/player.ts"
      provides: "Player type with credits field"
      contains: "credits.*number"
    - path: "apps/web/src/ui/hud/HUD.tsx"
      provides: "Credits display in HUD"
      contains: "credits"
  key_links:
    - from: "apps/game-server/src/game/player.service.ts"
      to: "packages/database/src/queries/characters.ts"
      via: "getCharacterById returns credits"
      pattern: "credits"
    - from: "apps/web/src/ui/hud/HUD.tsx"
      to: "player.credits"
      via: "reads from gameStore player"
      pattern: "player\\.credits"
---

<objective>
Add credits currency to the character data model and display it in the HUD.

Purpose: Establishes the currency system foundation for trading in later phases.
Output: Characters have a credits field, HUD shows "X cr" display.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/database/src/schema/characters.ts
@packages/shared-types/src/core/player.ts
@apps/web/src/ui/hud/HUD.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add credits column to characters schema and queries</name>
  <files>
    packages/database/src/schema/characters.ts
    packages/database/src/queries/characters.ts
  </files>
  <action>
1. In `packages/database/src/schema/characters.ts`:
   - Add `credits: integer('credits').notNull().default(1000)` to the characters table definition
   - Position it after `maxHealth` field for logical grouping with other numeric stats

2. In `packages/database/src/queries/characters.ts`:
   - Ensure `getCharacterById` query returns the credits field (it should automatically since it uses select())
   - No explicit changes needed if using wildcard select

3. Run `pnpm db:push` to push schema changes to dev database (no migration file needed for dev).
  </action>
  <verify>
Run `pnpm build` to verify schema compiles. Check that the characters table definition includes credits field.
  </verify>
  <done>
Characters table has `credits` column with default 1000. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add credits to Player type and wire through PlayerService</name>
  <files>
    packages/shared-types/src/core/player.ts
    apps/game-server/src/game/player.service.ts
  </files>
  <action>
1. In `packages/shared-types/src/core/player.ts`:
   - Add `credits: number;` to the `Player` interface
   - Add `credits: number;` to the `PlayerPublic` interface (credits are not secret)

2. In `apps/game-server/src/game/player.service.ts`:
   - In the `authenticate` method where player data is constructed from DB character, include `credits: character.credits`
   - Ensure the Player object returned includes credits field

The existing flow reads character from DB via `getCharacterById`, which now includes credits. Just wire it through to the Player object construction.
  </action>
  <verify>
Run `pnpm build` to verify types compile. Check PlayerService authenticate method includes credits in player object.
  </verify>
  <done>
Player type includes credits. PlayerService returns credits from DB on auth. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Display credits in HUD</name>
  <files>
    apps/web/src/ui/hud/HUD.tsx
    apps/web/src/ui/hud/HUD.css
  </files>
  <action>
1. In `apps/web/src/ui/hud/HUD.tsx`:
   - Add credits display in the `hud-top-left` section, after the stats-section
   - Use a dedicated div: `<div className="credits-display">{player.credits.toLocaleString()} cr</div>`
   - Import GiTwoCoins from react-icons/gi for a coin icon (consistent with other icons)
   - Format: icon + "{credits} cr" where credits uses toLocaleString() for number formatting

2. In `apps/web/src/ui/hud/HUD.css`:
   - Add `.credits-display` styling:
     - Position in the top-left info block
     - Gold/yellow color (#ffd700 or similar) for currency
     - Font size matching other stats
     - Icon size 1rem, margin-right 0.25rem
  </action>
  <verify>
Run `pnpm build` to verify client builds. Start dev server and visually confirm credits display appears in HUD with "1,000 cr" format for a new character.
  </verify>
  <done>
HUD displays credits with coin icon and "X cr" format. New characters show 1,000 cr. Styling matches existing HUD elements.
  </done>
</task>

</tasks>

<verification>
- [ ] `pnpm build` passes for all packages
- [ ] Characters table has credits column (check via drizzle-kit studio or direct DB query)
- [ ] Player type in shared-types includes credits
- [ ] HUD displays credits with proper formatting
</verification>

<success_criteria>
- New character starts with 1,000 credits visible in HUD
- Credits value persists after logout/login (same character shows same value)
- Credits display uses locale-aware number formatting (e.g., "1,000 cr")
</success_criteria>

<output>
After completion, create `.planning/phases/46-currency-and-hub-foundation/46-01-SUMMARY.md`
</output>
