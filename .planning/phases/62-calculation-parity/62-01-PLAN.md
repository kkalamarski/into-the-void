---
phase: 62-calculation-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/game-logic/src/stats/stat-helpers.ts
  - packages/game-logic/src/stats/stat-helpers.test.ts
  - packages/game-logic/src/index.ts
  - apps/web/src/components/ItemTooltip.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Item tooltip delta matches actual stat change when equipped"
    - "Client and server use same calculation functions from game-logic package"
    - "Integration test verifies server stats equal client stats for same equipment"
  artifacts:
    - path: "packages/game-logic/src/stats/stat-helpers.ts"
      provides: "Shared extractItemStats() and computeEquipmentDelta() functions"
      exports: ["extractItemStats", "computeEquipmentDelta"]
    - path: "packages/game-logic/src/stats/stat-helpers.test.ts"
      provides: "Integration tests for client/server parity (PARI-03)"
      contains: "describe.*Client/Server Stat Parity"
    - path: "packages/game-logic/src/index.ts"
      provides: "Package barrel exports for stat helpers"
      contains: "export.*stat-helpers"
    - path: "apps/web/src/components/ItemTooltip.tsx"
      provides: "Refactored tooltip using shared functions"
      contains: "import.*extractItemStats.*from '@into-the-void/game-logic'"
  key_links:
    - from: "apps/web/src/components/ItemTooltip.tsx"
      to: "packages/game-logic/src/stats/stat-helpers.ts"
      via: "import from @into-the-void/game-logic"
      pattern: "import.*extractItemStats.*computeEquipmentDelta.*from '@into-the-void/game-logic'"
    - from: "packages/game-logic/src/stats/stat-helpers.ts"
      to: "packages/game-logic/src/inventory/effects.ts"
      via: "resolveEffectsForTrigger import"
      pattern: "import.*resolveEffectsForTrigger.*from"
---

<objective>
Create shared stat extraction functions in game-logic package and refactor client tooltip to use them, eliminating client/server calculation drift.

Purpose: Currently ItemTooltip.tsx reimplements extractStatBonuses() and computeStatDeltas() locally. This creates drift risk where tooltip predictions diverge from actual server stat changes. By moving to shared functions, client and server use identical logic.

Output: Shared stat-helpers.ts with extractItemStats() and computeEquipmentDelta(), refactored ItemTooltip.tsx, integration tests proving parity.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-calculation-parity/62-RESEARCH.md

# Key files to understand existing patterns
@packages/game-logic/src/stats/char-stats.ts
@packages/game-logic/src/inventory/effects.ts
@packages/game-logic/src/index.ts
@apps/web/src/components/ItemTooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared stat helper functions in game-logic package</name>
  <files>
    packages/game-logic/src/stats/stat-helpers.ts
    packages/game-logic/src/index.ts
  </files>
  <action>
Create `packages/game-logic/src/stats/stat-helpers.ts` with two pure functions:

1. `extractItemStats(itemDef: ItemDefinition): Partial<CharacterStats>`
   - Import `resolveEffectsForTrigger` from `../inventory/effects`
   - Import `CharacterStats` type from `@into-the-void/shared-types`
   - Import `ItemDefinition` type from `@into-the-void/items`
   - Resolve both `on_equip` and `passive` effects (same as current tooltip logic)
   - Aggregate all numeric values into a Partial<CharacterStats> object
   - Return only stats that have non-zero values
   - Add JSDoc documenting this is a pure function used by both client and server

2. `computeEquipmentDelta(hoveredItem: ItemDefinition, equippedItem: ItemDefinition | undefined): Array<{ stat: keyof CharacterStats; delta: number }>`
   - Use extractItemStats() internally for both items
   - Compute delta = hovered - equipped for each stat
   - Return array of { stat, delta } for non-zero deltas
   - Add JSDoc explaining this is for tooltip "vs Equipped" comparisons

Update `packages/game-logic/src/index.ts`:
- Add export line: `export * from './stats/stat-helpers';`
- Place it near the existing `export * from './stats/char-stats';` line

Do NOT include any buff-related logic (Pitfall 4 from research: tooltips show equipment only, not buffs).
  </action>
  <verify>
Run `pnpm build` in packages/game-logic - should compile without errors.
Run `grep -n "extractItemStats\|computeEquipmentDelta" packages/game-logic/src/index.ts` - should show exports.
  </verify>
  <done>
stat-helpers.ts exists with extractItemStats() and computeEquipmentDelta() functions.
Functions are exported from game-logic package index.
Package builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor ItemTooltip to use shared functions</name>
  <files>apps/web/src/components/ItemTooltip.tsx</files>
  <action>
Refactor `apps/web/src/components/ItemTooltip.tsx`:

1. Update imports:
   - Add `extractItemStats, computeEquipmentDelta` to the import from `@into-the-void/game-logic`
   - Remove the separate `resolveEffectsForTrigger` import (no longer needed directly)

2. Delete local functions (lines 33-68 in current file):
   - Remove `function extractStatBonuses(item: ItemDefinition): Record<string, number>`
   - Remove `function computeStatDeltas(hoveredItem, equippedItem): Array<{ stat: string; delta: number }>`

3. Update component code (around lines 93-97):
   - Replace `extractStatBonuses(item)` with `extractItemStats(item)`
   - Replace `computeStatDeltas(item, equippedItem)` with `computeEquipmentDelta(item, equippedItem)`

4. Update render code:
   - The return type of extractItemStats is `Partial<CharacterStats>` (typed keys)
   - For itemStatBonuses display: Object.entries() works the same
   - The return type of computeEquipmentDelta uses `keyof CharacterStats` instead of `string`
   - No render changes needed since both are used the same way

The component should work identically but now uses shared code.
  </action>
  <verify>
Run `pnpm build` in apps/web - should compile without errors.
Run `grep -n "extractStatBonuses\|computeStatDeltas" apps/web/src/components/ItemTooltip.tsx` - should return no matches (local functions removed).
Run `grep -n "extractItemStats\|computeEquipmentDelta" apps/web/src/components/ItemTooltip.tsx` - should show imports and usage.
  </verify>
  <done>
ItemTooltip.tsx imports extractItemStats and computeEquipmentDelta from @into-the-void/game-logic.
Local extractStatBonuses and computeStatDeltas functions are removed.
Web app builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests for client/server parity (PARI-03)</name>
  <files>packages/game-logic/src/stats/stat-helpers.test.ts</files>
  <action>
Create `packages/game-logic/src/stats/stat-helpers.test.ts` with integration tests:

1. Import dependencies:
   ```typescript
   import { describe, it, expect, vi, afterEach } from 'vitest';
   import { computeCharStats } from './char-stats';
   import { extractItemStats, computeEquipmentDelta } from './stat-helpers';
   import { ItemRegistry } from '@into-the-void/items';
   import type { ItemDefinition } from '@into-the-void/items';
   import type { EquipmentJson, InventoryItemJson } from '@into-the-void/database';
   import type { CharacterStats } from '@into-the-void/shared-types';
   ```

2. Reuse test helpers from char-stats.test.ts pattern:
   - `createMockModule(id, stat, amount)` - creates module definition
   - `createInventoryItem(itemId)` - creates InventoryItemJson

3. Write parity tests in `describe('Client/Server Stat Parity (PARI-03)')`:

   Test 1: `it('extractItemStats matches computeCharStats delta for single item')`
   - Create mock module with durability: 20, power: 10
   - Mock ItemRegistry.get() to return it
   - Call extractItemStats(mockItem) for client-side result
   - Call computeCharStats(5, emptyEquipment) for base stats
   - Call computeCharStats(5, equipmentWithModule) for total stats
   - Compute server delta: total - base
   - Assert client extractItemStats result equals server delta for each stat

   Test 2: `it('computeEquipmentDelta matches server calculation for item swap')`
   - Create two mock items: item1 (+20 durability), item2 (+30 durability, +5 toughness)
   - Mock ItemRegistry.get() to return appropriate item by id
   - Call computeEquipmentDelta(item2, item1) for client delta
   - Call computeCharStats for server with item1 equipped
   - Call computeCharStats for server with item2 equipped
   - Compute server delta: withItem2 - withItem1
   - Assert client delta matches server delta for durability and toughness

   Test 3: `it('extractItemStats returns empty object for item with no stat effects')`
   - Create mock item with no effects array or empty effects
   - Assert extractItemStats returns {} or has no stat keys

4. Add afterEach to restore mocks: `vi.restoreAllMocks()`

IMPORTANT: Always mock ItemRegistry.get() in tests (Pitfall 3 from research).
  </action>
  <verify>
Run `pnpm test --filter=game-logic` - all tests should pass.
Run `grep -c "PARI-03" packages/game-logic/src/stats/stat-helpers.test.ts` - should return 1 or more.
  </verify>
  <done>
stat-helpers.test.ts exists with integration tests for PARI-03.
Tests verify extractItemStats matches computeCharStats delta.
Tests verify computeEquipmentDelta matches server swap calculation.
All tests pass.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. **PARI-01 satisfied:** Shared functions exist in game-logic package
   ```bash
   grep -n "export function extractItemStats\|export function computeEquipmentDelta" packages/game-logic/src/stats/stat-helpers.ts
   ```

2. **PARI-02 satisfied:** Client tooltip uses shared functions
   ```bash
   grep -n "import.*extractItemStats.*from '@into-the-void/game-logic'" apps/web/src/components/ItemTooltip.tsx
   ```

3. **PARI-03 satisfied:** Integration tests assert parity
   ```bash
   grep -n "PARI-03\|Client/Server Stat Parity" packages/game-logic/src/stats/stat-helpers.test.ts
   ```

4. **Full build succeeds:**
   ```bash
   pnpm build
   ```

5. **All tests pass:**
   ```bash
   pnpm test
   ```
</verification>

<success_criteria>
- extractItemStats() and computeEquipmentDelta() exist in packages/game-logic/src/stats/stat-helpers.ts
- Functions are exported from packages/game-logic/src/index.ts
- ItemTooltip.tsx imports and uses shared functions (no local reimplementation)
- Integration tests prove client extraction matches server computation
- pnpm build passes
- pnpm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/62-calculation-parity/62-01-SUMMARY.md`
</output>
