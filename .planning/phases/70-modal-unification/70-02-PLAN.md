---
phase: 70-modal-unification
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - apps/web/src/ui/GameUI.tsx
  - apps/web/src/ui/panels/TradingPanel.tsx
  - apps/web/src/ui/panels/TradingPanel.css
autonomous: false

must_haves:
  truths:
    - "GameUI renders only NpcInteractionModal for NPC interactions, not TradingPanel"
    - "Single modal appears when interacting with trader NPC"
    - "ESC key closes the unified modal without cascade bugs"
    - "Trade tab works within unified modal (buy/sell functional)"
  artifacts:
    - path: "apps/web/src/ui/GameUI.tsx"
      provides: "Cleaned imports, no TradingPanel render"
      min_lines: 100
  key_links:
    - from: "apps/web/src/ui/GameUI.tsx"
      to: "NpcInteractionModal"
      via: "conditional render"
      pattern: "interactingNpc && <NpcInteractionModal"
---

<objective>
Complete modal unification by updating GameUI rendering and cleaning up dead code.

Purpose: Eliminate the double-modal bug by ensuring only NpcInteractionModal renders for NPC interactions, then remove orphaned TradingPanel files.

Output: Updated GameUI.tsx without TradingPanel, deleted TradingPanel.tsx and TradingPanel.css, verified unified modal behavior.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-modal-unification/70-01-SUMMARY.md

# Key source files
@apps/web/src/ui/GameUI.tsx
@apps/web/src/ui/panels/TradingPanel.tsx
@apps/web/src/ui/panels/TradingPanel.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GameUI to remove TradingPanel render</name>
  <files>apps/web/src/ui/GameUI.tsx</files>
  <action>
Update GameUI.tsx to remove TradingPanel:

1. **Remove TradingPanel import** (line 24):
   - Delete: `import { TradingPanel } from './panels/TradingPanel';`

2. **Update useNpcStore destructure** (line 34):
   - Change from: `const { interactingNpc, showTrading } = useNpcStore();`
   - To: `const { interactingNpc } = useNpcStore();`

3. **Remove TradingPanel render** (line 116):
   - Delete: `{showTrading && <TradingPanel />}`

Keep all other imports and renders unchanged.
  </action>
  <verify>
Run `npx tsc --noEmit -p apps/web/tsconfig.app.json`. Should compile without errors now that all showTrading references are removed.
  </verify>
  <done>GameUI.tsx only renders NpcInteractionModal for NPC interactions, no reference to TradingPanel or showTrading</done>
</task>

<task type="auto">
  <name>Task 2: Delete TradingPanel.tsx and TradingPanel.css</name>
  <files>apps/web/src/ui/panels/TradingPanel.tsx, apps/web/src/ui/panels/TradingPanel.css</files>
  <action>
Delete the orphaned TradingPanel files:

```bash
rm apps/web/src/ui/panels/TradingPanel.tsx
rm apps/web/src/ui/panels/TradingPanel.css
```

These files are now dead code - all trade functionality has been embedded in NpcInteractionModal via TradeTab component, and styles have been merged into NpcInteractionModal.css.
  </action>
  <verify>
Run `ls apps/web/src/ui/panels/ | grep -i trading` to confirm no TradingPanel files exist.
Run `npx tsc --noEmit -p apps/web/tsconfig.app.json` to verify no import errors.
  </verify>
  <done>TradingPanel.tsx and TradingPanel.css deleted, no import errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify unified NPC modal behavior</name>
  <files>N/A - verification only</files>
  <action>
Human verification of the unified NPC interaction modal.

**What was built:**
Unified NPC interaction modal with tab navigation. The double-modal bug is fixed - trader NPCs now show a single window with Dialogue/Trade/Quests tabs instead of two overlapping modals.

**Changes made:**
1. npcStore simplified (removed showTrading/openTrading/closeTrading, added setActiveTab)
2. TradeTab component embedded in NpcInteractionModal
3. Trade styles merged into NpcInteractionModal.css
4. GameUI no longer renders separate TradingPanel
5. TradingPanel.tsx and TradingPanel.css deleted
6. Intelligent default tab (Quests tab when NPC has available/ready quests)

**Verification steps:**
1. Start the game: `pnpm dev`
2. Log in and enter a hub (any faction hub has traders)
3. Click on a trader NPC:
   - [ ] Verify SINGLE modal window appears (not two overlapping)
   - [ ] Verify tabs visible: Dialogue, Trade, Quests (if NPC has quests)
   - [ ] Click Trade tab - verify buy/sell grid renders
   - [ ] Try buying an item - verify credits deducted, item added to inventory
   - [ ] Try selling an item - verify credits gained, item removed
4. Press ESC key:
   - [ ] Verify modal closes completely
   - [ ] Verify Phaser keyboard is re-enabled (WASD movement works)
5. Click on a quest-giving NPC:
   - [ ] Verify modal defaults to Quests tab (not Dialogue) if NPC has available quests
6. Click on a non-trader NPC (guard/ambient):
   - [ ] Verify Trade tab is not shown
  </action>
  <verify>Type "approved" if all checks pass, or describe any issues found</verify>
  <done>User confirms unified modal works correctly for all NPC interaction scenarios</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/web/tsconfig.app.json` passes without errors
2. `ls apps/web/src/ui/panels/ | grep -i trading` returns empty
3. `grep -r "TradingPanel" apps/web/src/` returns no matches
4. `grep -r "showTrading" apps/web/src/` returns no matches
5. Manual test confirms single modal for trader NPCs
</verification>

<success_criteria>
- GameUI.tsx has no TradingPanel import or render
- TradingPanel.tsx and TradingPanel.css files deleted
- TypeScript compiles without errors
- Trader NPC interaction shows single modal with tabs
- Trade functionality (buy/sell) works within unified modal
- ESC closes modal and re-enables Phaser keyboard
- Quest NPCs default to Quests tab
</success_criteria>

<output>
After completion, create `.planning/phases/70-modal-unification/70-02-SUMMARY.md`
</output>
