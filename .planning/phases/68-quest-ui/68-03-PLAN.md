---
phase: 68-quest-ui
plan: 03
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - apps/web/src/ui/modals/QuestCompleteModal.tsx
  - apps/web/src/ui/modals/QuestCompleteModal.css
autonomous: true

must_haves:
  truths:
    - "Quest completion triggers celebration modal with reward display"
    - "Modal auto-dismisses after 5 seconds (QUEST-44)"
    - "Modal displays quest name and all rewards (credits, XP, items)"
  artifacts:
    - path: "apps/web/src/ui/modals/QuestCompleteModal.tsx"
      provides: "Quest completion celebration modal"
      exports: ["QuestCompleteModal"]
    - path: "apps/web/src/ui/modals/QuestCompleteModal.css"
      provides: "Quest completion modal styling"
      contains: ".quest-complete-overlay"
  key_links:
    - from: "apps/web/src/ui/modals/QuestCompleteModal.tsx"
      to: "useQuestStore"
      via: "completedQuestReward state"
      pattern: "completedQuestReward.*clearCompletedReward"
---

<objective>
Create QuestCompleteModal component that displays when a quest is completed, showing rewards with auto-dismiss.

Purpose: Celebrate quest completion with visual feedback showing earned rewards.
Output: Modal component with 5-second auto-dismiss following LevelUpNotification pattern.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-quest-ui/68-RESEARCH.md
@.planning/phases/68-quest-ui/68-01-SUMMARY.md
@apps/web/src/components/LevelUpNotification.tsx
@apps/web/src/components/LevelUpNotification.css
@apps/web/src/ui/DeathScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuestCompleteModal component</name>
  <files>apps/web/src/ui/modals/QuestCompleteModal.tsx, apps/web/src/ui/modals/QuestCompleteModal.css</files>
  <action>
First create the modals directory if it doesn't exist:
```bash
mkdir -p apps/web/src/ui/modals
```

Create QuestCompleteModal.tsx following LevelUpNotification pattern exactly:

```typescript
import React, { useEffect } from 'react';
import { useQuestStore } from '../../store/questStore';
import './QuestCompleteModal.css';

export const QuestCompleteModal: React.FC = () => {
  const completedQuestReward = useQuestStore(state => state.completedQuestReward);
  const clearCompletedReward = useQuestStore(state => state.clearCompletedReward);

  // QUEST-44: Auto-dismiss after 5 seconds
  useEffect(() => {
    if (!completedQuestReward) return;
    const timer = setTimeout(() => clearCompletedReward(), 5000);
    return () => clearTimeout(timer);
  }, [completedQuestReward, clearCompletedReward]);

  if (!completedQuestReward) return null;

  const { displayName, rewards } = completedQuestReward;

  return (
    <div className="quest-complete-overlay">
      <div className="quest-complete-modal">
        <div className="quest-complete-banner">Quest Complete!</div>
        <div className="quest-complete-name">{displayName}</div>
        <div className="quest-complete-rewards">
          <div className="rewards-title">Rewards</div>
          <div className="rewards-list">
            {rewards.credits && rewards.credits > 0 && (
              <div className="reward-item reward-credits">
                +{rewards.credits.toLocaleString()} Credits
              </div>
            )}
            {rewards.xp && rewards.xp > 0 && (
              <div className="reward-item reward-xp">
                +{rewards.xp.toLocaleString()} XP
              </div>
            )}
            {rewards.items && rewards.items.length > 0 && (
              rewards.items.map((item, i) => (
                <div key={i} className="reward-item reward-item-drop">
                  {item.quantity}x {item.itemId}
                </div>
              ))
            )}
          </div>
        </div>
      </div>
    </div>
  );
};
```

**CSS (QuestCompleteModal.css):**
Follow LevelUpNotification.css structure with z-index between panels and death screen:

```css
.quest-complete-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 200;  /* Above panels (100), below death screen (1000) */
  pointer-events: none;  /* Allow clicks through to game */
  animation: fadeIn 0.3s ease-out;
}

.quest-complete-modal {
  background: linear-gradient(135deg, var(--color-bg-primary) 0%, var(--color-bg-secondary) 100%);
  border: 2px solid var(--color-accent);
  border-radius: 8px;
  padding: 24px 32px;
  text-align: center;
  box-shadow: 0 0 30px rgba(var(--color-accent-rgb), 0.4);
  animation: celebratePop 0.4s ease-out;
}

.quest-complete-banner {
  font-size: 24px;
  font-weight: 700;
  color: var(--color-accent);
  text-transform: uppercase;
  letter-spacing: 2px;
  margin-bottom: 8px;
  text-shadow: 0 0 10px rgba(var(--color-accent-rgb), 0.5);
}

.quest-complete-name {
  font-size: 18px;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 16px;
}

.quest-complete-rewards {
  background: rgba(0, 0, 0, 0.3);
  border-radius: 4px;
  padding: 12px 16px;
}

.rewards-title {
  font-size: 12px;
  font-weight: 600;
  color: var(--color-text-secondary);
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 8px;
}

.rewards-list {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.reward-item {
  font-size: 14px;
  font-weight: 500;
}

.reward-credits {
  color: #ffd700;  /* Gold for credits */
}

.reward-xp {
  color: #00ff88;  /* Green for XP */
}

.reward-item-drop {
  color: var(--color-text-primary);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes celebratePop {
  0% {
    transform: scale(0.8);
    opacity: 0;
  }
  60% {
    transform: scale(1.05);
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}
```

Note: If --color-accent-rgb doesn't exist, use fallback rgba(76, 175, 80, 0.4) for green glow.
Check existing CSS files for variable names.
  </action>
  <verify>Files exist and TypeScript compiles: `cd apps/web && npx tsc --noEmit`</verify>
  <done>QuestCompleteModal.tsx renders celebration modal with rewards, auto-dismisses after 5 seconds (QUEST-43, QUEST-44)</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd apps/web && npx tsc --noEmit`
2. QuestCompleteModal.tsx exports QuestCompleteModal
3. CSS file exists with overlay and modal styles
4. useEffect cleanup properly clears timeout
5. Modal returns null when completedQuestReward is null
</verification>

<success_criteria>
- QuestCompleteModal displays quest name and rewards when completedQuestReward is set
- Modal auto-dismisses after 5 seconds (QUEST-44)
- Proper cleanup of setTimeout in useEffect (no memory leaks)
- Modal has higher z-index than panels but lower than death screen
- Animation provides celebration feel (pop effect)
</success_criteria>

<output>
After completion, create `.planning/phases/68-quest-ui/68-03-SUMMARY.md`
</output>
