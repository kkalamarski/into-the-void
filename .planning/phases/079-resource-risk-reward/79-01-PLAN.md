---
phase: 79-resource-risk-reward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/core/entity.ts
  - packages/entities/src/types.ts
  - packages/entities/src/definitions/minerals.ts
  - packages/entities/src/definitions/plants.ts
  - packages/entities/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Mineral and Plant entities can have a rarity tier (common, rare, epic)"
    - "Rare variants of minerals exist with higher yield multipliers"
    - "Entity definitions include rarity field for spawn system differentiation"
  artifacts:
    - path: "packages/shared-types/src/core/entity.ts"
      provides: "rarity field on Mineral and Plant interfaces"
      contains: "rarity?: NodeRarity"
    - path: "packages/entities/src/types.ts"
      provides: "NodeRarity type and rarity field on definitions"
      contains: "NodeRarity"
    - path: "packages/entities/src/definitions/minerals.ts"
      provides: "Rare mineral variants (3 per biome tier)"
      min_lines: 100
    - path: "packages/entities/src/definitions/plants.ts"
      provides: "Rare plant variants (2 per biome)"
      min_lines: 50
  key_links:
    - from: "packages/shared-types/src/core/entity.ts"
      to: "packages/entities/src/types.ts"
      via: "NodeRarity type import"
      pattern: "NodeRarity"
---

<objective>
Add rarity tier support to resource node entities and define rare variants of minerals and plants.

Purpose: Enable the spawn system to differentiate between common and rare resource nodes, and provide higher-yield variants for dangerous areas.

Output: Extended entity types with rarity field, 6-8 rare mineral definitions, 4-6 rare plant definitions with 1.5-2x yield multipliers.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/079-resource-risk-reward/79-RESEARCH.md
@packages/shared-types/src/core/entity.ts
@packages/entities/src/types.ts
@packages/entities/src/definitions/minerals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NodeRarity type and extend entity interfaces</name>
  <files>
    packages/shared-types/src/core/entity.ts
    packages/entities/src/types.ts
  </files>
  <action>
1. In packages/shared-types/src/core/entity.ts:
   - Add NodeRarity type: `export type NodeRarity = 'common' | 'rare' | 'epic';`
   - Add optional `rarity?: NodeRarity` field to Mineral interface (defaults to 'common' if undefined)
   - Add optional `rarity?: NodeRarity` field to Plant interface (defaults to 'common' if undefined)
   - Export NodeRarity from the module

2. In packages/entities/src/types.ts:
   - Import NodeRarity from @into-the-void/shared-types
   - Add optional `rarity?: NodeRarity` field to MineralDefinition interface
   - Add optional `rarity?: NodeRarity` field to PlantDefinition interface
   - Note in comments: if undefined, rarity defaults to 'common' in spawn logic
  </action>
  <verify>TypeScript compiles: `npx nx run shared-types:build && npx nx run entities:build`</verify>
  <done>
    - NodeRarity type exported from shared-types
    - Mineral and Plant interfaces have optional rarity field
    - MineralDefinition and PlantDefinition have optional rarity field
    - All packages build without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Define rare mineral and plant variants</name>
  <files>
    packages/entities/src/definitions/minerals.ts
    packages/entities/src/definitions/plants.ts
    packages/entities/src/index.ts
  </files>
  <action>
1. In packages/entities/src/definitions/minerals.ts:
   - Create rare variants of existing minerals with naming pattern: `MINERAL_{BASE}_RARE`
   - Set `rarity: 'rare'` on each variant
   - Use same biomes as base mineral
   - Increase yield amounts by 1.5x (minAmount and maxAmount)
   - Add bonus drop chance for reagents (0.3-0.5 chance)
   - Increase respawnSeconds by 2x (rare nodes respawn slower)
   - Increase requiredTier by 1 (rare needs better tools)

   Create these rare minerals:
   - MINERAL_VOID_CRYSTAL_RARE (void_plains) - yields 2-4 instead of 1-3, bonus void_essence
   - MINERAL_PRISMATIC_CRYSTAL_RARE (crystal_caves) - yields 3-5 instead of 2-3, bonus prism_shard
   - MINERAL_VOLCANIC_ORE_RARE (volcanic_ridge) - yields 3-6 instead of 2-4, bonus molten_core
   - MINERAL_COSMIC_FRAGMENT_RARE (starfall_crater) - yields 2-3 instead of 1-2, bonus cosmic_dust

   Create these epic minerals (one per high-tier biome):
   - MINERAL_ANOMALY_CRYSTAL_EPIC (ancient_ruins) - rarity: 'epic', yields 2-4, bonus anomaly_essence

2. In packages/entities/src/definitions/plants.ts (if exists, otherwise create):
   - Follow same pattern for 2-3 rare plant variants
   - PLANT_VOID_BLOOM_RARE (void_plains) - increased harvest yield
   - PLANT_CRYSTAL_MOSS_RARE (crystal_caves) - increased harvest yield

3. In packages/entities/src/index.ts:
   - Export all new rare mineral and plant definitions
   - Add their IDs to ENTITY_IDS constant
  </action>
  <verify>
    - `npx nx run entities:build` compiles
    - `grep -r "RARE" packages/entities/src/definitions/` shows new definitions
    - Entity IDs are exported in index.ts
  </verify>
  <done>
    - 4+ rare mineral definitions with 1.5x yield multipliers
    - 1+ epic mineral definition with 2x yield multiplier
    - 2+ rare plant definitions with increased yields
    - All definitions have rarity field set
    - New IDs exported from entities package
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript compilation
npx nx run shared-types:build
npx nx run entities:build

# Verify rarity type exists
grep "NodeRarity" packages/shared-types/src/core/entity.ts
grep "rarity" packages/entities/src/types.ts

# Verify rare definitions
grep "_RARE\|_EPIC" packages/entities/src/definitions/minerals.ts
```
</verification>

<success_criteria>
- NodeRarity type ('common' | 'rare' | 'epic') exists in shared-types
- Mineral and Plant entity interfaces have optional rarity field
- MineralDefinition and PlantDefinition have optional rarity field
- 5+ rare/epic mineral definitions exist with higher yields
- 2+ rare plant definitions exist with higher yields
- All packages build without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/079-resource-risk-reward/79-01-SUMMARY.md`
</output>
