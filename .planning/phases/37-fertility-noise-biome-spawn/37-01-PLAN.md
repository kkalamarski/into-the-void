---
phase: 37-fertility-noise-biome-spawn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/core/zone.ts
  - packages/world-gen/src/generation/biome.ts
  - packages/world-gen/src/generation/spawn.ts
  - packages/world-gen/src/generation/chunk.ts
autonomous: true

must_haves:
  truths:
    - "getFertilityAt(worldX, worldY) returns Barren, Normal, or Lush based on seeded noise"
    - "FertilityType is importable from @into-the-void/shared-types"
    - "generateSpawnPoints accepts BiomeGenerator instead of BiomeType"
  artifacts:
    - path: "packages/shared-types/src/core/zone.ts"
      provides: "FertilityType union type"
      contains: "FertilityType"
    - path: "packages/world-gen/src/generation/biome.ts"
      provides: "getFertilityAt method on BiomeGenerator"
      contains: "getFertilityAt"
    - path: "packages/world-gen/src/generation/spawn.ts"
      provides: "Updated generateSpawnPoints signature"
      contains: "biomeGenerator: BiomeGenerator"
  key_links:
    - from: "packages/world-gen/src/generation/spawn.ts"
      to: "BiomeGenerator.getFertilityAt"
      via: "biomeGenerator parameter"
      pattern: "biomeGenerator\\.getFertilityAt"
    - from: "packages/world-gen/src/generation/chunk.ts"
      to: "generateSpawnPoints"
      via: "passing this.biomeGenerator"
      pattern: "generateSpawnPoints.*this\\.biomeGenerator"
---

<objective>
Add fertility noise layer to BiomeGenerator and update spawn system signature

Purpose: Enables spawn density modulation based on Barren/Normal/Lush fertility zones
Output: FertilityType in shared-types, getFertilityAt() on BiomeGenerator, updated generateSpawnPoints signature
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/37-fertility-noise-biome-spawn/37-RESEARCH.md

@packages/shared-types/src/core/zone.ts
@packages/world-gen/src/generation/biome.ts
@packages/world-gen/src/generation/spawn.ts
@packages/world-gen/src/generation/chunk.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FertilityType to shared-types and getFertilityAt to BiomeGenerator</name>
  <files>
    packages/shared-types/src/core/zone.ts
    packages/world-gen/src/generation/biome.ts
  </files>
  <action>
In packages/shared-types/src/core/zone.ts:
- Add `export type FertilityType = 'Barren' | 'Normal' | 'Lush';` after the BiomeType import (near top of file)

In packages/world-gen/src/generation/biome.ts:
- Import FertilityType from @into-the-void/shared-types
- Add private fertilityNoise property (SimplexNoise) to BiomeGenerator class
- In constructor, add: `this.fertilityNoise = new SimplexNoise(\`${worldSeed}_fertility\`);`
- Add private constant FERTILITY_SCALE = 0.0012 (between temperature 0.001 and moisture 0.0015)
- Add method:
```typescript
getFertilityAt(worldX: number, worldY: number): FertilityType {
  const raw = this.fertilityNoise.fbm(
    worldX * FERTILITY_SCALE,
    worldY * FERTILITY_SCALE,
    3  // 3 octaves for variation without tiny patches
  );
  const normalized = (raw + 1) / 2; // Map [-1, 1] to [0, 1]
  if (normalized < 0.33) return 'Barren';
  if (normalized < 0.66) return 'Normal';
  return 'Lush';
}
```
- Make FERTILITY_SCALE a private readonly class property or local constant inside the method
  </action>
  <verify>
Run `pnpm build` - BiomeGenerator compiles with getFertilityAt method.
Run `grep -n "getFertilityAt" packages/world-gen/src/generation/biome.ts` - method exists.
Run `grep -n "FertilityType" packages/shared-types/src/core/zone.ts` - type exported.
  </verify>
  <done>
FertilityType exported from shared-types. BiomeGenerator.getFertilityAt() returns Barren/Normal/Lush based on seeded noise using a 4th SimplexNoise instance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update generateSpawnPoints signature to accept BiomeGenerator</name>
  <files>
    packages/world-gen/src/generation/spawn.ts
    packages/world-gen/src/generation/chunk.ts
  </files>
  <action>
In packages/world-gen/src/generation/spawn.ts:
- Import BiomeGenerator from './biome'
- Change function signature from:
  `generateSpawnPoints(worldSeed, chunkX, chunkY, biome: BiomeType, collisionMap)`
  to:
  `generateSpawnPoints(worldSeed, chunkX, chunkY, biomeGenerator: BiomeGenerator, collisionMap)`
- Inside the function, get the chunk-center biome for density calculation:
  ```typescript
  const centerX = chunkX * ZONE_SIZE + ZONE_SIZE / 2;
  const centerY = chunkY * ZONE_SIZE + ZONE_SIZE / 2;
  const biome = biomeGenerator.getChunkBiome(chunkX, chunkY, ZONE_SIZE);
  const config = BIOME_SPAWN_CONFIGS[biome];
  ```
- Keep the rest of the function as-is for now (fertility multiplier and per-tile sampling added in Plan 37-02)

In packages/world-gen/src/generation/chunk.ts:
- Update the generateSpawnPoints call in WorldGenerator.generateChunk():
  From: `generateSpawnPoints(this.worldSeed, chunkX, chunkY, biome, collisions)`
  To: `generateSpawnPoints(this.worldSeed, chunkX, chunkY, this.biomeGenerator, collisions)`
  </action>
  <verify>
Run `pnpm build` - no compilation errors in world-gen or dependent packages.
Run `pnpm test` - existing tests still pass (spawn behavior unchanged, just signature updated).
  </verify>
  <done>
generateSpawnPoints accepts BiomeGenerator parameter. WorldGenerator.generateChunk() passes this.biomeGenerator. Spawn behavior is unchanged (biome still derived from chunk center).
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `FertilityType` importable: `import { FertilityType } from '@into-the-void/shared-types'` compiles
- `BiomeGenerator.getFertilityAt()` callable: `new BiomeGenerator('seed').getFertilityAt(0, 0)` returns 'Barren' | 'Normal' | 'Lush'
- `generateSpawnPoints` signature uses `BiomeGenerator` instead of `BiomeType`
</verification>

<success_criteria>
1. FertilityType type exists in shared-types/src/core/zone.ts
2. BiomeGenerator has private fertilityNoise property initialized in constructor
3. getFertilityAt(worldX, worldY) method returns FertilityType
4. generateSpawnPoints signature accepts BiomeGenerator as 4th parameter
5. WorldGenerator.generateChunk() passes this.biomeGenerator to generateSpawnPoints
6. Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-fertility-noise-biome-spawn/37-01-SUMMARY.md`
</output>
