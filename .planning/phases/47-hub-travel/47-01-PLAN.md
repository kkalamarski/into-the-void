---
phase: 47-hub-travel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/world-gen/src/generation/terrain.ts
  - packages/world-gen/src/generation/structures.ts
  - packages/tiles/src/registry.ts
  - packages/tiles/src/ids.ts
autonomous: true

must_haves:
  truths:
    - "Portal structure tile type exists in the tile registry"
    - "Portal structures spawn in open-world zones at predictable locations"
    - "Portal tiles are walkable but visually distinct"
  artifacts:
    - path: "packages/tiles/src/ids.ts"
      provides: "PORTAL tile ID constant"
      contains: "PORTAL"
    - path: "packages/tiles/src/registry.ts"
      provides: "Portal tile definition in TileRegistry"
      contains: "portal"
    - path: "packages/world-gen/src/generation/structures.ts"
      provides: "Portal spawn logic in open-world chunks"
      contains: "portal"
  key_links:
    - from: "packages/world-gen/src/generation/structures.ts"
      to: "packages/tiles/src/ids.ts"
      via: "import TILE_IDS"
      pattern: "TILE_IDS\\.PORTAL"
---

<objective>
Add portal structure tile type to the tile registry and place portal structures in open-world zones.

Purpose: Portals are the primary means for players to travel between open-world zones and their faction hub. This plan establishes the portal as a recognizable, interactable tile type.

Output: PORTAL tile ID in tiles package, portal spawned in open-world chunks via structures.ts.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/tiles/src/ids.ts
@packages/tiles/src/registry.ts
@packages/world-gen/src/generation/structures.ts
@packages/world-gen/src/generation/terrain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PORTAL tile ID to tiles package</name>
  <files>
    packages/tiles/src/ids.ts
    packages/tiles/src/registry.ts
  </files>
  <action>
1. In `packages/tiles/src/ids.ts`:
   - Add `PORTAL: 'portal'` to the TILE_IDS constant object
   - Add `'portal'` to the TileIdKey union type

2. In `packages/tiles/src/registry.ts`:
   - Add portal tile definition to the registry:
     ```typescript
     portal: {
       id: TILE_IDS.PORTAL,
       name: 'Portal',
       walkable: true,       // Players can walk on portals
       transparent: true,    // Can see through
       sprite: 'tile_portal',
       elevation: 0,
       category: 'structure',
     },
     ```
   - Note: Sprite `tile_portal` may not exist yet - use `tile_void_floor` as fallback for now (Phase 48+ can add visual)

Build verification: `nx run tiles:build` should pass.
  </action>
  <verify>
    `nx run tiles:build --skip-nx-cache` passes; grep for "PORTAL" in ids.ts returns the constant
  </verify>
  <done>
    TILE_IDS.PORTAL exists and TileRegistry includes a portal tile definition with walkable=true
  </done>
</task>

<task type="auto">
  <name>Task 2: Place portal structures in open-world chunks</name>
  <files>
    packages/world-gen/src/generation/structures.ts
  </files>
  <action>
1. In `packages/world-gen/src/generation/structures.ts`:
   - Import TILE_IDS from `@into-the-void/tiles` if not already imported
   - Add a `placePortals()` function that places 1-2 portal tiles per chunk:
     - Use seeded random to pick positions (avoid chunk edges, stay within tiles 16-48 to be in walkable area)
     - Only place portal if the tile is currently walkable (collision=false)
     - Set the tile to PORTAL tile ID (convert to number using existing pattern)
     - Keep collision as FALSE (portals are walkable)
     - Heights remain 0 (floor level)
   - Call `placePortals()` from `generateStructures()` after other structures
   - Use deterministic seeding so portals appear at same positions on re-generation

2. Portal placement strategy:
   - 1 portal per chunk guaranteed
   - Position: center-ish region (x/y in range 20-44) to avoid walls/edges
   - Avoid placing on existing structure tiles

3. Return the portal positions in the structures array as type: 'feature' with tileId 'portal'

Build verification: `nx run world-gen:build` should pass.
  </action>
  <verify>
    `nx run world-gen:build --skip-nx-cache` passes; grep for "portal" in structures.ts shows placement logic
  </verify>
  <done>
    Open-world chunks contain 1 portal tile at a deterministic position; portal appears in the chunk's tile array
  </done>
</task>

<task type="auto">
  <name>Task 3: Add portal tile to terrain.ts mapping</name>
  <files>
    packages/world-gen/src/generation/terrain.ts
  </files>
  <action>
1. In `packages/world-gen/src/generation/terrain.ts`:
   - Add PORTAL to the TileId enum (for backwards compatibility with legacy code):
     ```typescript
     PORTAL = 16,  // Next available ID after CRATER_DEBRIS (15)
     ```
   - Add PORTAL to the `tileIdToString()` mapping:
     ```typescript
     [TileId.PORTAL]: TILE_IDS.PORTAL,
     ```
   - This ensures the numeric tile ID from chunk generation can convert to string ID for client rendering

2. Also ensure TILE_IDS import includes PORTAL (should be automatic if already importing TILE_IDS)

Build verification: Full `pnpm build` should pass (tests all packages).
  </action>
  <verify>
    `pnpm build` passes; TileId.PORTAL exists with value 16
  </verify>
  <done>
    TileId.PORTAL exists in terrain.ts enum and maps correctly to string 'portal' via tileIdToString()
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes for all packages
2. `grep -r "PORTAL" packages/tiles/src/` shows constant in ids.ts and registry.ts
3. `grep -r "portal" packages/world-gen/src/generation/` shows placement in structures.ts and mapping in terrain.ts
</verification>

<success_criteria>
- TILE_IDS.PORTAL constant exists in tiles package
- TileRegistry includes portal definition with walkable=true
- TileId.PORTAL enum value (16) exists in terrain.ts
- generateStructures() places 1 portal per open-world chunk
- Portal positions are deterministic (same seed = same position)
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-hub-travel/47-01-SUMMARY.md`
</output>
