---
phase: 47-hub-travel
plan: 05
type: execute
wave: 1
depends_on: ["47-04"]
files_modified:
  - packages/world-gen/src/generation/hub.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Hub zones contain a portal tile (ID 16) that players can step on to leave"
    - "Stepping on the hub portal triggers portal:use and returns player to open world"
  artifacts:
    - path: "packages/world-gen/src/generation/hub.ts"
      provides: "Portal tile placement in generateHubChunk()"
      contains: "16"
  key_links:
    - from: "packages/world-gen/src/generation/hub.ts"
      to: "apps/web/src/game/scenes/WorldScene.ts"
      via: "tile ID 16 triggers checkPortalTile()"
      pattern: "16"
---

<objective>
Place a portal tile (ID 16) in hub zones so players can leave by stepping on it.

Purpose: Closes the final verification gap — hub exit. The client's `checkPortalTile()` already emits `portal:use` when detecting tile 16. The server's `handlePortalUse` already delegates to `handleHubLeave` when in a hub zone. The only missing piece is the actual portal tile in the hub layout.

Output: Hub zones have a portal tile at position (32, 32) — center of walkable area.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hub-travel/47-04-SUMMARY.md
@.planning/phases/47-hub-travel/47-VERIFICATION.md
@packages/world-gen/src/generation/hub.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add portal tile at hub center</name>
  <files>
    packages/world-gen/src/generation/hub.ts
  </files>
  <action>
1. In `packages/world-gen/src/generation/hub.ts`:
   - Define a constant for portal tile ID at the top of the file:
     ```typescript
     const PORTAL_TILE_ID = 16; // TileId.PORTAL from terrain.ts
     ```

2. In `generateHubChunk()`, after the tile generation loop (after line 95, before the return statement):
   - Place a portal tile at the center of the walkable area (position 32, 32):
     ```typescript
     // Place portal tile at hub center for exit
     const portalX = 32;
     const portalY = 32;
     tiles[portalY][portalX] = PORTAL_TILE_ID;
     // Portal is walkable (collision already false from floor tile generation)
     ```

3. This position (32, 32) is:
   - Inside the walkable area (perimeter is 0-7 and 56-63)
   - Near the faction spawn point (which is typically at 32, 32 per getFactionRespawnPosition)
   - Easy to find when entering the hub

Build verification: `pnpm build` passes.
  </action>
  <verify>
    `pnpm build` passes; grep for "PORTAL_TILE_ID" in hub.ts shows the constant and usage
  </verify>
  <done>
    Hub zones contain tile ID 16 at position (32, 32); stepping on it triggers checkPortalTile → portal:use → handleHubLeave → return to open world
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes for all packages
2. `grep -r "PORTAL_TILE_ID\|16" packages/world-gen/src/generation/hub.ts` shows portal placement
3. Manual test: teleport to hub (H key or portal), walk to center (32,32), step on portal → should return to open world position
</verification>

<success_criteria>
- Hub zones contain portal tile (ID 16) at center position
- checkPortalTile() in WorldScene detects tile 16 and emits portal:use
- Server handlePortalUse delegates to handleHubLeave (already implemented)
- Player returns to their saved lastWorldPosition
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-hub-travel/47-05-SUMMARY.md`
</output>
