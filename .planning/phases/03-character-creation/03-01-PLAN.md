---
phase: 03-character-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/characters.css
  - apps/web/src/routes/router.tsx
autonomous: true

must_haves:
  truths:
    - "Faction selection CSS styles exist for radio button card pattern"
    - "Character create route is registered and protected"
  artifacts:
    - path: "apps/web/src/styles/characters.css"
      provides: "Faction selection card styles"
      contains: ".faction-options"
    - path: "apps/web/src/routes/router.tsx"
      provides: "Character create route configuration"
      contains: "/character-create"
  key_links:
    - from: "apps/web/src/routes/router.tsx"
      to: "CharacterCreateScreen"
      via: "lazy import"
      pattern: "lazy.*CharacterCreateScreen"
---

<objective>
Add faction selection styles and character create route configuration for Phase 03 foundation.

Purpose: Provides CSS foundation for faction radio cards and route infrastructure for character creation screen.
Output: Extended characters.css with faction selection styles, router.tsx with character-create route.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-character-creation/03-RESEARCH.md
@apps/web/src/styles/characters.css
@apps/web/src/routes/router.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Faction Selection CSS Styles</name>
  <files>apps/web/src/styles/characters.css</files>
  <action>
Extend characters.css with faction selection card styles. Add these CSS rules:

1. `.character-create-card` - Container with max-width: 600px for wider form
2. `.form-hint` - Small helper text (12px, text-secondary, margin-top: 6px)
3. `.faction-options` - Grid layout: 2 columns (grid-template-columns: repeat(2, 1fr)), gap: 12px, margin-top: 12px
4. `.faction-card` - Label wrapper with cursor: pointer, position: relative, display: block
5. `.faction-radio` - Hidden radio input using position: absolute, opacity: 0, width/height: 0 (NOT display:none - keeps accessibility)
6. `.faction-card-content` - Card styling: bg-tertiary background, 2px solid bg-tertiary border, border-radius: 8px, padding: 16px, flex column with gap: 8px
7. `.faction-card:hover .faction-card-content` - Hover state: border-color: accent
8. `.faction-radio:checked + .faction-card-content` - Selected state: border-color: accent, background: rgba(123, 104, 238, 0.1)
9. `.faction-radio:focus-visible + .faction-card-content` - Focus state: outline: 2px solid accent, outline-offset: 2px
10. `.faction-indicator` - Color bar: width: 100%, height: 4px, border-radius: 2px
11. `.faction-name` - Text: 16px, font-weight: 600, text-primary
12. `.faction-description` - Text: 12px, text-secondary

Add responsive rule at bottom:
```css
@media (max-width: 500px) {
  .faction-options {
    grid-template-columns: 1fr;
  }
}
```

Use CSS variables from global.css (--color-bg-tertiary, --color-accent, --color-text-primary, etc.) matching existing patterns in characters.css.
  </action>
  <verify>CSS file contains .faction-options, .faction-card, .faction-radio selectors. No syntax errors (check with prettier or visual inspection).</verify>
  <done>Faction selection CSS styles added to characters.css with hover, selected, and focus states.</done>
</task>

<task type="auto">
  <name>Task 2: Add Character Create Route</name>
  <files>apps/web/src/routes/router.tsx</files>
  <action>
Add character-create route to router.tsx:

1. Add new route object for `/character-create` path:
```typescript
{
  path: '/character-create',
  lazy: () => import('../screens/CharacterCreateScreen'),
}
```

2. This route uses the lazy() pattern for code splitting - action and component will be exported from CharacterCreateScreen.tsx (created in Plan 02).

3. No loader needed - the action pattern handles auth via existing apiCall utility (which redirects on 401).

4. Place this route after `/character-select` and before `/game` for logical ordering.

Note: The CharacterCreateScreen.tsx file does not exist yet - this is intentional. React Router lazy loading means the import is deferred until route navigation. Plan 02 will create the screen file.
  </action>
  <verify>Run `grep -n "character-create" apps/web/src/routes/router.tsx` to confirm route exists. TypeScript should compile (npm run typecheck or tsc --noEmit).</verify>
  <done>Character create route registered at /character-create with lazy loading.</done>
</task>

</tasks>

<verification>
1. `grep -n "faction-options" apps/web/src/styles/characters.css` returns results
2. `grep -n "character-create" apps/web/src/routes/router.tsx` returns results
3. No TypeScript or CSS syntax errors
</verification>

<success_criteria>
- Faction selection CSS styles exist in characters.css
- Character create route configured in router.tsx
- Route uses lazy loading pattern
- CSS uses design tokens from global.css
</success_criteria>

<output>
After completion, create `.planning/phases/03-character-creation/03-01-SUMMARY.md`
</output>
