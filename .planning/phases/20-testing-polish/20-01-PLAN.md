---
phase: 20-testing-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/styles/loading.css
  - apps/web/src/game/systems/PathfindingController.ts
  - apps/web/src/game/scenes/WorldScene.ts
  - apps/web/src/components/GameContainer.tsx
autonomous: true

must_haves:
  truths:
    - "Chunk loading indicator visible above minimap without overlap"
    - "PathfindingController graphics destroyed on scene shutdown"
    - "ChunkManager state cleared on socket disconnect"
    - "Loading indicator fades in/out subtly instead of instant show/hide"
  artifacts:
    - path: "apps/web/src/styles/loading.css"
      provides: "Loading indicator positioned above minimap with opacity transition"
      contains: "bottom: 210px"
    - path: "apps/web/src/game/systems/PathfindingController.ts"
      provides: "destroy method for cleanup"
      exports: ["PathfindingController"]
    - path: "apps/web/src/game/scenes/WorldScene.ts"
      provides: "PathfindingController.destroy call, getChunkManager() accessor"
      contains: "getChunkManager"
  key_links:
    - from: "apps/web/src/game/scenes/WorldScene.ts"
      to: "PathfindingController.destroy"
      via: "shutdown method"
      pattern: "this\\.pathfindingController\\?.destroy"
    - from: "apps/web/src/game/scenes/WorldScene.ts"
      to: "ChunkManager"
      via: "public accessor"
      pattern: "getChunkManager.*ChunkManager"
    - from: "apps/web/src/components/GameContainer.tsx"
      to: "ChunkManager.clear"
      via: "socket disconnect handler with cleanup"
      pattern: "getChunkManager.*clear.*off.*disconnect"
---

<objective>
Fix known issues identified during Phase 20 research before executing validation test scenarios.

Purpose: Resolve pre-existing visual conflicts and memory leak risks discovered during codebase inspection, ensuring clean baseline for testing.

Output: Bug-free chunk loading indicator, proper PathfindingController cleanup, and socket disconnect handling.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-testing-polish/20-RESEARCH.md
@apps/web/src/styles/loading.css
@apps/web/src/game/systems/PathfindingController.ts
@apps/web/src/game/scenes/WorldScene.ts
@apps/web/src/components/GameContainer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition chunk loading indicator above minimap</name>
  <files>apps/web/src/styles/loading.css</files>
  <action>
In `.chunk-loading-indicator` CSS rule:
1. Change `bottom: 20px` to `bottom: 210px` (180px minimap + 20px gap + 10px buffer)
2. Add `opacity: 0` initially and `transition: opacity 0.3s ease 0.2s` for subtle fade-in
3. Add a new class `.chunk-loading-indicator.visible` with `opacity: 1` and `transition-delay: 0`

The 0.2s delay on appearance prevents brief flashes when fast loads complete, matching the "subtle" UX decision. The instant fade-out (delay: 0 on visible) ensures responsive hiding.

Do NOT add any animation or transition to chunk tiles themselves - only the loading indicator.
  </action>
  <verify>
CSS file contains:
- `bottom: 210px` in .chunk-loading-indicator
- `opacity` and `transition` properties
- `.chunk-loading-indicator.visible` rule
  </verify>
  <done>Loading indicator positioned above minimap with subtle fade transition</done>
</task>

<task type="auto">
  <name>Task 2: Add destroy method to PathfindingController</name>
  <files>apps/web/src/game/systems/PathfindingController.ts</files>
  <action>
Add a public `destroy()` method to PathfindingController class that:
1. Calls `this.cancelPath()` to clear any active path and timer
2. If `this.pathGraphics` exists, call `this.pathGraphics.destroy()` and set to null
3. Set `this.scene = null` and `this.isoTransform = null` to clear references

Place the method after `getRemainingSteps()` and before `getDirection()` for logical ordering.

This ensures the Phaser Graphics object is properly destroyed when the scene shuts down, preventing memory leaks during chunk transitions.
  </action>
  <verify>
TypeScript compiles: `pnpm nx run web:build --skip-nx-cache 2>&1 | tail -20`
PathfindingController has destroy method that destroys pathGraphics
  </verify>
  <done>PathfindingController.destroy() method exists and properly cleans up Graphics object</done>
</task>

<task type="auto">
  <name>Task 3: Wire cleanup calls to WorldScene shutdown and socket disconnect</name>
  <files>apps/web/src/game/scenes/WorldScene.ts, apps/web/src/components/GameContainer.tsx</files>
  <action>
**In WorldScene.ts:**

1. Add a public accessor method for ChunkManager (required before GameContainer can call it):
```typescript
public getChunkManager(): ChunkManager | null {
  return this.chunkManager;
}
```
Place this method near other public accessors in the class.

2. In shutdown() method: Replace `this.pathfindingController?.cancelPath()` with `this.pathfindingController?.destroy()`
   This ensures Graphics object is destroyed, not just path cleared.

**In GameContainer.tsx:**

1. Add a socket disconnect handler inside the useEffect that manages socket events:
```typescript
const handleDisconnect = () => {
  const worldScene = gameRef.current?.scene?.getScene('WorldScene') as WorldScene | undefined;
  worldScene?.getChunkManager()?.clear();
};
gameSocket.on('disconnect', handleDisconnect);
```

2. **CRITICAL:** Add cleanup in the useEffect return function to prevent listener leaks:
```typescript
return () => {
  // ... existing cleanup ...
  gameSocket.off('disconnect', handleDisconnect);
};
```

This prevents stale 'loading' entries from persisting after reconnection AND properly cleans up the listener to avoid memory leaks (MEM-02 compliance).
  </action>
  <verify>
TypeScript compiles: `pnpm nx run web:build --skip-nx-cache 2>&1 | tail -20`
WorldScene has public getChunkManager() method: `grep -n "getChunkManager" apps/web/src/game/scenes/WorldScene.ts`
WorldScene.shutdown calls pathfindingController.destroy()
GameContainer has socket disconnect handling with cleanup: `grep -A5 "disconnect" apps/web/src/components/GameContainer.tsx | grep -E "(on|off).*disconnect"`
  </verify>
  <done>Cleanup properly wired: PathfindingController destroyed on shutdown, ChunkManager cleared on disconnect with proper listener cleanup</done>
</task>

<task type="auto">
  <name>Task 4: Update GameContainer to use visible class for loading indicator</name>
  <files>apps/web/src/components/GameContainer.tsx</files>
  <action>
Update the chunk loading indicator rendering in GameContainer:

1. Instead of conditionally rendering with `{chunksLoading > 0 && <div className="chunk-loading-indicator">...}`, always render the indicator but control visibility with the CSS class
2. Change to: `<div className={\`chunk-loading-indicator \${chunksLoading > 0 ? 'visible' : ''}\`}>...</div>`
3. Move the indicator element outside any conditional render so CSS transitions work

This allows the CSS opacity transition to animate smoothly. The element is always in DOM but visually hidden when not loading.
  </action>
  <verify>
TypeScript compiles: `pnpm nx run web:build --skip-nx-cache 2>&1 | tail -20`
Loading indicator uses className toggle instead of conditional render
  </verify>
  <done>Loading indicator uses CSS class toggle for smooth opacity transitions</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm nx run web:build --skip-nx-cache` - TypeScript compiles without errors
2. Loading indicator CSS has `bottom: 210px` and opacity transition
3. PathfindingController has `destroy()` method
4. WorldScene.shutdown calls `pathfindingController.destroy()`
5. WorldScene has public `getChunkManager()` accessor
6. GameContainer has socket disconnect handler AND cleanup (`gameSocket.off('disconnect', ...)`)
7. Loading indicator rendered with class toggle pattern
</verification>

<success_criteria>
- All known issues from research pitfalls are fixed
- TypeScript builds successfully
- No new console errors introduced
- Clean baseline established for Phase 20 Plan 02 validation testing
</success_criteria>

<output>
After completion, create `.planning/phases/20-testing-polish/20-01-SUMMARY.md`
</output>
