---
phase: 127-particle-weather-system
plan: 03
type: execute
wave: 3
depends_on: [127-02]
files_modified: []
autonomous: true
requirements: [WTHR-01, WTHR-02, WTHR-03, WTHR-04, WTHR-05]

must_haves:
  truths:
    - "TypeScript compiles with no errors across the entire project"
    - "WeatherSystem has configs for all 16 biomes with appropriate weather types"
    - "All integration points in WorldScene are wired (create, commitZoneTransition, fullZoneReset, renderChunk, shutdown)"
    - "No orphan emitter paths exist — every emitter creation path has a corresponding destroy path"
  artifacts:
    - path: "apps/web/src/game/systems/WeatherSystem.ts"
      provides: "Complete weather system"
      exports: ["WeatherSystem"]
    - path: "apps/web/src/game/scenes/WorldScene.ts"
      provides: "WeatherSystem integration"
      contains: "weatherSystem"
    - path: "apps/web/src/game/scenes/PreloadScene.ts"
      provides: "weather-pixel texture"
      contains: "weather-pixel"
---

<objective>
Verify the complete weather system build passes all checks.

Purpose: Confirm that all 5 WTHR requirements are satisfied: viewport-fixed particles, all 16 biome configs, smooth transitions, correct depth ordering, and proper cleanup.

Output: Build verification results confirming phase completion.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-particle-weather-system/127-01-SUMMARY.md
@.planning/phases/127-particle-weather-system/127-02-SUMMARY.md

@apps/web/src/game/systems/WeatherSystem.ts
@apps/web/src/game/scenes/WorldScene.ts
@apps/web/src/game/scenes/PreloadScene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build verification for all WTHR requirements</name>
  <files></files>
  <action>
Run the following verification checks and fix any issues found:

**1. TypeScript compilation (full project):**
```bash
cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit --project apps/web/tsconfig.json
```
Must exit 0. If there are type errors, fix them.

**2. WTHR-01 — Viewport-fixed particles:**
```bash
grep -n "setScrollFactor(0)" apps/web/src/game/systems/WeatherSystem.ts
```
Must find at least 1 match confirming emitters are viewport-relative.

**3. WTHR-02 — All 16 biomes have weather configs:**
Verify all 16 BiomeType values appear in WEATHER_CONFIGS:
```bash
for biome in void_plains crystal_caves toxic_wastes ancient_ruins frozen_expanse volcanic_ridge fungal_forest starfall_crater miasma_marshes petrified_expanse tidal_pools kelp_forests deep_trenches void_rift crystalline_wastes bioluminescent_depths; do
  grep -q "$biome" apps/web/src/game/systems/WeatherSystem.ts || echo "MISSING: $biome"
done
```
Must produce no output (all biomes present).

**4. WTHR-03 — Weather transitions wired:**
```bash
# Crossfade in commitZoneTransition
grep -n "weatherSystem.*setBiome.*false" apps/web/src/game/scenes/WorldScene.ts
# Instant in fullZoneReset
grep -n "weatherSystem.*setBiome.*true" apps/web/src/game/scenes/WorldScene.ts
```
Both must return matches.

**5. WTHR-04 — Depth budget:**
```bash
grep -n "setDepth(9500)" apps/web/src/game/systems/WeatherSystem.ts
```
Must find at least 1 match confirming weather renders above terrain but below UI.

**6. WTHR-05 — Cleanup on shutdown:**
```bash
grep -n "weatherSystem.*destroy\|weatherSystem = null" apps/web/src/game/scenes/WorldScene.ts
```
Must find both destroy() call and null assignment.

**7. Minimap exclusion:**
```bash
grep -n "updateMinimapWeatherIgnore\|minimapCamera.*ignore.*weather\|getActiveEmitters" apps/web/src/game/scenes/WorldScene.ts
```
Must find minimap ignore calls.

**8. Pixel texture:**
```bash
grep -n "weather-pixel" apps/web/src/game/scenes/PreloadScene.ts
```
Must find the generateTexture call.

**9. Lint check:**
```bash
cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx nx run web:lint 2>&1 | tail -5
```
Must pass or only have pre-existing warnings.

If any check fails, fix the issue and re-run. Report all results.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | tail -3 && echo "---" && for biome in void_plains crystal_caves toxic_wastes ancient_ruins frozen_expanse volcanic_ridge fungal_forest starfall_crater miasma_marshes petrified_expanse tidal_pools kelp_forests deep_trenches void_rift crystalline_wastes bioluminescent_depths; do grep -q "$biome" apps/web/src/game/systems/WeatherSystem.ts || echo "MISSING: $biome"; done && echo "All biomes verified"</automated>
  </verify>
  <done>All 5 WTHR requirements verified: viewport-fixed emitters (WTHR-01), all 16 biome configs (WTHR-02), transition hooks wired (WTHR-03), correct depth at 9500 (WTHR-04), cleanup on shutdown (WTHR-05). TypeScript and lint pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project apps/web/tsconfig.json` exits 0
2. All 16 biome types present in WeatherSystem.ts
3. setScrollFactor(0) present (WTHR-01)
4. setBiome wired in commitZoneTransition (false) and fullZoneReset (true) (WTHR-03)
5. setDepth(9500) present (WTHR-04)
6. destroy() called in WorldScene shutdown (WTHR-05)
7. weather-pixel texture in PreloadScene
8. Minimap ignore calls present
</verification>

<success_criteria>
- Zero TypeScript compilation errors
- All 16 biomes have weather configs (no missing biomes)
- All 5 WTHR requirements verifiable via grep patterns
- Lint passes (or pre-existing warnings only)
- Phase 127 is complete and ready for Phase 128
</success_criteria>

<output>
After completion, create `.planning/phases/127-particle-weather-system/127-03-SUMMARY.md`
</output>
