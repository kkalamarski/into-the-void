---
phase: 87-item-integration-balance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/items/src/definitions/exotic-suits.ts
  - packages/items/src/definitions/exotic-tools.ts
autonomous: true

must_haves:
  truths:
    - "Player can obtain 3 exotic suits (void-touched, anomaly, null) with anomaly-resistant stats"
    - "Player can obtain 3 exotic tools (phase extractor, void pick, reality anchor) with exotic toolTypes"
    - "High-tier exotic items require existing Tier I-II materials (horizontal progression)"
  artifacts:
    - path: "packages/items/src/definitions/exotic-suits.ts"
      provides: "3 exotic suit definitions (void-touched exotic, anomaly exotic, null legendary)"
      exports: ["SUIT_VOID_TOUCHED_EXOTIC", "SUIT_ANOMALY_EXOTIC", "SUIT_NULL_LEGENDARY", "ALL_EXOTIC_SUITS"]
    - path: "packages/items/src/definitions/exotic-tools.ts"
      provides: "3 exotic tool definitions (phase extractor, void pick, reality anchor)"
      exports: ["TOOL_PHASE_EXTRACTOR_EXOTIC", "TOOL_VOID_PICK_EXOTIC", "TOOL_REALITY_ANCHOR_EXOTIC", "ALL_EXOTIC_TOOLS"]
  key_links:
    - from: "exotic-suits.ts"
      to: "packages/items/src/utils.ts"
      via: "generateSuitStats() and computeIlvl() imports"
      pattern: "import.*generateSuitStats.*from.*utils"
    - from: "exotic-tools.ts"
      to: "packages/items/src/utils.ts"
      via: "getToolStats helper pattern"
      pattern: "STAT_RARITY_MULTIPLIERS|TIER_MULTIPLIERS"
---

<objective>
Create exotic equipment definitions (3 suits + 3 tools) for Phases 84-86 exotic biomes.

Purpose: Satisfy ITEM-04 (exotic suits) and ITEM-05 (exotic tools) requirements, enabling players to survive and operate in Bioluminescent Depths, Crystalline Wastes, and Void Rifts with appropriate gear. Implements horizontal progression (sidegrades, not upgrades).

Output: Two new definition files exporting exotic equipment with proper tier/rarity scaling and anomaly-themed abilities.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/087-item-integration-balance/87-RESEARCH.md
@packages/items/src/definitions/suits.ts
@packages/items/src/definitions/tools.ts
@packages/items/src/utils.ts
@packages/items/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exotic suit definitions</name>
  <files>packages/items/src/definitions/exotic-suits.ts</files>
  <action>
Create exotic-suits.ts with 3 suit definitions following the exact pattern from suits.ts:

1. **SUIT_VOID_TOUCHED_EXOTIC** (Tier III, exotic, requiredLevel: 25)
   - Archetype: 'scout' — mobility-focused void exploration
   - ilvl: computeIlvl(3, 'exotic')
   - moduleSlots: 5
   - grantedAbilities: ['nano_repair', 'magnetic_field', 'fortify_systems', 'overclock']
   - Description: Suit incorporating Anomaly Zone materials. Partial reality distortion resistance. "The suit sometimes moves before the wearer commands it. This is considered 'normal'."

2. **SUIT_ANOMALY_EXOTIC** (Tier IV, exotic, requiredLevel: 30)
   - Archetype: 'recon' — perception-focused anomaly resistance
   - ilvl: computeIlvl(4, 'exotic')
   - moduleSlots: 5
   - grantedAbilities: ['nano_repair', 'energy_barrier', 'regeneration_protocol', 'power_surge']
   - Description: Nexus Frontiers prototype with Ancient stabilization technology. "The research team cannot explain how it works. They stopped asking after the third incident."

3. **SUIT_NULL_LEGENDARY** (Tier IV, legendary, requiredLevel: 40)
   - Archetype: 'balanced' — ultimate void survivability
   - ilvl: computeIlvl(4, 'legendary')
   - moduleSlots: 6
   - grantedAbilities: ['nano_repair', 'regeneration_protocol', 'energy_barrier', 'overclock', 'void_drain']
   - Description: Impossibly advanced suit from a Null Pocket. "Analysis shows it is not powered by any detectable source. It simply... works."

Export ALL_EXOTIC_SUITS array.

Use imports: `import type { ItemDefinition } from '../types';` and `import { computeIlvl, generateSuitStats } from '../utils';`

Colors: void_touched=0x5500aa, anomaly=0x6600cc, null=0x220044 (purple gradient for void theme).

PROG-03 compliance: Different archetypes (scout/recon/balanced) create sidegrades, not pure upgrades. Void-Touched (scout) trades durability for mobility compared to existing tank-archetype exotics.
  </action>
  <verify>File exists with correct TypeScript syntax: `npx tsc --noEmit packages/items/src/definitions/exotic-suits.ts 2>&1 | head -20`</verify>
  <done>3 exotic suit definitions exist with different archetypes for horizontal progression.</done>
</task>

<task type="auto">
  <name>Task 2: Create exotic tool definitions</name>
  <files>packages/items/src/definitions/exotic-tools.ts</files>
  <action>
Create exotic-tools.ts with 3 tool definitions following the exact pattern from tools.ts:

First, add a local helper function (same pattern as getToolStats in tools.ts):
```typescript
function getExoticToolStats(toolType: ToolType, rarity: ItemRarity, tier: 1 | 2 | 3 | 4 | 5) {
  const base = 15;
  const rarityMult = STAT_RARITY_MULTIPLIERS[rarity];
  const tierMult = TIER_MULTIPLIERS[tier];
  const value = Math.round(base * rarityMult * tierMult);

  const gatheringStats: { yieldBonus?: number; gatherSpeed?: number } = {};
  if (toolType === 'mining' || toolType === 'bio') {
    switch (tier) {
      case 3: gatheringStats.yieldBonus = 0.2; gatheringStats.gatherSpeed = 0.2; break;
      case 4: gatheringStats.yieldBonus = 0.3; gatheringStats.gatherSpeed = 0.3; break;
    }
  }

  switch (toolType) {
    case 'anomaly': return { type: 'stats', resilience: value };
    case 'mining': return { type: 'stats', perception: value, ...gatheringStats };
    case 'research': return { type: 'stats', perception: value };
    default: return { type: 'stats', power: value };
  }
}
```

Then define:

1. **TOOL_PHASE_EXTRACTOR_EXOTIC** (Tier III, exotic, mining, requiredLevel: 25)
   - toolType: 'mining'
   - range: 4
   - grantedAbilities: ['mine', 'basic_strike', 'thermal_lance', 'plasma_burst', 'void_drain']
   - Description: Void-tech extraction device using spatial compression. "How it works is proprietary. That it works is undeniable."

2. **TOOL_VOID_PICK_EXOTIC** (Tier IV, exotic, mining, requiredLevel: 30)
   - toolType: 'mining'
   - range: 4
   - grantedAbilities: ['mine', 'basic_strike', 'plasma_burst', 'void_drain', 'dimensional_shift']
   - Description: Advanced mining tool incorporating dimensional instability. Extracts materials from Void Rift formations.

3. **TOOL_REALITY_ANCHOR_EXOTIC** (Tier III, exotic, anomaly, requiredLevel: 25)
   - toolType: 'anomaly'
   - range: 3
   - grantedAbilities: ['stabilize_anomaly', 'energy_barrier', 'analyze_specimen', 'resource_scan']
   - Description: Nexus device for navigating reality distortions. "Essential for prolonged Void Rift operations. Side effects are... being studied."

Export ALL_EXOTIC_TOOLS array.

Use imports: `import type { ItemDefinition, ItemRarity, ToolType } from '../types';` and `import { computeIlvl, STAT_RARITY_MULTIPLIERS, TIER_MULTIPLIERS } from '../utils';`

Colors: phase_extractor=0x7700ff, void_pick=0x5500ff, reality_anchor=0x8800ff (bright purple for exotic theme).
  </action>
  <verify>File exists with correct TypeScript syntax: `npx tsc --noEmit packages/items/src/definitions/exotic-tools.ts 2>&1 | head -20`</verify>
  <done>3 exotic tool definitions exist with anomaly toolType and void-themed abilities.</done>
</task>

</tasks>

<verification>
1. Both files compile without TypeScript errors
2. Each file exports ALL_EXOTIC_SUITS or ALL_EXOTIC_TOOLS array
3. Suit definitions use generateSuitStats() with different archetypes (scout, recon, balanced)
4. Tool definitions include 'anomaly' toolType for reality anchor
5. All exotic items have requiredLevel 25+ (requires Tier I-II progression first)
6. Stat distributions create sidegrades, not pure upgrades (PROG-03)
</verification>

<success_criteria>
- packages/items/src/definitions/exotic-suits.ts exists with 3 suits
- packages/items/src/definitions/exotic-tools.ts exists with 3 tools
- All 6 items follow existing patterns with horizontal progression
- TypeScript compilation succeeds
- ITEM-04 (exotic suits) and ITEM-05 (exotic tools) requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/087-item-integration-balance/87-02-SUMMARY.md`
</output>
