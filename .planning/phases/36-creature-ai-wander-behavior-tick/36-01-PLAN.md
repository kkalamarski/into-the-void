---
phase: 36-creature-ai-wander-behavior-tick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/ai.service.ts
  - apps/game-server/src/game/game.module.ts
autonomous: true

must_haves:
  truths:
    - "Zones with no active players have no AI tick running"
    - "A zone becomes active when its first player joins"
    - "A zone deactivates when its last player leaves"
  artifacts:
    - path: "apps/game-server/src/game/ai.service.ts"
      provides: "Zone-scoped AI tick loop with setTimeout self-rescheduling"
      exports: ["AiService"]
    - path: "apps/game-server/src/game/game.module.ts"
      provides: "AiService registration"
      contains: "AiService"
  key_links:
    - from: "apps/game-server/src/game/ai.service.ts"
      to: "apps/game-server/src/zones/zones.service.ts"
      via: "DI injection"
      pattern: "constructor.*ZonesService"
---

<objective>
Create AiService with self-rescheduling setTimeout pattern scoped to activePlayerZones

Purpose: Implement the zone-scoped AI tick loop infrastructure that activates only when players are present, using self-rescheduling setTimeout (not setInterval) to prevent event loop stalls
Output: AiService NestJS service with zone activation/deactivation lifecycle
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-creature-ai-wander-behavior-tick/36-RESEARCH.md

# Pattern references
@apps/game-server/src/zones/zones.service.ts — setServer() and tick loop pattern
@apps/game-server/src/game/game.module.ts — service registration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiService with zone-scoped setTimeout tick loop</name>
  <files>apps/game-server/src/game/ai.service.ts</files>
  <action>
Create `AiService` as a NestJS `@Injectable()` implementing `OnModuleInit`:

1. Private state:
   - `AI_TICK_INTERVAL_MS = 1000` (creatures move at half player speed)
   - `AI_TICK_WARN_MS = 200` (log warning if tick exceeds threshold)
   - `activeZones: Set<string>` — zones with at least one player
   - `tickTimers: Map<string, ReturnType<typeof setTimeout>>` — per-zone timers
   - `server: Server | null = null` — Socket.IO reference

2. Constructor injects:
   - `ZonesService` (for getZoneEntities, getChunk, updateEntity)
   - `PlayerService` (for getPlayersInZone)

3. `onModuleInit()` — starts with no active zones (zones activate when players join)

4. `setServer(server: Server)` — called by GameGateway.afterInit()

5. `activateZone(zoneId: string)`:
   - Guard: if already in activeZones, return immediately (prevents duplicate timers)
   - Add to activeZones
   - Call scheduleNextTick(zoneId)

6. `deactivateZone(zoneId: string)`:
   - Remove from activeZones
   - Get timer from tickTimers, clearTimeout if exists
   - Delete from tickTimers

7. `scheduleNextTick(zoneId: string)` (private):
   - Create setTimeout with AI_TICK_INTERVAL_MS delay
   - Inside callback:
     - Delete timer from tickTimers (timer has fired)
     - Guard: if zoneId not in activeZones, return (deactivated while waiting)
     - Capture `start = Date.now()`
     - `await this.runZoneTick(zoneId)`
     - Calculate elapsed, log warning if > AI_TICK_WARN_MS
     - Guard: if still in activeZones, call scheduleNextTick(zoneId) again

8. `runZoneTick(zoneId: string)` (private async):
   - Stub implementation for now (Plan 36-02 adds FSM logic)
   - `const entities = await this.zonesService.getZoneEntities(zoneId)`
   - Filter to creatures: `entities.filter(e => e.type === 'creature' && e.active && e.health > 0)`
   - If no creatures, return early
   - For now, just log: `console.log([AiService] Tick zone ${zoneId}: ${creatures.length} creatures)`

Import Position and Creature types from @into-the-void/shared-types. Import Server from socket.io.
  </action>
  <verify>
    `npx nx run game-server:build --skip-nx-cache` passes with no errors
  </verify>
  <done>
    AiService exists with activateZone, deactivateZone, setServer, and stub runZoneTick — per-zone self-rescheduling setTimeout pattern implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Register AiService in GameModule</name>
  <files>apps/game-server/src/game/game.module.ts</files>
  <action>
1. Import AiService from './ai.service'
2. Add AiService to the `providers` array (after EntityService)
3. Add AiService to the `exports` array (after EntityService)

The service ordering in providers should be: GameGateway, GameService, PlayerService, InventoryService, StorageService, EntityService, AiService
  </action>
  <verify>
    `npx nx run game-server:build --skip-nx-cache` passes with no circular dependency errors
  </verify>
  <done>
    AiService is registered in GameModule and can be injected into other services
  </done>
</task>

</tasks>

<verification>
1. `npx nx run game-server:build --skip-nx-cache` — compiles without errors
2. Grep check: `grep -r "activeZones" apps/game-server/src/game/ai.service.ts` returns matches
3. Grep check: `grep -r "AiService" apps/game-server/src/game/game.module.ts` returns matches
</verification>

<success_criteria>
- AiService is a valid NestJS injectable with zone activation/deactivation methods
- Self-rescheduling setTimeout pattern is implemented (not setInterval)
- AI tick warning threshold logging is in place
- AiService is registered in GameModule providers and exports
</success_criteria>

<output>
After completion, create `.planning/phases/36-creature-ai-wander-behavior-tick/36-01-SUMMARY.md`
</output>
