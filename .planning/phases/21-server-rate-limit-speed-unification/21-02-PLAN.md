---
phase: 21-server-rate-limit-speed-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/constants.ts
  - apps/web/src/game/scenes/WorldScene.ts
  - apps/web/src/game/systems/PathfindingController.ts
autonomous: true

must_haves:
  truths:
    - "WASD movement and click-to-move pathfinding advance at identical 150ms speed"
    - "A single MOVE_DELAY_MS constant controls timing for both input paths"
    - "No hardcoded 500ms or 150ms values remain in WorldScene or PathfindingController"
  artifacts:
    - path: "packages/shared-types/src/constants.ts"
      provides: "Shared MOVE_DELAY_MS constant"
      exports: ["MOVE_DELAY_MS"]
    - path: "apps/web/src/game/scenes/WorldScene.ts"
      provides: "Keyboard movement using shared constant"
      contains: "MOVE_DELAY_MS"
    - path: "apps/web/src/game/systems/PathfindingController.ts"
      provides: "Pathfinding movement using shared constant"
      contains: "MOVE_DELAY_MS"
  key_links:
    - from: "apps/web/src/game/scenes/WorldScene.ts"
      to: "packages/shared-types/src/constants.ts"
      via: "import MOVE_DELAY_MS"
      pattern: "import.*MOVE_DELAY_MS.*shared-types"
    - from: "apps/web/src/game/systems/PathfindingController.ts"
      to: "packages/shared-types/src/constants.ts"
      via: "import MOVE_DELAY_MS"
      pattern: "import.*MOVE_DELAY_MS.*shared-types"
---

<objective>
Extract MOVE_DELAY_MS = 150 constant and use it in both WorldScene and PathfindingController

Purpose: Unify keyboard and click-to-move speed under a single shared constant, eliminating the current mismatch (500ms keyboard vs 150ms pathfinding).

Output: Shared constant in shared-types, imported by both movement systems
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/game/scenes/WorldScene.ts
@apps/web/src/game/systems/PathfindingController.ts
@packages/shared-types/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MOVE_DELAY_MS constant in shared-types</name>
  <files>packages/shared-types/src/constants.ts, packages/shared-types/src/index.ts</files>
  <action>
1. Create new file `packages/shared-types/src/constants.ts`:
```typescript
/**
 * Movement timing constants shared between client and server.
 */

/**
 * Delay between player movement inputs in milliseconds.
 * Both keyboard (WASD) and click-to-move pathfinding use this value.
 * Server rate limit should be slightly lower (125ms) to allow network jitter.
 */
export const MOVE_DELAY_MS = 150;
```

2. Add export to `packages/shared-types/src/index.ts`:
```typescript
export * from './constants';
```

This places the constant at the package boundary for easy import by all apps.
  </action>
  <verify>
Run `pnpm exec nx run shared-types:build` to confirm the package compiles.
Check `grep -r "MOVE_DELAY_MS" packages/shared-types/` shows the constant and export.
  </verify>
  <done>
MOVE_DELAY_MS = 150 constant exists in shared-types and is exported from package index.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update WorldScene to use MOVE_DELAY_MS</name>
  <files>apps/web/src/game/scenes/WorldScene.ts</files>
  <action>
1. Add import at top of file:
```typescript
import { MOVE_DELAY_MS } from '@into-the-void/shared-types';
```

2. Replace the hardcoded moveDelay property (line 34):
   - Remove: `private moveDelay = 500;`
   - Add: `private moveDelay = MOVE_DELAY_MS;`

3. Update PathfindingController instantiation (around line 103-108) to also use the constant:
   - Currently: `this.pathfindingController = new PathfindingController(this.movementController, this.moveDelay, ...)`
   - Keep as-is since `this.moveDelay` now equals MOVE_DELAY_MS

This ensures keyboard WASD movement uses 150ms delay instead of 500ms.
  </action>
  <verify>
Run `grep -n "moveDelay" apps/web/src/game/scenes/WorldScene.ts` to confirm:
- No hardcoded 500 value
- Property initialized from MOVE_DELAY_MS
  </verify>
  <done>
WorldScene.moveDelay is initialized from MOVE_DELAY_MS constant. No hardcoded 500ms remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PathfindingController to use MOVE_DELAY_MS default</name>
  <files>apps/web/src/game/systems/PathfindingController.ts</files>
  <action>
1. Add import at top of file:
```typescript
import { MOVE_DELAY_MS } from '@into-the-void/shared-types';
```

2. Update constructor default parameter (line 20):
   - Change: `moveDelay = 150`
   - To: `moveDelay = MOVE_DELAY_MS`

This ensures the default matches the shared constant even if WorldScene changes.
  </action>
  <verify>
Run `grep -n "MOVE_DELAY_MS" apps/web/src/game/systems/PathfindingController.ts` to confirm import and usage.
Run `pnpm exec nx run web:build` to verify compilation.
  </verify>
  <done>
PathfindingController constructor default uses MOVE_DELAY_MS constant. No hardcoded 150 in default parameter.
  </done>
</task>

</tasks>

<verification>
1. Grep confirms MOVE_DELAY_MS imported and used in WorldScene and PathfindingController
2. No hardcoded 500 or 150 values remain in movement timing logic
3. Build passes: `pnpm build`
4. Both paths reference the same constant (single source of truth)
</verification>

<success_criteria>
- [ ] MOVE_DELAY_MS = 150 constant in shared-types
- [ ] WorldScene imports and uses MOVE_DELAY_MS for moveDelay property
- [ ] PathfindingController imports and uses MOVE_DELAY_MS as default parameter
- [ ] No hardcoded timing values (500, 150) in movement code
- [ ] Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/21-server-rate-limit-speed-unification/21-02-SUMMARY.md`
</output>
