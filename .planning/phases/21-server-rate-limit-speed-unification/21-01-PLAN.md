---
phase: 21-server-rate-limit-speed-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/game.gateway.ts
autonomous: true

must_haves:
  truths:
    - "Server accepts moves at 150ms cadence without rate limit errors during normal gameplay"
    - "Server rate limit is 125ms (150ms client - 25ms tolerance) instead of 140ms"
  artifacts:
    - path: "apps/game-server/src/game/game.gateway.ts"
      provides: "Rate limit enforcement for player movement"
      contains: "125"
  key_links:
    - from: "apps/game-server/src/game/game.gateway.ts"
      to: "handleMove"
      via: "Rate limit check before movePlayer"
      pattern: "< 125"
---

<objective>
Reduce server movement rate limit from 140ms to 125ms

Purpose: Allow clients to move at 150ms cadence with 25ms network tolerance (instead of 10ms) to prevent false rate limit rejections during normal gameplay.

Output: Updated GameGateway with 125ms rate limit
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/game-server/src/game/game.gateway.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update server rate limit constant</name>
  <files>apps/game-server/src/game/game.gateway.ts</files>
  <action>
In the `handleMove` method of GameGateway, change the rate limit from 140ms to 125ms:

1. Find line 132-133: `if (now - lastMoveTime < 140)`
2. Change `140` to `125`
3. Update the comment to reflect the new tolerance: "Rate limit: minimum 125ms between moves (150ms client delay - 25ms tolerance)"

This allows 25ms of network jitter tolerance for clients sending moves at 150ms intervals, reducing false rate limit rejections.
  </action>
  <verify>
Run `grep -n "125" apps/game-server/src/game/game.gateway.ts` to confirm the new rate limit value is present.
Verify the old value 140 is no longer in the rate limit check.
  </verify>
  <done>
Server rate limit is 125ms instead of 140ms. Comment accurately describes 150ms client delay - 25ms tolerance.
  </done>
</task>

</tasks>

<verification>
1. Grep confirms `< 125` appears in GameGateway handleMove method
2. No occurrences of `< 140` remain in rate limit logic
3. TypeScript compiles: `pnpm exec nx run game-server:build`
</verification>

<success_criteria>
- [ ] Rate limit value changed from 140 to 125 in game.gateway.ts
- [ ] Comment updated to reflect 25ms tolerance
- [ ] Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-server-rate-limit-speed-unification/21-01-SUMMARY.md`
</output>
