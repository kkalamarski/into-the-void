---
phase: 82-aquatic-biome-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/game/biome.ts
  - packages/tiles/src/definitions/aquatic-tiles.ts
  - packages/tiles/src/definitions/index.ts
  - packages/tiles/src/types.ts
autonomous: true

must_haves:
  truths:
    - "Three aquatic biome types exist: tidal_pools, kelp_forests, deep_trenches"
    - "Aquatic tiles have water-specific properties (shallow/deep water states)"
    - "Tile definitions include speed modifiers appropriate for aquatic movement"
  artifacts:
    - path: "packages/shared-types/src/game/biome.ts"
      provides: "BiomeType union includes tidal_pools | kelp_forests | deep_trenches"
      contains: "tidal_pools"
    - path: "packages/tiles/src/definitions/aquatic-tiles.ts"
      provides: "Aquatic tile definitions with water movement properties"
      exports: ["TIDAL_FLOOR", "TIDAL_SHALLOW", "KELP_FLOOR", "KELP_WALL", "TRENCH_FLOOR", "TRENCH_DEEP", "SHORE_TRANSITION"]
    - path: "packages/tiles/src/types.ts"
      provides: "TileState enum for water tile classification"
      contains: "TileState"
  key_links:
    - from: "packages/tiles/src/definitions/aquatic-tiles.ts"
      to: "packages/tiles/src/definitions/index.ts"
      via: "export re-export"
      pattern: "aquatic-tiles"
    - from: "packages/tiles/src/definitions/index.ts"
      to: "packages/tiles/src/registry.ts"
      via: "ALL_TILES array"
      pattern: "TIDAL_FLOOR"
---

<objective>
Create type foundation for aquatic biomes with tile definitions supporting water-specific movement and visibility.

Purpose: Establish the data model for aquatic biomes before generation and gameplay integration. This is the foundational layer that all subsequent aquatic work depends on.

Output: Extended BiomeType with 3 aquatic biomes, TileState enum for water classification, and complete aquatic tile definitions.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/shared-types/src/game/biome.ts
@packages/tiles/src/types.ts
@packages/tiles/src/definitions/index.ts
@packages/tiles/src/definitions/void-tiles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BiomeType and Add Display Constants</name>
  <files>packages/shared-types/src/game/biome.ts</files>
  <action>
Add three aquatic biome types to the BiomeType union:
- `tidal_pools` - Tier I shallow coastal biome
- `kelp_forests` - Tier II dense underwater vegetation
- `deep_trenches` - Tier III high-pressure deep ocean

Add to BIOME_DISPLAY_NAMES:
- tidal_pools: 'Tidal Pools'
- kelp_forests: 'Kelp Forests'
- deep_trenches: 'Deep Trenches'

Add to BIOME_COLORS:
- tidal_pools: '#5f9ea0' (cadet blue - shallow coastal)
- kelp_forests: '#228b22' (forest green - dense vegetation)
- deep_trenches: '#191970' (midnight blue - deep ocean)

Add new BiomeHazard type 'pressure' for deep_trenches.
  </action>
  <verify>
Run `npx nx run shared-types:build` - build succeeds with no type errors.
Grep for 'tidal_pools' in output to confirm type is exported.
  </verify>
  <done>BiomeType includes all 3 aquatic biomes with display names and colors.</done>
</task>

<task type="auto">
  <name>Task 2: Add TileState Enum to Tile Types</name>
  <files>packages/tiles/src/types.ts</files>
  <action>
Add a TileState enum to classify tile traversability beyond simple blocking:

```typescript
/**
 * Tile traversability state - extends beyond simple blocking
 * Used for water tiles that are traversable but with special rules
 */
export type TileState = 'solid' | 'traversable' | 'shallow_water' | 'deep_water';
```

Add optional `tileState` property to TileDefinition interface:
```typescript
/** Tile state for special traversal rules (water, etc). Defaults to 'traversable' if not blocking, 'solid' if blocking */
readonly tileState?: TileState;
```

Add optional `visibilityModifier` property to TileDefinition:
```typescript
/** Visibility radius modifier (1.0 = normal, 0.7 = reduced). Affects fog of war reveal. */
readonly visibilityModifier?: number;
```

This is additive - existing tiles don't need updates (they default to solid/traversable based on isBlocking).
  </action>
  <verify>
Run `npx nx run tiles:build` - build succeeds.
Check that TileState is exported in packages/tiles/src/index.ts (add if missing).
  </verify>
  <done>TileDefinition supports tileState and visibilityModifier properties.</done>
</task>

<task type="auto">
  <name>Task 3: Create Aquatic Tile Definitions</name>
  <files>packages/tiles/src/definitions/aquatic-tiles.ts, packages/tiles/src/definitions/index.ts</files>
  <action>
Create new file `packages/tiles/src/definitions/aquatic-tiles.ts` with tile definitions:

**Tidal Pools (Tier I):**
- `TIDAL_FLOOR`: Sandy/rocky coastal floor, traversable, speed 1.0, visibility 0.85
- `TIDAL_SHALLOW`: Shallow water, traversable, speed 0.7 (reduced), tileState: 'shallow_water', visibility 0.85

**Kelp Forests (Tier II):**
- `KELP_FLOOR`: Seafloor corridor, traversable, speed 0.6, tileState: 'shallow_water', visibility 0.7
- `KELP_WALL`: Dense kelp formation, BLOCKING, used for corridor walls

**Deep Trenches (Tier III):**
- `TRENCH_FLOOR`: Deep ocean floor, traversable, speed 0.3, tileState: 'deep_water', visibility 0.6
- `TRENCH_DEEP`: Abyssal depth, traversable but very slow (speed 0.2), tileState: 'deep_water', visibility 0.5

**Shore Transitions:**
- `SHORE_TRANSITION`: Beach/shore tile, traversable, speed 0.9, used at water/land boundaries

Colors (hex):
- TIDAL_FLOOR: 0xc2b280 (sandy tan)
- TIDAL_SHALLOW: 0x87ceeb (sky blue - shallow water)
- KELP_FLOOR: 0x2e8b57 (sea green)
- KELP_WALL: 0x006400 (dark green)
- TRENCH_FLOOR: 0x000080 (navy)
- TRENCH_DEEP: 0x00001a (near black blue)
- SHORE_TRANSITION: 0xf5deb3 (wheat/sand)

Update `packages/tiles/src/definitions/index.ts`:
1. Import all exports from aquatic-tiles.ts
2. Add all aquatic tiles to ALL_TILES array
3. Add TILE_IDS constants for each aquatic tile
4. Re-export the tile definitions
  </action>
  <verify>
Run `npx nx run tiles:build` - build succeeds.
Run `npx nx run tiles:test` (if tests exist) - all pass.
Verify TileRegistry.get('tidal_shallow').tileState === 'shallow_water' in a quick test or grep.
  </verify>
  <done>Seven aquatic tile definitions exist and are registered in ALL_TILES.</done>
</task>

</tasks>

<verification>
1. `npx nx run shared-types:build` - BiomeType exports without errors
2. `npx nx run tiles:build` - Tile package builds without errors
3. `grep -r "tidal_pools" packages/shared-types/` - Returns biome type definition
4. `grep -r "TIDAL_FLOOR" packages/tiles/` - Returns tile definition and index export
5. TypeScript: `TileRegistry.get('kelp_wall').isBlocking === true`
6. TypeScript: `TileRegistry.get('trench_floor').movementSpeed === 0.3`
</verification>

<success_criteria>
- BiomeType union includes 'tidal_pools' | 'kelp_forests' | 'deep_trenches'
- TileState type exists with 4 states: solid, traversable, shallow_water, deep_water
- 7 aquatic tiles defined with appropriate speed/visibility modifiers
- All tiles registered in TileRegistry via ALL_TILES
- Both packages build without errors
</success_criteria>

<output>
After completion, create `.planning/phases/082-aquatic-biome-foundation/082-01-SUMMARY.md`
</output>
