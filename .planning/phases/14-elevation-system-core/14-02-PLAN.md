---
phase: 14-elevation-system-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/utils/IsometricTransform.ts
  - apps/web/src/game/rendering/DepthSorter.ts
  - apps/web/src/game/rendering/EntityRenderer.ts
autonomous: true

must_haves:
  truths:
    - "Depth calculation includes elevation as weighted component"
    - "Entities on elevated terrain render at correct depth relative to lower terrain"
    - "screenY remains dominant factor in depth (elevation weight is conservative)"
  artifacts:
    - path: "apps/web/src/game/utils/IsometricTransform.ts"
      provides: "calculateDepth with elevation parameter"
      contains: "elevation"
    - path: "apps/web/src/game/rendering/DepthSorter.ts"
      provides: "Depth updates passing elevation to calculateDepth"
      contains: "elevation"
    - path: "apps/web/src/game/rendering/EntityRenderer.ts"
      provides: "Entity container stores elevation data"
      contains: "elevation"
  key_links:
    - from: "DepthSorter.update"
      to: "IsometricTransform.calculateDepth"
      via: "elevation parameter from container data"
      pattern: "calculateDepth.*elevation"
    - from: "EntityRenderer"
      to: "container.setData"
      via: "stores elevation for depth sorting"
      pattern: "setData.*elevation"
---

<objective>
Update client-side depth sorting to incorporate elevation in composite depth calculation.

Purpose: Entities and tiles on elevated terrain should render in front of entities on lower terrain at the same screen position. This creates visual depth without breaking the dominant screenY-based sorting.

Output: IsometricTransform.calculateDepth accepts optional elevation parameter, DepthSorter passes elevation from container data, and EntityRenderer stores elevation on containers.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-elevation-system-core/14-RESEARCH.md
@apps/web/src/game/utils/IsometricTransform.ts
@apps/web/src/game/rendering/DepthSorter.ts
@apps/web/src/game/rendering/EntityRenderer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IsometricTransform.calculateDepth with elevation parameter</name>
  <files>apps/web/src/game/utils/IsometricTransform.ts</files>
  <action>
Modify calculateDepth method to accept optional elevation parameter:

1. Add private elevationWeight constant (start conservative at 0.1 - tunable later):
   private elevationWeight = 0.1;

2. Update calculateDepth signature to include elevation:
   calculateDepth(gridX: number, gridY: number, elevation: number = 0, priorityBoost: number = 0): number

3. Update depth calculation formula to include elevation component:
   return screen.y + (gridX * 0.0001) + (elevation * this.elevationWeight) + priorityBoost;

4. Add optional setter method for runtime tuning:
   setElevationWeight(weight: number): void {
     this.elevationWeight = weight;
   }

The elevationWeight starts at 0.1 (conservative) so screenY remains dominant. Research suggests values 0.05-0.2 are safe; higher values risk breaking visual depth.
  </action>
  <verify>TypeScript compiles: pnpm build --filter=web</verify>
  <done>calculateDepth accepts elevation parameter with default 0, returns composite depth including elevation component</done>
</task>

<task type="auto">
  <name>Task 2: Update DepthSorter and EntityRenderer to use elevation</name>
  <files>
apps/web/src/game/rendering/DepthSorter.ts
apps/web/src/game/rendering/EntityRenderer.ts
  </files>
  <action>
**DepthSorter.ts:**

1. In update() method, after getting gridX and gridY from container data, also get elevation:
   const elevation = (container.getData('elevation') as number) ?? 0;

2. Update the calculateDepth call to pass elevation:
   const depth = isoTransform.calculateDepth(gridX, gridY, elevation, priorityBoost);

3. In updateImmediate() method, do the same:
   const elevation = (container.getData('elevation') as number) ?? 0;
   const depth = isoTransform.calculateDepth(gridX, gridY, elevation, priorityBoost);

**EntityRenderer.ts:**

1. In createEntityContainer, store elevation on container (default 0 for now, WorldScene will update):
   container.setData('elevation', 0);

2. In updateEntityPosition, add optional elevation parameter with default 0:
   updateEntityPosition(container: Container, gridX: number, gridY: number, elevation: number = 0): void

3. Update the setData calls to include elevation:
   container.setData('elevation', elevation);

4. Update the calculateDepth call to pass elevation:
   const depth = this.isoTransform.calculateDepth(gridX, gridY, elevation);

NOTE: Elevation values come from ChunkData.heights[][] which is already flowing from server. WorldScene will need to look up heights[y][x] when updating entity positions. This wiring happens in Phase 15 renderer integration - for now, entities default to elevation 0 but the infrastructure is ready.
  </action>
  <verify>
Full web build: pnpm build --filter=web
Run type check: cd apps/web && pnpm tsc --noEmit
  </verify>
  <done>
DepthSorter reads elevation from container data and passes to calculateDepth
EntityRenderer stores elevation on containers and accepts elevation in updateEntityPosition
Backward compatible (elevation defaults to 0 when not set)
  </done>
</task>

</tasks>

<verification>
1. Web build passes: pnpm build --filter=web succeeds
2. Type check passes: pnpm tsc --noEmit in apps/web
3. calculateDepth signature includes elevation parameter
4. DepthSorter reads and uses elevation from container data
5. EntityRenderer stores and updates elevation on containers
</verification>

<success_criteria>
- IsometricTransform.calculateDepth accepts optional elevation parameter (default 0)
- Composite depth formula: screenY + (gridX * 0.0001) + (elevation * 0.1) + priorityBoost
- DepthSorter.update() reads elevation from container.getData('elevation')
- DepthSorter.updateImmediate() reads elevation from container.getData('elevation')
- EntityRenderer.createEntityContainer sets elevation data (default 0)
- EntityRenderer.updateEntityPosition accepts elevation parameter (default 0)
- All changes backward compatible (default elevation 0 = no behavioral change)
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-elevation-system-core/14-02-SUMMARY.md`
</output>
