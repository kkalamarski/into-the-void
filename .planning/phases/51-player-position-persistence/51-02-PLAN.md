---
phase: 51-player-position-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/player.service.ts
autonomous: true

must_haves:
  truths:
    - "Player who logged out in a hub zone spawns in that hub zone on next login"
    - "Player who logged out in open world spawns at that exact tile on next login"
    - "Position restore works correctly after server restart"
  artifacts:
    - path: "apps/game-server/src/game/player.service.ts"
      provides: "Hub zone position restore handling"
      contains: "isHubZone"
  key_links:
    - from: "apps/game-server/src/game/player.service.ts"
      to: "authenticate"
      via: "Position from character.position is used directly"
      pattern: "position.*character.position"
---

<objective>
Ensure player position restores correctly on login, including edge cases for hub zones and server restarts.

Purpose: Verify and document that position restore works correctly. The character.position is already loaded from DB in authenticate(), but we need to ensure hub zone positions are handled correctly (no accidental teleport to open world on login).

Output: Verified position restore logic with any necessary fixes for hub zone edge cases.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/game-server/src/game/player.service.ts
@packages/database/src/schema/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify hub zone position restore on login</name>
  <files>apps/game-server/src/game/player.service.ts</files>
  <action>
Review and verify the authenticate() method in PlayerService:

1. **Current behavior check**: The authenticate() method already:
   - Loads character from DB via `findCharacterById()`
   - Uses `character.position` directly to create the ConnectedPlayer
   - This means if a player logged out in a hub zone (e.g., `hub_verdant_dynamics`), they will spawn there

2. **No code changes needed IF**: The character.position field in the DB already persists correctly (which Plan 51-01 ensures on disconnect).

3. **Verification steps** (manual review):
   - Confirm `player.position = character.position` in authenticate() - this uses DB value directly
   - Confirm hub zone IDs are valid position.zoneId values (they are, format: `hub_{faction_id}`)
   - Confirm no code overwrites position after loading from character

4. **Add clarifying comment** in authenticate() after creating the player object to document this behavior:
   ```typescript
   // Player position is restored from database - supports both open-world and hub zones
   // Position persistence is handled by handleDisconnect saving to DB
   ```

This task is primarily verification and documentation. The restore logic already works because:
- authenticate() loads character from DB with character.position
- character.position is set to whatever was saved on last disconnect (Plan 51-01)
- No code modifies position between load and player creation
  </action>
  <verify>
1. Read authenticate() method and confirm character.position is used directly
2. Grep for any position overwriting: `grep -n "player.position.*=" apps/game-server/src/game/player.service.ts`
3. Confirm comment was added: `grep -n "restored from database" apps/game-server/src/game/player.service.ts`
  </verify>
  <done>
authenticate() uses character.position from database directly. Comment documents the position restore flow. No additional code changes needed for restore functionality.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document position persistence flow for future maintainability</name>
  <files>apps/game-server/src/game/player.service.ts</files>
  <action>
Add JSDoc comments to clarify the position persistence contract:

1. On `handleDisconnect()` method, update/add JSDoc:
   ```typescript
   /**
    * Handle player disconnect - saves position and inventory to database.
    * Position is persisted so player spawns at same location on next login.
    * Works for both graceful disconnects and abrupt drops (browser close, network loss).
    */
   ```

2. On `updatePosition()` method, add JSDoc clarifying it's in-memory only:
   ```typescript
   /**
    * Update player position in memory (called during movement).
    * Note: This is in-memory only. Persistence to DB happens on disconnect.
    */
   ```

These comments ensure future developers understand the persistence flow:
- Movement updates in-memory position via updatePosition()
- On disconnect, handleDisconnect() persists to DB
- On next login, authenticate() loads from DB
  </action>
  <verify>
1. Grep for JSDoc on handleDisconnect: `grep -B 5 "handleDisconnect" apps/game-server/src/game/player.service.ts | grep -E "(@|\\*.*position|\\*.*persist)"`
2. Grep for JSDoc on updatePosition: `grep -B 5 "updatePosition" apps/game-server/src/game/player.service.ts | grep -E "(@|\\*.*memory|\\*.*disconnect)"`
  </verify>
  <done>
JSDoc comments added to handleDisconnect() and updatePosition() documenting the position persistence flow.
  </done>
</task>

</tasks>

<verification>
1. Position restore from DB works for open-world zones (character.position)
2. Position restore from DB works for hub zones (hub_* zoneId values)
3. JSDoc comments document the persistence flow
4. Build passes
</verification>

<success_criteria>
- Player logging out in hub zone restores to hub zone on next login
- Player logging out in open world restores to exact tile on next login
- Code is documented for maintainability
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/51-player-position-persistence/51-02-SUMMARY.md`
</output>
