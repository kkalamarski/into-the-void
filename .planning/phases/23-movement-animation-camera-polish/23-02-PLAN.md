---
phase: 23-movement-animation-camera-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/scenes/WorldScene.ts
autonomous: true

must_haves:
  truths:
    - "Main camera glides after player with smooth interpolation instead of instant snap"
    - "Minimap camera remains instant-follow (no lerp)"
  artifacts:
    - path: "apps/web/src/game/scenes/WorldScene.ts"
      provides: "Camera lerp configuration"
      contains: "startFollow(this.localPlayer!, true, 0.1, 0.1)"
  key_links:
    - from: "updateLocalPlayer"
      to: "cameras.main.startFollow"
      via: "lerp values 0.1, 0.1"
      pattern: "startFollow\\(this\\.localPlayer!, true, 0\\.1, 0\\.1\\)"
---

<objective>
Change main camera to smooth follow with lerp interpolation.

Purpose: Camera should glide after the player instead of snapping instantly, creating a more polished feel.
Output: Modified WorldScene.ts with camera lerp values changed from (1, 1) to (0.1, 0.1).
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-movement-animation-camera-polish/23-RESEARCH.md
@apps/web/src/game/scenes/WorldScene.ts
@apps/web/src/game/rendering/MinimapCamera.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change main camera lerp from instant to smooth</name>
  <files>apps/web/src/game/scenes/WorldScene.ts</files>
  <action>
In `updateLocalPlayer()` method (around line 1053-1067), find the camera follow setup:

```typescript
this.cameras.main.startFollow(this.localPlayer!, true, 1, 1);
```

Change to:
```typescript
this.cameras.main.startFollow(this.localPlayer!, true, 0.1, 0.1);
```

Phaser's startFollow signature: `startFollow(target, roundPixels, lerpX, lerpY)`
- lerpX/lerpY = 1 means instant snap (current behavior)
- lerpX/lerpY = 0.1 means smooth follow with 10% interpolation per frame

DO NOT modify the minimap camera setup (`this.minimapCamera.startFollow(this.localPlayer!)`). The MinimapCamera class handles its own camera and must remain instant-follow. Verify MinimapCamera.ts does NOT use lerp args in its startFollow call.
  </action>
  <verify>
Run `pnpm build` - should compile without errors.
Visually verify: Camera should smoothly follow player with slight lag instead of snapping.
Verify minimap still updates instantly (no lag).
  </verify>
  <done>
Main camera uses lerp (0.1, 0.1) for smooth follow. Minimap camera unchanged (instant follow).
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without TypeScript errors
2. WorldScene.ts contains `startFollow(this.localPlayer!, true, 0.1, 0.1)` for main camera
3. MinimapCamera.ts does NOT have 0.1 lerp values (remains instant)
</verification>

<success_criteria>
- Main camera glides after player with smooth interpolation
- Minimap camera remains instant-follow
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-movement-animation-camera-polish/23-02-SUMMARY.md`
</output>
