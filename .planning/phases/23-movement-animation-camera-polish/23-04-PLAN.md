---
phase: 23-movement-animation-camera-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/systems/HoverController.ts
autonomous: true

must_haves:
  truths:
    - "Tile hover highlight is absent from the screen (no white diamond on hovered tile)"
    - "No import errors or missing module references related to HoverController"
  artifacts:
    - path: "apps/web/src/game/systems/HoverController.ts"
      provides: "File must NOT exist (deleted)"
      exists: false
  key_links: []
---

<objective>
Remove HoverController and tile hover highlight rendering.

Purpose: HoverController was disabled in Phase 22 due to issues with elevation. The file is dead code and should be deleted.
Output: HoverController.ts deleted. No hover highlight appears.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-movement-animation-camera-polish/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete HoverController.ts file</name>
  <files>apps/web/src/game/systems/HoverController.ts</files>
  <action>
First verify HoverController is not imported anywhere in the codebase:

```bash
grep -r "HoverController" apps/web/src/ --include="*.ts" --include="*.tsx"
```

Expected: Only the file itself should match (`apps/web/src/game/systems/HoverController.ts`).

If no other files import HoverController, delete the file:

```bash
rm apps/web/src/game/systems/HoverController.ts
```

The file is confirmed dead code per research - it was disabled in Phase 22 (commit b6d7df1: "remove HoverController (broken with elevation)") but the file itself was left behind.
  </action>
  <verify>
Run `pnpm build` - should compile without errors (no missing imports).
Run `ls apps/web/src/game/systems/HoverController.ts` - should return "No such file".
  </verify>
  <done>
HoverController.ts is deleted. Build passes. No tile hover highlight appears in the game.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without TypeScript errors
2. `apps/web/src/game/systems/HoverController.ts` does not exist
3. No "Cannot find module" errors related to HoverController
</verification>

<success_criteria>
- HoverController.ts file is deleted
- Build passes without import errors
- No tile hover highlight renders in the game
</success_criteria>

<output>
After completion, create `.planning/phases/23-movement-animation-camera-polish/23-04-SUMMARY.md`
</output>
