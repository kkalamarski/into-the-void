---
phase: 23-movement-animation-camera-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/scenes/WorldScene.ts
autonomous: true

must_haves:
  truths:
    - "Player sprite visibly slides between tiles during prediction moves (not teleporting)"
    - "Server reconciliation causes a short smooth correction (not jarring snap)"
  artifacts:
    - path: "apps/web/src/game/scenes/WorldScene.ts"
      provides: "Prediction tween (130ms Linear) and reconciliation tween (80ms Cubic.easeOut)"
      contains: "duration: 130"
  key_links:
    - from: "updateLocalPlayerSprite"
      to: "this.tweens.add"
      via: "prediction branch adds 130ms Linear tween"
      pattern: "duration:\\s*130"
---

<objective>
Add tile-to-tile tween animation on prediction moves and increase reconciliation tween duration.

Purpose: Player sprite should visually glide between tiles instead of teleporting, creating fluid movement feel.
Output: Modified WorldScene.ts with tweens in both prediction and reconciliation branches.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-movement-animation-camera-polish/23-RESEARCH.md
@apps/web/src/game/scenes/WorldScene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prediction tween and increase reconciliation tween duration</name>
  <files>apps/web/src/game/scenes/WorldScene.ts</files>
  <action>
In `updateLocalPlayerSprite()` method (around line 1019-1051), modify the tween logic:

1. In the `if (reconciling)` branch:
   - Change `duration: 50` to `duration: 80` (increase reconciliation smoothness)
   - Keep `ease: 'Cubic.easeOut'` unchanged

2. In the `else` branch (prediction path):
   - Replace the direct position assignment:
     ```typescript
     this.localPlayer.x = screenPos.x;
     this.localPlayer.y = targetY;
     ```
   - With a tween call:
     ```typescript
     this.tweens.killTweensOf(this.localPlayer);
     this.tweens.add({
       targets: this.localPlayer,
       x: screenPos.x,
       y: targetY,
       duration: 130,  // MOVE_DELAY_MS (150) - 20ms to prevent drift
       ease: 'Linear',
     });
     ```

The grid data updates (`setData('gridX', ...)`, `setData('gridY', ...)`, `setData('elevation', ...)`, and `setDepth()`) must remain AFTER the tween block - they are based on grid position not pixel position, so they update immediately.

Critical: The reconciliation branch already has `killTweensOf` - the prediction branch also needs it to prevent tween queuing.
  </action>
  <verify>
Run `pnpm build` - should compile without errors.
Visually verify: Player sprite should smoothly slide between tiles when pressing WASD, not teleport.
  </verify>
  <done>
Both prediction and reconciliation paths use tweens. Prediction uses 130ms Linear, reconciliation uses 80ms Cubic.easeOut. No direct position assignment in the else branch.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without TypeScript errors
2. WorldScene.ts contains `duration: 130` in prediction branch
3. WorldScene.ts contains `duration: 80` in reconciliation branch
4. Both branches have `killTweensOf` before `tweens.add`
</verification>

<success_criteria>
- Player sprite visibly slides between tiles during WASD movement
- Server reconciliation is smooth (80ms) not jarring
- No tween queuing issues (killTweensOf prevents it)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-movement-animation-camera-polish/23-01-SUMMARY.md`
</output>
