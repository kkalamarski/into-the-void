---
phase: 140-biome-tile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/game/biome.ts
  - packages/world-gen/src/generation/biome.ts
  - packages/world-gen/src/generation/terrain.ts
autonomous: true
requirements:
  - BIOME-01
  - BIOME-02
  - BIOME-03
  - BIOME-04

must_haves:
  truths:
    - "Four new hub biome type literals exist in the BiomeType union: canopy_station, ironhold_station, meridian_station, salvage_station"
    - "All BiomeType-keyed Record objects compile without missing key errors"
    - "Each hub biome has a distinct display name and color matching its faction palette"
  artifacts:
    - path: "packages/shared-types/src/game/biome.ts"
      provides: "BiomeType union with 4 new hub station literals, BIOME_DISPLAY_NAMES, BIOME_COLORS, BIOME_TIERS entries"
      contains: "canopy_station"
    - path: "packages/world-gen/src/generation/biome.ts"
      provides: "getBiomeDangerLevel and getBiomeColor entries for all 4 hub biome types"
      contains: "canopy_station"
    - path: "packages/world-gen/src/generation/terrain.ts"
      provides: "Placeholder entries in BIOME_TILES, BIOME_TILE_IDS, BIOME_ELEVATION_RANGES, getWallThreshold for hub biomes"
      contains: "canopy_station"
  key_links:
    - from: "packages/shared-types/src/game/biome.ts"
      to: "packages/world-gen/src/generation/biome.ts"
      via: "BiomeType import"
      pattern: "Record<BiomeType,"
    - from: "packages/shared-types/src/game/biome.ts"
      to: "packages/world-gen/src/generation/terrain.ts"
      via: "BiomeType import"
      pattern: "Record<BiomeType,"
---

<objective>
Register four hub station biome types in the type system and all BiomeType-keyed records across shared-types and world-gen packages.

Purpose: Downstream plans (tile definitions, hub config wiring) depend on these biome types existing and compiling. Without them, no hub tile set can be mapped.

Output: `canopy_station`, `ironhold_station`, `meridian_station`, `salvage_station` exist in `BiomeType` and all Record<BiomeType, ...> maps compile with entries for all four.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/shared-types/src/game/biome.ts
@packages/world-gen/src/generation/biome.ts
@packages/world-gen/src/generation/terrain.ts

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From packages/shared-types/src/game/biome.ts:
```typescript
export type BiomeType =
  | 'void_plains'
  | 'crystal_caves'
  // ... 14 more world biome types
  | 'bioluminescent_depths';

export const BIOME_DISPLAY_NAMES: Record<BiomeType, string> = { ... };
export const BIOME_COLORS: Record<BiomeType, string> = { ... };
export const BIOME_TIERS: Record<BiomeType, BiomeTier> = { ... };
```

From packages/world-gen/src/generation/biome.ts:
```typescript
export function getBiomeDangerLevel(biome: BiomeType): number {
  const dangerLevels: Record<BiomeType, number> = { ... };
}
export function getBiomeColor(biome: BiomeType): number {
  const colors: Record<BiomeType, number> = { ... };
}
```

From packages/world-gen/src/generation/terrain.ts:
```typescript
const BIOME_TILES: Record<BiomeType, { floor: TileId; wall: TileId; feature: TileId }> = { ... };
const BIOME_TILE_IDS: Record<BiomeType, { floor: string; wall: string; feature: string }> = { ... };
const BIOME_ELEVATION_RANGES: Record<BiomeType, { min: number; max: number }> = { ... };
function getWallThreshold(biome: BiomeType): number {
  const thresholds: Record<BiomeType, number> = { ... };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 hub biome types to shared-types BiomeType and all keyed records</name>
  <files>packages/shared-types/src/game/biome.ts</files>
  <action>
Add four new hub station biome type literals to the `BiomeType` union type. Place them after `bioluminescent_depths` with a comment section `// Hub Station Biomes`:

```typescript
  | 'canopy_station'         // Verdant Dynamics hub — bioluminescent green/blue
  | 'ironhold_station'       // Helix Extraction hub — industrial gray/rust/orange
  | 'meridian_station'       // Nexus Frontiers hub — corporate silver/white/blue
  | 'salvage_station';       // Unaffiliated hub — patchwork/mixed
```

Add entries to `BIOME_DISPLAY_NAMES`:
```typescript
  canopy_station: 'Canopy Station',
  ironhold_station: 'Ironhold Station',
  meridian_station: 'Meridian Station',
  salvage_station: 'Salvage Station',
```

Add entries to `BIOME_COLORS` with faction-correct palette primary colors:
```typescript
  canopy_station: '#22cc88',       // Bioluminescent green
  ironhold_station: '#aa5522',     // Rust/industrial orange
  meridian_station: '#c0d0e0',     // Corporate silver-blue
  salvage_station: '#8a7a5a',      // Patchwork tan/mixed
```

Add entries to `BIOME_TIERS` — all hub stations are safe (Tier 1, no danger):
```typescript
  canopy_station: 1,
  ironhold_station: 1,
  meridian_station: 1,
  salvage_station: 1,
```
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx nx run shared-types:build 2>&1 | tail -5</automated>
  </verify>
  <done>BiomeType union includes all 4 hub station types. BIOME_DISPLAY_NAMES, BIOME_COLORS, and BIOME_TIERS compile with all 20 entries (16 world + 4 hub).</done>
</task>

<task type="auto">
  <name>Task 2: Add hub biome entries to all world-gen BiomeType-keyed records</name>
  <files>packages/world-gen/src/generation/biome.ts, packages/world-gen/src/generation/terrain.ts</files>
  <action>
**In `packages/world-gen/src/generation/biome.ts`:**

Add entries to `getBiomeDangerLevel` record — hub stations have danger level 0 (safe zones):
```typescript
  canopy_station: 0,
  ironhold_station: 0,
  meridian_station: 0,
  salvage_station: 0,
```

Add entries to `getBiomeColor` record — match the hex equivalents of the CSS colors added in Task 1:
```typescript
  canopy_station: 0x22cc88,
  ironhold_station: 0xaa5522,
  meridian_station: 0xc0d0e0,
  salvage_station: 0x8a7a5a,
```

**In `packages/world-gen/src/generation/terrain.ts`:**

Add entries to `BIOME_TILES` — use temporary placeholder tile IDs (existing tiles) that will be replaced by Plan 03 after hub tiles are defined. Use `TileId.VOID_FLOOR` / `TileId.VOID_WALL` for all four:
```typescript
  canopy_station: { floor: TileId.VOID_FLOOR, wall: TileId.VOID_WALL, feature: TileId.VOID_WALL },
  ironhold_station: { floor: TileId.VOID_FLOOR, wall: TileId.VOID_WALL, feature: TileId.VOID_WALL },
  meridian_station: { floor: TileId.VOID_FLOOR, wall: TileId.VOID_WALL, feature: TileId.VOID_WALL },
  salvage_station: { floor: TileId.VOID_FLOOR, wall: TileId.VOID_WALL, feature: TileId.VOID_WALL },
```

Add entries to `BIOME_TILE_IDS` — same placeholder approach using `TILE_IDS.VOID_FLOOR` / `TILE_IDS.VOID_WALL`:
```typescript
  canopy_station: { floor: TILE_IDS.VOID_FLOOR, wall: TILE_IDS.VOID_WALL, feature: TILE_IDS.VOID_WALL },
  ironhold_station: { floor: TILE_IDS.VOID_FLOOR, wall: TILE_IDS.VOID_WALL, feature: TILE_IDS.VOID_WALL },
  meridian_station: { floor: TILE_IDS.VOID_FLOOR, wall: TILE_IDS.VOID_WALL, feature: TILE_IDS.VOID_WALL },
  salvage_station: { floor: TILE_IDS.VOID_FLOOR, wall: TILE_IDS.VOID_WALL, feature: TILE_IDS.VOID_WALL },
```

Add entries to `BIOME_ELEVATION_RANGES` — hub interiors are flat:
```typescript
  canopy_station: { min: 0, max: 1 },
  ironhold_station: { min: 0, max: 1 },
  meridian_station: { min: 0, max: 1 },
  salvage_station: { min: 0, max: 1 },
```

Add entries to `getWallThreshold` — hubs don't use procedural wall generation (they use hand-designed maps), but the record must be complete. Use 1.0 (never spawn walls):
```typescript
  canopy_station: 1.0,
  ironhold_station: 1.0,
  meridian_station: 1.0,
  salvage_station: 1.0,
```
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx nx run world-gen:build 2>&1 | tail -5</automated>
  </verify>
  <done>All four Record&lt;BiomeType, ...&gt; maps in world-gen compile with all 20 biome entries. No missing key errors. Placeholder tile mappings are in place for Plan 03 to replace.</done>
</task>

</tasks>

<verification>
1. `npx nx run shared-types:build` succeeds — all BiomeType-keyed records have 20 entries
2. `npx nx run world-gen:build` succeeds — all terrain.ts and biome.ts records compile
3. `grep -c 'canopy_station' packages/shared-types/src/game/biome.ts` returns 4+ (type + 3 records)
4. `grep -c 'canopy_station' packages/world-gen/src/generation/biome.ts` returns 2+ (danger + color)
5. `grep -c 'canopy_station' packages/world-gen/src/generation/terrain.ts` returns 4+ (tiles + tile_ids + elevation + threshold)
</verification>

<success_criteria>
- Four new BiomeType literals (`canopy_station`, `ironhold_station`, `meridian_station`, `salvage_station`) exist in the shared-types BiomeType union
- All BiomeType-keyed Record objects across shared-types and world-gen compile without TS errors
- Each hub biome has faction-correct display name, color, and Tier 1 classification
- The full build succeeds: `npx nx run shared-types:build && npx nx run world-gen:build`
</success_criteria>

<output>
After completion, create `.planning/phases/140-biome-tile-foundation/140-01-SUMMARY.md`
</output>
