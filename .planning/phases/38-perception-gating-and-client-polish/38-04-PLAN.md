---
phase: 38-perception-gating-and-client-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/store/gameStore.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Client displays an error message to the player when server emits 'error' event with code 'TOOL_USE_FAILED'"
    - "Level-gated interaction rejection is visible in the chat panel as a system message"
  artifacts:
    - path: "apps/web/src/store/gameStore.ts"
      provides: "gameSocket.on('error', ...) handler that displays error messages"
      contains: "gameSocket.on('error'"
  key_links:
    - from: "apps/web/src/store/gameStore.ts"
      to: "gameSocket error event"
      via: "gameSocket.on('error', ...) handler calling addChatMessage"
      pattern: "gameSocket\\.on\\('error'"
---

<objective>
Wire the 'error' socket event handler to display server-side game errors (e.g., TOOL_USE_FAILED from level gating) to the player via the chat system.

Purpose: Close the gap identified in 38-VERIFICATION.md — server correctly rejects entity:tool_use for level-gated creatures and emits error event, but client silently discards it.

Output: Players see level-gating rejection messages in the chat panel.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/store/gameStore.ts
@apps/web/src/network/socket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register error event handler in gameStore.ts</name>
  <files>apps/web/src/store/gameStore.ts</files>
  <action>
Add a `gameSocket.on('error', ...)` handler after the existing socket event handlers (after the `player:left` handler at line 296).

The error event payload is `{ code: string; message: string }` (per shared-types ServerEvents['error']).

Handler implementation:
1. Extract `message` from the error payload
2. Create a ChatMessage object with the correct interface fields:
   - `id`: Generate using `Date.now().toString()`
   - `senderId`: `'system'`
   - `senderName`: `'System'`
   - `message`: The error message from the payload
   - `channel`: `'system'`
   - `timestamp`: `Date.now()` (number, not ISO string)
3. Call `useGameStore.getState().addChatMessage(chatMessage)` to display it in chat

Example:
```typescript
// Handle server errors (e.g., level-gated interaction rejection)
gameSocket.on('error', ({ code, message }: { code: string; message: string }) => {
  const chatMessage: ChatMessage = {
    id: Date.now().toString(),
    senderId: 'system',
    senderName: 'System',
    message: message,
    channel: 'system',
    timestamp: Date.now(),
  };
  useGameStore.getState().addChatMessage(chatMessage);
});
```

Note: `ChatMessage` is already imported at line 2. No new imports needed.
  </action>
  <verify>
1. Check TypeScript compiles: `pnpm --filter web build` (or `pnpm build`)
2. Grep for the handler: `grep -n "gameSocket.on('error'" apps/web/src/store/gameStore.ts`
3. Verify ChatMessage structure matches the interface in shared-types
  </verify>
  <done>
gameSocket.on('error', ...) handler exists in gameStore.ts. When server emits an error event with code TOOL_USE_FAILED, the message appears in the player's chat panel as a system message.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. `grep "gameSocket.on('error'" apps/web/src/store/gameStore.ts` returns the handler
3. Manual test (optional): Trigger a level-gated interaction in-game and observe the chat panel for the error message
</verification>

<success_criteria>
- The 'error' socket event is handled in gameStore.ts
- Error messages from the server are displayed to the player via the chat system
- INTR-07 requirement fully satisfied: level-gated rejection is now visible to the player
</success_criteria>

<output>
After completion, create `.planning/phases/38-perception-gating-and-client-polish/38-04-SUMMARY.md`
</output>
