---
phase: 04-websocket-connection-auth-handshake
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/web/src/components/ErrorModal.tsx
  - apps/web/src/components/ReconnectOverlay.tsx
  - apps/web/src/styles/loading.css
autonomous: true

must_haves:
  truths:
    - "ErrorModal displays user-friendly message with E-XXXX code"
    - "ErrorModal has action button (retry, go to login, etc)"
    - "ReconnectOverlay shows 'Reconnecting...' with progress dots"
    - "ReconnectOverlay appears during disconnect state"
  artifacts:
    - path: "apps/web/src/components/ErrorModal.tsx"
      provides: "Error modal with code and action button"
      min_lines: 50
    - path: "apps/web/src/components/ReconnectOverlay.tsx"
      provides: "Reconnecting overlay"
      min_lines: 30
  key_links:
    - from: "ErrorModal.tsx"
      to: "ErrorCodeInfo type"
      via: "props interface"
      pattern: "ErrorCodeInfo"
---

<objective>
Create ErrorModal and ReconnectOverlay components for error handling and reconnection UI.

Purpose: Show user-friendly errors with support codes, non-blocking reconnection overlay
Output: ErrorModal.tsx, ReconnectOverlay.tsx, updated loading.css
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-websocket-connection-auth-handshake/04-RESEARCH.md
@.planning/phases/04-websocket-connection-auth-handshake/04-01-SUMMARY.md

@packages/shared-types/src/network/messages.ts
@apps/web/src/styles/screens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ErrorModal component</name>
  <files>apps/web/src/components/ErrorModal.tsx</files>
  <action>
Create ErrorModal.tsx component per user decision (modal overlay with retry option):

1. Import React, useNavigate from react-router
2. Import ErrorCodeInfo type from @into-the-void/shared-types

3. Props interface:
   ```typescript
   interface ErrorModalProps {
     error: {
       code: string;      // E-XXXX
       message: string;   // User-friendly message
       action: 'redirect-login' | 'redirect-characters' | 'retry' | 'none';
     };
     onRetry?: () => void;
     onClose?: () => void;
   }
   ```

4. Component implementation:
   - Full-screen semi-transparent overlay (position: fixed, background: rgba(0,0,0,0.8))
   - Centered modal box (max-width: 400px, padding, border-radius)
   - Error icon (simple CSS or text-based)
   - Message text (large, clear)
   - Support code text: "(E-XXXX)" in smaller muted text
   - Action button based on error.action:
     - 'redirect-login': "Go to Login" -> navigate('/login')
     - 'redirect-characters': "Select Character" -> navigate('/character-select')
     - 'retry': "Try Again" -> calls onRetry prop
     - 'none': "Close" -> calls onClose prop

5. Use existing CSS variables (--color-*, etc) from screens.css
6. Import and append styles to loading.css (or add error-modal styles)
  </action>
  <verify>File exists and exports ErrorModal component</verify>
  <done>ErrorModal displays message with E-code and appropriate action button</done>
</task>

<task type="auto">
  <name>Task 2: Create ReconnectOverlay component</name>
  <files>apps/web/src/components/ReconnectOverlay.tsx</files>
  <action>
Create ReconnectOverlay.tsx component (non-blocking modal per Claude's discretion):

1. Import React, useState, useEffect
2. Import useGameStore for connectionState

3. Component shows when connectionState === 'disconnected' (not 'error')

4. Implementation:
   - Semi-transparent overlay that doesn't block all interaction (pointer-events: none on overlay, pointer-events: auto on content)
   - Centered modal with "Reconnecting..." text
   - Animated dots (...) that cycle: "." -> ".." -> "..." -> "."
   - No close button - auto-dismisses when connection restored
   - Optional: show reconnect attempt count if available

5. Use useEffect for dot animation (cycle every 500ms)

6. The overlay appears over the game world so player can still see their position while waiting
  </action>
  <verify>File exists and exports ReconnectOverlay component</verify>
  <done>ReconnectOverlay shows non-blocking reconnecting message with animated dots</done>
</task>

<task type="auto">
  <name>Task 3: Add error and reconnect styles to loading.css</name>
  <files>apps/web/src/styles/loading.css</files>
  <action>
Append error modal and reconnect overlay styles to loading.css:

```css
/* Error Modal */
.error-modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.85);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1100;
}

.error-modal {
  background: var(--color-bg-secondary, #1a1a2e);
  border: 1px solid var(--color-danger, #ff4444);
  border-radius: 12px;
  padding: 32px;
  max-width: 400px;
  width: 90%;
  text-align: center;
}

.error-modal-icon {
  font-size: 48px;
  margin-bottom: 16px;
  color: var(--color-danger, #ff4444);
}

.error-modal-message {
  font-size: 18px;
  color: var(--color-text-primary, #fff);
  margin-bottom: 8px;
  line-height: 1.4;
}

.error-modal-code {
  font-size: 14px;
  color: var(--color-text-secondary, #888);
  margin-bottom: 24px;
}

.error-modal-button {
  padding: 12px 24px;
  background: var(--color-accent, #00bfff);
  color: #fff;
  border: none;
  border-radius: 6px;
  font-size: 16px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.error-modal-button:hover {
  background: var(--color-accent-hover, #0099cc);
}

/* Reconnect Overlay */
.reconnect-overlay {
  position: fixed;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
  z-index: 1050;
}

.reconnect-modal {
  background: rgba(0, 0, 0, 0.9);
  border: 1px solid #444;
  border-radius: 8px;
  padding: 24px 32px;
  pointer-events: auto;
}

.reconnect-text {
  font-size: 18px;
  color: #fff;
  display: flex;
  align-items: center;
  gap: 4px;
}

.reconnect-dots {
  min-width: 24px;
  text-align: left;
}
```
  </action>
  <verify>loading.css contains error-modal-* and reconnect-* classes</verify>
  <done>Styles exist for ErrorModal and ReconnectOverlay</done>
</task>

</tasks>

<verification>
1. Build web app: `cd apps/web && pnpm build`
2. Build passes without errors
3. ErrorModal.tsx exists with error prop (code, message, action)
4. ErrorModal.tsx has action button that navigates or calls callback
5. ReconnectOverlay.tsx exists with animated dots
6. loading.css has error-modal-* and reconnect-* classes
</verification>

<success_criteria>
- ErrorModal shows user-friendly message with E-XXXX support code
- ErrorModal has action button appropriate to error type (login, retry, etc)
- ReconnectOverlay shows non-blocking "Reconnecting..." with animated dots
- Styles follow existing CSS variable patterns
</success_criteria>

<output>
After completion, create `.planning/phases/04-websocket-connection-auth-handshake/04-04-SUMMARY.md`
</output>
