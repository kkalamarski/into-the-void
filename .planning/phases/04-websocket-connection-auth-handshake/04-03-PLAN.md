---
phase: 04-websocket-connection-auth-handshake
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/web/src/components/LoadingScreen.tsx
  - apps/web/src/components/ConnectionIndicator.tsx
  - apps/web/src/styles/loading.css
autonomous: true

must_haves:
  truths:
    - "Loading screen shows progress bar with percentage"
    - "Loading screen rotates lore-accurate tips every 5 seconds"
    - "Loading screen displays current stage text"
    - "Connection indicator shows colored dot (green/yellow/red)"
    - "Connection indicator shows latency bars (1-4) when connected"
    - "Connection indicator shows latency in ms when connected"
  artifacts:
    - path: "apps/web/src/components/LoadingScreen.tsx"
      provides: "Loading screen with progress bar and tips"
      min_lines: 80
    - path: "apps/web/src/components/ConnectionIndicator.tsx"
      provides: "Corner connection status with latency"
      min_lines: 60
    - path: "apps/web/src/styles/loading.css"
      provides: "Styles for loading and connection UI"
      min_lines: 50
  key_links:
    - from: "LoadingScreen.tsx"
      to: "gameStore loadingStage"
      via: "useGameStore hook"
      pattern: "useGameStore.*loadingStage"
    - from: "ConnectionIndicator.tsx"
      to: "gameStore latency"
      via: "useGameStore hook"
      pattern: "useGameStore.*latency"
---

<objective>
Create LoadingScreen and ConnectionIndicator UI components per user decisions.

Purpose: Show loading progress with lore tips, always-visible connection status with latency
Output: LoadingScreen.tsx, ConnectionIndicator.tsx, loading.css
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-websocket-connection-auth-handshake/04-RESEARCH.md
@.planning/phases/04-websocket-connection-auth-handshake/04-02-SUMMARY.md

@apps/web/src/store/gameStore.ts
@apps/web/src/styles/screens.css
@lore/world-bible.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoadingScreen component with lore tips</name>
  <files>apps/web/src/components/LoadingScreen.tsx</files>
  <action>
Create LoadingScreen.tsx component:

1. Import useGameStore for loadingStage and loadingProgress
2. Import React, useState, useEffect

3. Create LOADING_TIPS array with lore-accurate tips from world-bible.md:
   ```typescript
   const LOADING_TIPS = [
     "Terminus orbits a stable yellow star in a region nearly devoid of other stellar bodies.",
     "The Ancients vanished without a trace. No bodies. No war damage. Simply... gone.",
     "Verdant Dynamics: 'Sustainability is Profitability' - the green corporation.",
     "Helix Extraction: 'Humanity's Survival Demands Sacrifice' - the pragmatists.",
     "Nexus Frontiers: 'Connecting Worlds, Creating Opportunities' - the moderates.",
     "Anomaly Zones: Regions where reality behaves incorrectly. High risk, high reward.",
     "Maniacs attack anything they perceive, constantly, without regard for self-preservation.",
     "The Luminous Canopy: dense forests where bioluminescence saturates every surface.",
     "The Coastal Shallows: where tides reveal secrets beneath dual moons.",
     "The Scarred Badlands: what's left after everything is taken.",
     "Experience is the only teacher that matters on Terminus.",
     "On Terminus, you are what you do. Your past doesn't matter.",
   ];
   ```

4. Component implementation:
   - Use useState for currentTip index
   - Use useEffect to rotate tips every 5 seconds (setInterval)
   - Get loadingStage and loadingProgress from useGameStore

5. Get stage text:
   ```typescript
   const getStageText = (stage: string): string => {
     switch (stage) {
       case 'connecting': return 'Connecting to server...';
       case 'authenticating': return 'Authenticating character...';
       case 'loading-world': return 'Loading world data...';
       case 'spawning': return 'Spawning into world...';
       case 'ready': return 'Ready!';
       default: return 'Preparing...';
     }
   };
   ```

6. Render:
   - Full-screen black overlay (position: fixed, inset: 0)
   - Stage text (24px, bold)
   - Progress bar (400px wide, 24px tall, rounded, gradient fill)
   - Percentage text
   - Lore tip (italic, max-width 500px, min-height to prevent layout shift)

7. Import loading.css for styles
  </action>
  <verify>File exists and exports LoadingScreen component</verify>
  <done>LoadingScreen component renders with progress bar and rotating lore tips</done>
</task>

<task type="auto">
  <name>Task 2: Create ConnectionIndicator component</name>
  <files>apps/web/src/components/ConnectionIndicator.tsx</files>
  <action>
Create ConnectionIndicator.tsx component:

1. Import useGameStore for connectionState and latency
2. Import React

3. Create helper functions:
   ```typescript
   const getStatusColor = (state: ConnectionState, latency: number): string => {
     switch (state) {
       case 'authenticated':
         return latency < 100 ? '#44cc44' : latency < 200 ? '#ffcc00' : '#ff8800';
       case 'connected':
       case 'connecting':
         return '#ffcc00'; // Yellow for pending
       case 'disconnected':
       case 'error':
         return '#ff4444'; // Red for problems
       default:
         return '#666666';
     }
   };

   const getLatencyBars = (latency: number): number => {
     if (latency < 50) return 4;  // Excellent
     if (latency < 100) return 3; // Good
     if (latency < 200) return 2; // Fair
     return 1; // Poor
   };

   const getStatusText = (state: ConnectionState): string | null => {
     switch (state) {
       case 'connecting': return 'Connecting...';
       case 'connected': return 'Authenticating...';
       case 'disconnected': return 'Disconnected';
       case 'error': return 'Connection Error';
       default: return null;
     }
   };
   ```

4. Render (per user decision - corner indicator):
   - Position: fixed, top: 10px, right: 10px
   - Background: rgba(0, 0, 0, 0.7), padding, border-radius
   - Status dot: 12px circle with appropriate color
   - When authenticated: show latency bars (4 vertical bars, increasing height, colored based on filled count)
   - When authenticated: show latency in ms
   - When not authenticated: show status text instead of latency

5. Import loading.css for styles
  </action>
  <verify>File exists and exports ConnectionIndicator component</verify>
  <done>ConnectionIndicator shows colored dot, latency bars when connected, status text when not</done>
</task>

<task type="auto">
  <name>Task 3: Create loading.css styles</name>
  <files>apps/web/src/styles/loading.css</files>
  <action>
Create loading.css with styles for loading and connection UI:

```css
/* Loading Screen */
.loading-screen {
  position: fixed;
  inset: 0;
  background-color: #000;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #fff;
  gap: 32px;
  z-index: 1000;
}

.loading-stage-text {
  font-size: 24px;
  font-weight: 600;
  color: var(--color-text-primary, #fff);
}

.loading-progress-container {
  width: 400px;
  height: 24px;
  background-color: #222;
  border-radius: 12px;
  overflow: hidden;
  border: 2px solid #444;
}

.loading-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #00bfff, #44cc44);
  transition: width 0.3s ease;
}

.loading-percentage {
  font-size: 18px;
  opacity: 0.7;
}

.loading-tip {
  max-width: 500px;
  text-align: center;
  font-size: 16px;
  opacity: 0.8;
  font-style: italic;
  margin-top: 32px;
  min-height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Connection Indicator */
.connection-indicator {
  position: fixed;
  top: 10px;
  right: 10px;
  display: flex;
  align-items: center;
  gap: 8px;
  background: rgba(0, 0, 0, 0.7);
  padding: 8px 12px;
  border-radius: 6px;
  font-size: 14px;
  color: #fff;
  z-index: 999;
}

.connection-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  flex-shrink: 0;
}

.connection-latency-bars {
  display: flex;
  gap: 2px;
  align-items: flex-end;
  height: 16px;
}

.connection-latency-bar {
  width: 4px;
  border-radius: 1px;
  transition: background-color 0.2s;
}

.connection-latency-bar:nth-child(1) { height: 4px; }
.connection-latency-bar:nth-child(2) { height: 8px; }
.connection-latency-bar:nth-child(3) { height: 12px; }
.connection-latency-bar:nth-child(4) { height: 16px; }

.connection-latency-bar.active {
  /* Color set inline based on latency */
}

.connection-latency-bar.inactive {
  background-color: #444;
}

.connection-latency-ms {
  font-size: 12px;
  opacity: 0.8;
  min-width: 40px;
}

.connection-status-text {
  font-size: 12px;
  opacity: 0.9;
}
```
  </action>
  <verify>File exists with CSS classes for loading-screen, loading-progress-*, connection-*</verify>
  <done>Styles exist for LoadingScreen and ConnectionIndicator components</done>
</task>

</tasks>

<verification>
1. Build web app: `cd apps/web && pnpm build`
2. Build passes without errors
3. LoadingScreen.tsx exists with LOADING_TIPS array (lore-accurate)
4. LoadingScreen.tsx uses useGameStore for loadingStage/loadingProgress
5. ConnectionIndicator.tsx exists with getStatusColor, getLatencyBars helpers
6. ConnectionIndicator.tsx uses useGameStore for connectionState/latency
7. loading.css exists with all required classes
</verification>

<success_criteria>
- LoadingScreen shows progress bar (0-100%) with current stage text
- LoadingScreen rotates lore-accurate tips every 5 seconds
- ConnectionIndicator shows colored dot (green/yellow/red based on state + latency)
- ConnectionIndicator shows 1-4 latency bars when authenticated
- ConnectionIndicator shows ms value when authenticated
- ConnectionIndicator shows status text when not authenticated
</success_criteria>

<output>
After completion, create `.planning/phases/04-websocket-connection-auth-handshake/04-03-SUMMARY.md`
</output>
