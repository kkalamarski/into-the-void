---
phase: 04-websocket-connection-auth-handshake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/game.gateway.ts
  - apps/game-server/src/game/player.service.ts
  - packages/shared-types/src/network/messages.ts
  - packages/shared-types/src/network/index.ts
autonomous: true

must_haves:
  truths:
    - "Server disconnects clients that don't authenticate within 5 seconds"
    - "Server disconnects clients with invalid/expired tokens"
    - "Server returns error codes with user-friendly messages"
    - "Server supports ping/pong for latency measurement"
    - "Server enables connection state recovery for brief disconnects"
  artifacts:
    - path: "apps/game-server/src/game/game.gateway.ts"
      provides: "Enhanced auth with timeout, disconnect, ping handler"
      contains: "authTimeout"
    - path: "packages/shared-types/src/network/messages.ts"
      provides: "Error code types with E-XXXX format"
      contains: "ErrorCodeMap"
  key_links:
    - from: "game.gateway.ts handleConnection"
      to: "authTimeout setTimeout"
      via: "5 second timeout on unauthenticated connections"
      pattern: "setTimeout.*5000"
    - from: "game.gateway.ts handleAuth"
      to: "client.disconnect()"
      via: "disconnect on auth failure"
      pattern: "client\\.disconnect"
---

<objective>
Enhance game-server WebSocket authentication with timeout, proper disconnect on failure, error codes, and ping/pong support.

Purpose: Prevent stuck connections, provide actionable error codes, measure latency
Output: Hardened auth flow, error code system, latency measurement endpoint
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-websocket-connection-auth-handshake/04-RESEARCH.md

@apps/game-server/src/game/game.gateway.ts
@apps/game-server/src/game/player.service.ts
@packages/shared-types/src/network/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error code types and E-XXXX mapping</name>
  <files>packages/shared-types/src/network/messages.ts, packages/shared-types/src/network/index.ts</files>
  <action>
Add ErrorCodeMap interface and E-XXXX code mapping to messages.ts:

1. Add ErrorCodeMap type that maps internal codes to user-facing codes:
   - AUTH_FAILED -> E-1001
   - AUTH_EXPIRED -> E-1002
   - AUTH_TIMEOUT -> E-1003
   - INVALID_CHARACTER -> E-1004
   - SERVER_UNAVAILABLE -> E-2001
   - CONNECTION_LOST -> E-2002
   - (keep existing codes: INVALID_ACTION, INVALID_TARGET, etc with E-3xxx range)

2. Add ErrorCodeInfo interface with:
   - code: string (E-XXXX)
   - internalCode: ErrorCode
   - message: string (user-friendly)
   - action: 'redirect-login' | 'redirect-characters' | 'retry' | 'none'

3. Add getErrorInfo function that takes ErrorCode and returns ErrorCodeInfo with appropriate user message

4. Export new types from index.ts
  </action>
  <verify>Run `pnpm build` in packages/shared-types - should compile without errors</verify>
  <done>Error code system exists with E-XXXX format, user-friendly messages, and action hints</done>
</task>

<task type="auto">
  <name>Task 2: Enhance game gateway with auth timeout, disconnect, and ping</name>
  <files>apps/game-server/src/game/game.gateway.ts</files>
  <action>
Enhance game.gateway.ts:

1. Add connectionStateRecovery to @WebSocketGateway decorator:
   ```typescript
   connectionStateRecovery: {
     maxDisconnectionDuration: 2 * 60 * 1000, // 2 minutes
     skipMiddlewares: true,
   }
   ```

2. In handleConnection:
   - Set 5 second auth timeout using setTimeout
   - Store timeout reference in client.data.authTimeout
   - On timeout: emit auth:error with E-1003 (AUTH_TIMEOUT), then disconnect
   - Log timeout events for monitoring

3. In handleAuth:
   - Clear authTimeout immediately: clearTimeout(client.data.authTimeout)
   - On auth failure: emit auth:error with proper ErrorCodeInfo format, then call client.disconnect()
   - Add try-catch wrapper (already exists, enhance error response format)
   - Use getErrorInfo from shared-types for consistent error responses

4. Add ping handler:
   ```typescript
   @SubscribeMessage('ping')
   handlePing(@MessageBody() timestamp: number): number {
     return timestamp; // Acknowledgment callback returns to client
   }
   ```

5. Import getErrorInfo from @into-the-void/shared-types
  </action>
  <verify>Run `pnpm build` in apps/game-server - should compile without errors</verify>
  <done>Server disconnects unauthenticated clients after 5s, returns ErrorCodeInfo on failure, supports ping</done>
</task>

<task type="auto">
  <name>Task 3: Add isAuthenticated check to PlayerService</name>
  <files>apps/game-server/src/game/player.service.ts</files>
  <action>
Add helper method to PlayerService for checking if a socket is authenticated:

1. Add method:
   ```typescript
   isAuthenticated(socketId: string): boolean {
     return this.socketToPlayer.has(socketId);
   }
   ```

2. This enables the gateway to check auth status during timeout evaluation
   (already used in handleConnection timeout check via getPlayerBySocket)
  </action>
  <verify>Run `pnpm build` in apps/game-server - should compile without errors</verify>
  <done>PlayerService has isAuthenticated method for checking socket auth status</done>
</task>

</tasks>

<verification>
1. Build shared-types: `cd packages/shared-types && pnpm build`
2. Build game-server: `cd apps/game-server && pnpm build`
3. All builds pass without errors
4. ErrorCodeInfo interface exists with code, internalCode, message, action fields
5. game.gateway.ts has authTimeout in handleConnection
6. game.gateway.ts has client.disconnect() in handleAuth failure path
7. game.gateway.ts has @SubscribeMessage('ping') handler
</verification>

<success_criteria>
- Server disconnects clients that don't auth within 5 seconds (AUTH_TIMEOUT E-1003)
- Server disconnects clients with invalid tokens (AUTH_FAILED E-1001)
- Server returns ErrorCodeInfo with user-friendly message and action hint
- Server responds to ping events for latency measurement
- Connection state recovery enabled for 2-minute disconnections
</success_criteria>

<output>
After completion, create `.planning/phases/04-websocket-connection-auth-handshake/04-01-SUMMARY.md`
</output>
