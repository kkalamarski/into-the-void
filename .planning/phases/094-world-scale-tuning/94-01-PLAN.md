---
phase: 94-world-scale-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/world-gen/src/generation/biome.ts
autonomous: true

must_haves:
  truths:
    - "Biome regions are approximately 3-4 chunks (192-256 tiles) in diameter"
    - "Biome boundaries remain organic with domain warping (no grid-like edges)"
    - "Player walking straight in one direction encounters a different biome within 2-3 minutes"
  artifacts:
    - path: "packages/world-gen/src/generation/biome.ts"
      provides: "Tuned biome generation parameters"
      contains: "minBiomeChunks: 4"
  key_links:
    - from: "packages/world-gen/src/generation/biome.ts"
      to: "BiomeGenerator.getBiome()"
      via: "minBiomeChunks controls region snapping"
      pattern: "minBiomeChunks.*chunkSize"
---

<objective>
Reduce biome scale to encourage exploration

Purpose: Current biomes (640+ tiles) take 5+ minutes to cross, discouraging exploration. Target 192-256 tile biomes for 2-3 minute traversal.

Output: Tuned biome.ts with smaller biome regions and adjusted noise parameters for natural transitions
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/world-gen/src/generation/biome.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce minBiomeChunks from 10 to 4</name>
  <files>packages/world-gen/src/generation/biome.ts</files>
  <action>
Update DEFAULT_BIOME_PARAMS in biome.ts:

1. Change `minBiomeChunks` from 10 to 4:
   - Current: 10 chunks = 640 tiles (10 x 64)
   - Target: 4 chunks = 256 tiles (4 x 64)
   - At 2 tiles/sec, crossing 256 tiles = 128 seconds (~2 min)
   - Walking to neighboring biome center (half + half = 256 tiles) = ~2-2.5 min

2. Increase noise scales proportionally to maintain variation at smaller scale:
   - `temperatureScale`: 0.002 -> 0.005 (2.5x increase matches 2.5x smaller regions)
   - `moistureScale`: 0.003 -> 0.0075 (same ratio)
   - `elevationScale`: 0.0012 -> 0.003 (same ratio)

3. Adjust warp parameters in getWarpOffset() for smaller regions:
   - `warpScale`: 0.003 -> 0.006 (higher frequency wiggle for smaller regions)
   - `warpStrength`: keep at regionSize * 0.4 (self-scaling to new region size)

Update the comment on minBiomeChunks to reflect the new target: "Minimum 4 chunks per biome region (~2 min to cross at 2 tiles/sec)"
  </action>
  <verify>Run `pnpm build` - no TypeScript errors in world-gen package</verify>
  <done>DEFAULT_BIOME_PARAMS has minBiomeChunks=4, increased noise scales, and warpScale adjusted for smaller regions</done>
</task>

<task type="auto">
  <name>Task 2: Add BIOME_PARAMS constant export for validation</name>
  <files>packages/world-gen/src/generation/biome.ts</files>
  <action>
Export the DEFAULT_BIOME_PARAMS constant so it can be imported by tests and validation scripts:

1. Change `const DEFAULT_BIOME_PARAMS` to `export const DEFAULT_BIOME_PARAMS`

2. Export the BiomeParams interface as well: `export interface BiomeParams`

This enables Plan 94-02 to validate biome sizes programmatically without magic numbers.
  </action>
  <verify>Run `pnpm build` - exports are available from @into-the-void/world-gen</verify>
  <done>DEFAULT_BIOME_PARAMS and BiomeParams are exported from biome.ts</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds with no errors
2. `grep -n "minBiomeChunks" packages/world-gen/src/generation/biome.ts` shows value of 4
3. `grep -n "temperatureScale" packages/world-gen/src/generation/biome.ts` shows 0.005
</verification>

<success_criteria>
1. minBiomeChunks reduced from 10 to 4
2. Noise scales increased proportionally (2.5x)
3. Warp scale adjusted for smaller regions
4. Constants exported for validation use
5. Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/094-world-scale-tuning/94-01-SUMMARY.md`
</output>
