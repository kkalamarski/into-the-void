---
phase: 66-quest-completion-rewards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/inventory.service.ts
  - apps/game-server/src/game/trade.service.ts
autonomous: true

must_haves:
  truths:
    - "Quest items cannot be dropped from inventory"
    - "Quest items cannot be sold to traders"
    - "Items marked with isQuestItem=true are protected"
  artifacts:
    - path: "apps/game-server/src/game/inventory.service.ts"
      provides: "Quest item drop guard in removeItem"
      contains: "isQuestItem"
    - path: "apps/game-server/src/game/trade.service.ts"
      provides: "Quest item sell guard"
      contains: "isQuestItem"
  key_links:
    - from: "InventoryService.removeItem"
      to: "item.properties.isQuestItem"
      via: "property check before removal"
      pattern: "properties\\.isQuestItem"
    - from: "TradeService.sell"
      to: "item.properties.isQuestItem"
      via: "property check before sale"
      pattern: "properties\\.isQuestItem"
---

<objective>
Add quest item guards to prevent dropping or trading items marked as quest-bound.

Purpose: Quest items must be protected from player actions while the quest is active. This prevents players from accidentally losing quest-critical items or selling them to traders.

Output: Modified InventoryService and TradeService with isQuestItem guards.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-quest-completion-rewards/66-RESEARCH.md
@packages/database/src/schema/inventories.ts
@apps/game-server/src/game/inventory.service.ts
@apps/game-server/src/game/trade.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add quest item drop guard to InventoryService</name>
  <files>apps/game-server/src/game/inventory.service.ts</files>
  <action>
Modify `InventoryService.removeItem()` to check for quest items before allowing removal:

1. After finding the item by instanceId (line ~176), add a guard check:
```typescript
// GUARD: Prevent removing quest items
if (item.properties?.isQuestItem === true) {
  return { success: false, reason: 'Quest items cannot be dropped' };
}
```

2. The check must happen BEFORE the splice operation that removes the item.

3. The `properties` field already exists in InventoryItemJson as `Record<string, unknown>`, so no type changes needed.

4. Do NOT modify addItem or other methods - only removeItem needs the guard.

Pattern from research: mirrors TradeService sell guard pattern (check property, return early with error).
  </action>
  <verify>
Read the modified removeItem method and confirm:
- Guard check exists before splice
- Returns `{ success: false, reason: 'Quest items cannot be dropped' }` when isQuestItem is true
- Normal removal logic unchanged for non-quest items
  </verify>
  <done>
InventoryService.removeItem rejects removal of items with `properties.isQuestItem === true` and returns appropriate error message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add quest item sell guard to TradeService</name>
  <files>apps/game-server/src/game/trade.service.ts</files>
  <action>
Modify `TradeService.sell()` to check for quest items before allowing sale:

1. After finding the item in inventory (line ~141-144), add a guard check:
```typescript
// GUARD: Prevent selling quest items
if (item.properties?.isQuestItem === true) {
  return { success: false, error: 'Quest items cannot be sold' };
}
```

2. The check must happen AFTER item is found but BEFORE the removeItem call (line ~159).

3. Place the guard check immediately after the "Unknown item" check for itemDef (around line 150).

4. Do NOT modify buy or other methods - only sell needs the guard.

Pattern from research: Same guard pattern as drop protection. Early return prevents item from being sold and credits from being granted.
  </action>
  <verify>
Read the modified sell method and confirm:
- Guard check exists after item lookup, before removeItem call
- Returns `{ success: false, error: 'Quest items cannot be sold' }` when isQuestItem is true
- Normal sale logic unchanged for non-quest items
  </verify>
  <done>
TradeService.sell rejects sale of items with `properties.isQuestItem === true` and returns appropriate error message.
  </done>
</task>

</tasks>

<verification>
Both guards implemented:
1. `grep -n "isQuestItem" apps/game-server/src/game/inventory.service.ts` shows guard in removeItem
2. `grep -n "isQuestItem" apps/game-server/src/game/trade.service.ts` shows guard in sell
3. `pnpm build` succeeds (no type errors)
</verification>

<success_criteria>
- InventoryService.removeItem guards against quest item removal
- TradeService.sell guards against quest item sale
- Build passes with no errors
- Guards return meaningful error messages
</success_criteria>

<output>
After completion, create `.planning/phases/66-quest-completion-rewards/66-01-SUMMARY.md`
</output>
