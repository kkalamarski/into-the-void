---
phase: 31-server-wiring-socket-delivery
plan: 03
type: execute
wave: 1
depends_on: ["31-02"]
files_modified:
  - apps/web/src/ui/GameUI.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "statsStore.ts module is imported and its socket handler registers at runtime"
    - "After login, statsStore.stats contains the CharStatsPayload from stats:update"
  artifacts:
    - path: "apps/web/src/ui/GameUI.tsx"
      provides: "Side-effect import that loads statsStore module"
      contains: "import '../store/statsStore'"
  key_links:
    - from: "apps/web/src/ui/GameUI.tsx"
      to: "apps/web/src/store/statsStore.ts"
      via: "side-effect import"
      pattern: "import.*statsStore"
---

<objective>
Wire statsStore.ts into the client module graph so its socket handler registers at runtime.

Purpose: Close verification gap — statsStore.ts exists and is correct but is never imported, so `gameSocket.on('stats:update', ...)` never executes.
Output: GameUI.tsx imports statsStore.ts; stats:update events populate the store.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-server-wiring-socket-delivery/31-VERIFICATION.md
@apps/web/src/ui/GameUI.tsx
@apps/web/src/store/statsStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add statsStore side-effect import to GameUI.tsx</name>
  <files>apps/web/src/ui/GameUI.tsx</files>
  <action>
Add a side-effect import for statsStore at line 12 (after the inventoryStore import):

```typescript
import '../store/statsStore'; // Side-effect: registers stats:update socket handler
```

This follows the same pattern as inventoryStore.ts which is imported by multiple components to ensure its module-level socket wiring executes. Since GameUI.tsx is always rendered when a player is in-game, the import guarantees statsStore loads at runtime.

Do NOT import `useStatsStore` — that is Phase 32's job (rendering the stats panel). This plan only needs the side-effect import to activate the socket handler.
  </action>
  <verify>
1. `grep -n "statsStore" apps/web/src/ui/GameUI.tsx` shows the import line
2. `pnpm build` completes without errors
3. No TypeScript errors in the file
  </verify>
  <done>
statsStore.ts is in the module graph via GameUI.tsx import; its `gameSocket.on('stats:update', ...)` handler will execute when the module loads at runtime.
  </done>
</task>

</tasks>

<verification>
- [ ] GameUI.tsx contains `import '../store/statsStore'` (grep confirms)
- [ ] Build passes: `pnpm build` exits 0
- [ ] statsStore.ts is no longer orphaned (has at least 1 importer)
</verification>

<success_criteria>
The two failed verification truths are now satisfiable:
1. "Client receives stats:update event and stores payload in statsStore" — import activates socket handler
2. "statsStore.stats contains total, base, and equipment breakdown after login" — store receives data once handler is active
</success_criteria>

<output>
After completion, create `.planning/phases/31-server-wiring-socket-delivery/31-03-SUMMARY.md`
</output>
