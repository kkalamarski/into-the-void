---
phase: 30-type-foundation-pure-computation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/core/player.ts
  - packages/game-logic/src/combat/damage.ts
  - packages/game-logic/src/combat/turn-order.ts
  - packages/database/src/schema/characters.ts
autonomous: true

must_haves:
  truths:
    - "CharacterStats type has all 8 stats: durability, toughness, power, haste, vigor, recovery, perception, resilience"
    - "PlayerStats type no longer exists anywhere in the codebase"
    - "Combat functions use new stat names (power, toughness, haste) not old names (strength, endurance, agility)"
    - "StatsJson in database schema has 8-stat shape matching CharacterStats"
  artifacts:
    - path: "packages/shared-types/src/core/player.ts"
      provides: "CharacterStats and StatScaleTarget types"
      contains: "interface CharacterStats"
    - path: "packages/game-logic/src/combat/damage.ts"
      provides: "Updated damage calculation with new stat names"
      contains: "attackerStats.power"
    - path: "packages/game-logic/src/combat/turn-order.ts"
      provides: "Updated initiative calculation with haste"
      contains: "stats?.haste"
    - path: "packages/database/src/schema/characters.ts"
      provides: "StatsJson with 8-stat shape"
      contains: "durability: number"
  key_links:
    - from: "packages/game-logic/src/combat/damage.ts"
      to: "@into-the-void/shared-types"
      via: "import CharacterStats"
      pattern: "import.*CharacterStats.*from.*@into-the-void/shared-types"
    - from: "packages/game-logic/src/combat/turn-order.ts"
      to: "@into-the-void/shared-types"
      via: "import CharacterStats"
      pattern: "import.*CharacterStats.*from.*@into-the-void/shared-types"
---

<objective>
Replace the legacy 5-stat PlayerStats type with the canonical 8-stat CharacterStats type across all packages.

Purpose: Establish the type foundation so downstream code (computeCharStats, server, UI) can import CharacterStats from shared-types without type errors.

Output: Updated type definitions in shared-types, combat files in game-logic, and StatsJson in database schema.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-type-foundation-pure-computation/30-RESEARCH.md
@packages/shared-types/src/core/player.ts
@packages/game-logic/src/combat/damage.ts
@packages/game-logic/src/combat/turn-order.ts
@packages/database/src/schema/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PlayerStats with CharacterStats in shared-types</name>
  <files>packages/shared-types/src/core/player.ts</files>
  <action>
Replace the `PlayerStats` interface (lines 52-64) with `CharacterStats` containing all 8 stats:
- durability (affects max health and suit HP)
- toughness (affects damage reduction / armor)
- power (affects damage output - replaces strength)
- haste (affects turn order / action speed - replaces agility)
- vigor (affects energy pool and stamina)
- recovery (affects regeneration rate)
- perception (affects detection range - retained from legacy)
- resilience (affects status-effect resistance)

Add JSDoc comments explaining each stat's purpose (see RESEARCH.md Pattern 1).

Also add `StatScaleTarget` type: `'player' | 'creature'` - used by computeCharStats in Plan 02.

Delete the entire `PlayerStats` interface - do not keep it as an alias. The shared-types/src/index.ts already uses `export * from './core/player'` so CharacterStats will be exported automatically.
  </action>
  <verify>
Run `pnpm tsc --noEmit` in packages/shared-types - should pass (or show errors in consumer packages which is expected at this stage).
Grep for PlayerStats: `grep -r "PlayerStats" packages/shared-types/` should return nothing.
  </verify>
  <done>
CharacterStats interface exists with all 8 stats, PlayerStats is deleted, StatScaleTarget type exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update combat files to use CharacterStats and new stat names</name>
  <files>packages/game-logic/src/combat/damage.ts, packages/game-logic/src/combat/turn-order.ts</files>
  <action>
In damage.ts:
1. Change import from `PlayerStats` to `CharacterStats`
2. Update `DamageParams` interface: change `Partial<PlayerStats>` to `Partial<CharacterStats>`
3. Rename stat field accesses:
   - `attackerStats.strength` -> `attackerStats.power`
   - `attackerStats.agility` -> `attackerStats.haste`
   - `defenderStats.endurance` -> `defenderStats.toughness`
4. In `calculateHitChance`: rename `attackerAgility`/`defenderAgility` to `attackerHaste`/`defenderHaste`, access `.haste` instead of `.agility`

In turn-order.ts:
1. Change import from `PlayerStats` to `CharacterStats`
2. Update all `Partial<PlayerStats>` to `Partial<CharacterStats>`
3. Rename stat access: `stats?.agility` -> `stats?.haste`
4. Rename variable `agilityBonus` -> `hasteBonus` for clarity

CRITICAL: After renaming, grep for old stat names to ensure no references remain:
`grep -E "(strength|agility|endurance)" packages/game-logic/src/combat/`
  </action>
  <verify>
Run `pnpm tsc --noEmit` in packages/game-logic - should pass.
Grep confirms no old stat names: `grep -E "\\.(strength|agility|endurance)" packages/game-logic/src/combat/` returns nothing.
  </verify>
  <done>
Both combat files import CharacterStats, use Partial<CharacterStats>, and reference power/haste/toughness instead of strength/agility/endurance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update StatsJson in database schema to 8-stat shape</name>
  <files>packages/database/src/schema/characters.ts</files>
  <action>
Update `StatsJson` interface to match CharacterStats:
```typescript
export interface StatsJson {
  durability: number;
  toughness: number;
  power: number;
  haste: number;
  vigor: number;
  recovery: number;
  perception: number;
  resilience: number;
}
```

Update the default value in the `stats` column definition (jsonb default):
```typescript
stats: jsonb('stats').$type<StatsJson>().notNull().default({
  durability: 100,
  toughness: 50,
  power: 50,
  haste: 50,
  vigor: 80,
  recovery: 30,
  perception: 40,
  resilience: 30,
}),
```

These default values match the player base stats at level 1 from RESEARCH.md SCALE_CONSTANTS.

NOTE: This updates the TypeScript interface and schema default. Existing DB rows still have old JSON shape - Phase 31 handles the migration script. This is intentional per the research findings.
  </action>
  <verify>
Run `pnpm tsc --noEmit` in packages/database - should pass.
Grep confirms new shape: `grep "durability" packages/database/src/schema/characters.ts` returns the interface line.
  </verify>
  <done>
StatsJson has 8 fields matching CharacterStats, default values are set to level-1 player base stats.
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. Run `pnpm tsc --noEmit` from repo root - entire workspace compiles with no type errors
2. Grep for PlayerStats anywhere: `grep -r "PlayerStats" packages/` returns nothing
3. Grep for old stat names in combat: `grep -E "\\.(strength|agility|endurance)" packages/game-logic/` returns nothing
4. CharacterStats is importable: add a test import `import { CharacterStats } from '@into-the-void/shared-types'` in a scratch file
</verification>

<success_criteria>
- [x] CharacterStats type defined with all 8 primary stats
- [x] PlayerStats type completely removed from codebase
- [x] damage.ts uses power/toughness/haste instead of strength/endurance/agility
- [x] turn-order.ts uses haste instead of agility
- [x] StatsJson in database schema matches the 8-stat shape
- [x] Entire workspace compiles with `pnpm tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/30-type-foundation-pure-computation/30-01-SUMMARY.md`
</output>
