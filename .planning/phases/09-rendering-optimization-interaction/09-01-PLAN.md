---
phase: 09-rendering-optimization-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/scenes/WorldScene.ts
autonomous: true

must_haves:
  truths:
    - "W/Up arrow moves player toward top of screen (NW in grid)"
    - "S/Down arrow moves player toward bottom of screen (SE in grid)"
    - "A/Left arrow moves player toward left of screen (SW in grid)"
    - "D/Right arrow moves player toward right of screen (NE in grid)"
    - "WASD cancels active pathfinding"
  artifacts:
    - path: "apps/web/src/game/scenes/WorldScene.ts"
      provides: "Screen-relative WASD input mapping"
      contains: "direction = 'nw'"
  key_links:
    - from: "WorldScene.handleInput"
      to: "MovementController.processInput"
      via: "diagonal direction strings"
      pattern: "processInput\\('(nw|ne|se|sw)'\\)"
---

<objective>
Update WASD input handling to use screen-relative directions for intuitive isometric movement.

Purpose: Players expect "up" to move toward the top of the screen, not true North. In isometric view, the top of the screen is Northwest in grid space. This plan remaps keyboard input so controls feel natural.

Output: WorldScene.handleInput() maps W=NW, D=NE, S=SE, A=SW
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rendering-optimization-interaction/09-RESEARCH.md
@apps/web/src/game/scenes/WorldScene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap WASD to screen-relative diagonal directions</name>
  <files>apps/web/src/game/scenes/WorldScene.ts</files>
  <action>
Update the handleInput() method in WorldScene to map keyboard input to screen-relative directions:

1. Find the handleInput() method (around line 235)
2. Change the direction mappings from cardinal to diagonal:
   - W/Up → 'nw' (visual up = Northwest in grid)
   - D/Right → 'ne' (visual right = Northeast in grid)
   - S/Down → 'se' (visual down = Southeast in grid)
   - A/Left → 'sw' (visual left = Southwest in grid)

Replace:
```typescript
if (this.cursors?.up.isDown || this.wasd?.W.isDown) direction = 'n';
else if (this.cursors?.down.isDown || this.wasd?.S.isDown) direction = 's';
else if (this.cursors?.left.isDown || this.wasd?.A.isDown) direction = 'w';
else if (this.cursors?.right.isDown || this.wasd?.D.isDown) direction = 'e';
```

With:
```typescript
// Screen-relative mapping for isometric view:
// Visual "up" (W) = Northwest in grid
// Visual "right" (D) = Northeast in grid
// Visual "down" (S) = Southeast in grid
// Visual "left" (A) = Southwest in grid
if (this.cursors?.up.isDown || this.wasd?.W.isDown) direction = 'nw';
else if (this.cursors?.right.isDown || this.wasd?.D.isDown) direction = 'ne';
else if (this.cursors?.down.isDown || this.wasd?.S.isDown) direction = 'se';
else if (this.cursors?.left.isDown || this.wasd?.A.isDown) direction = 'sw';
```

Note: MovementController and server already support diagonal directions ('nw', 'ne', 'se', 'sw'). The pathfinding getDirection() also supports diagonals.
  </action>
  <verify>
Run TypeScript compilation: `cd /Users/krzysztof.kalamarski/Projects/into-the-void && pnpm nx run web:build`
Grep for diagonal direction assignments: `grep -n "direction = 'n" apps/web/src/game/scenes/WorldScene.ts`
  </verify>
  <done>
WASD/arrow keys map to diagonal directions ('nw', 'ne', 'se', 'sw') instead of cardinal ('n', 's', 'e', 'w'). Comment explains screen-relative mapping rationale.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. handleInput() contains diagonal direction assignments ('nw', 'ne', 'se', 'sw')
3. No cardinal direction assignments remain ('n', 's', 'e', 'w' for WASD)
4. Comment documents the screen-relative mapping rationale
</verification>

<success_criteria>
- [ ] W/Up arrow triggers 'nw' direction
- [ ] D/Right arrow triggers 'ne' direction
- [ ] S/Down arrow triggers 'se' direction
- [ ] A/Left arrow triggers 'sw' direction
- [ ] TypeScript compilation passes
- [ ] Existing pathfinding cancellation behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-rendering-optimization-interaction/09-01-SUMMARY.md`
</output>
