---
phase: 50-trading-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/queries/characters.ts
  - packages/shared-types/src/network/events.ts
autonomous: true

must_haves:
  truths:
    - "deductCredits validates sufficient balance before deduction"
    - "addCredits increases player balance atomically"
    - "trade:buy and trade:sell socket event types exist"
  artifacts:
    - path: "packages/database/src/queries/characters.ts"
      provides: "deductCredits() and addCredits() functions"
      exports: ["deductCredits", "addCredits"]
    - path: "packages/shared-types/src/network/events.ts"
      provides: "trade:buy and trade:sell client/server event types"
      contains: "trade:buy"
  key_links:
    - from: "deductCredits"
      to: "characters table"
      via: "UPDATE with WHERE credits >= amount check"
      pattern: "update.*characters.*credits"
---

<objective>
Add credit mutation operations to database layer and socket event types for trading.

Purpose: Establish the atomic DB operations needed for safe trading (deduct with validation, add) and the socket contract for trade events.
Output: deductCredits(), addCredits() in database package; trade:buy, trade:sell event types in shared-types
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/database/src/queries/characters.ts
@packages/shared-types/src/network/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add credit mutation functions to database package</name>
  <files>packages/database/src/queries/characters.ts</files>
  <action>
Add two new exported functions to characters.ts:

1. `deductCredits(db: DbClient, characterId: string, amount: number): Promise<{ success: boolean; newBalance?: number; error?: string }>`
   - Validate amount > 0, reject negative
   - Use UPDATE with WHERE clause: `credits >= amount` to prevent negative balance
   - Return `{ success: true, newBalance }` on success
   - Return `{ success: false, error: 'Insufficient credits' }` if WHERE fails (rowcount 0)
   - Pattern: `UPDATE characters SET credits = credits - $amount WHERE id = $id AND credits >= $amount RETURNING credits`

2. `addCredits(db: DbClient, characterId: string, amount: number): Promise<{ success: boolean; newBalance: number }>`
   - Validate amount > 0
   - Use simple UPDATE: `SET credits = credits + $amount`
   - Return updated credits balance

Use Drizzle's `sql` template literal for the atomic UPDATE with check. Import `sql` from 'drizzle-orm'.
  </action>
  <verify>
Run `pnpm build --filter @into-the-void/database` - should compile without errors.
Check exports: `grep -n "deductCredits\|addCredits" packages/database/src/queries/characters.ts`
  </verify>
  <done>
deductCredits and addCredits functions exist in characters.ts with atomic credit validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Export credit functions from database package index</name>
  <files>packages/database/src/index.ts</files>
  <action>
Add exports for the new functions:
```typescript
export { deductCredits, addCredits } from './queries/characters';
```

Ensure they are importable from '@into-the-void/database'.
  </action>
  <verify>
Run `grep -n "deductCredits\|addCredits" packages/database/src/index.ts` - should show both exports.
  </verify>
  <done>
deductCredits and addCredits are exported from @into-the-void/database.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add trade socket event types to shared-types</name>
  <files>packages/shared-types/src/network/events.ts</files>
  <action>
1. Add to ClientEventType union:
   - 'trade:buy'
   - 'trade:sell'

2. Add to ClientEvents interface:
   ```typescript
   'trade:buy': { npcId: string; itemId: string; quantity: number };
   'trade:sell': { npcId: string; itemInstanceId: string; quantity: number };
   ```

3. Add to ServerEventType union:
   - 'trade:result'
   - 'credits:update'

4. Add to ServerEvents interface:
   ```typescript
   'trade:result': {
     success: boolean;
     action: 'buy' | 'sell';
     itemId?: string;
     quantity?: number;
     totalPrice?: number;
     newBalance?: number;
     error?: string;
   };
   'credits:update': { credits: number };
   ```

Note: trade:result is the response after a trade completes. credits:update pushes balance changes to client.
  </action>
  <verify>
Run `pnpm build --filter @into-the-void/shared-types` - should compile without errors.
Run `grep -n "trade:buy\|trade:sell\|trade:result\|credits:update" packages/shared-types/src/network/events.ts` - should show all 4 events.
  </verify>
  <done>
trade:buy, trade:sell, trade:result, and credits:update event types exist in shared-types.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build --filter @into-the-void/database` passes
2. `pnpm build --filter @into-the-void/shared-types` passes
3. Both deductCredits and addCredits are exported from database package
4. All 4 trade event types exist in events.ts
</verification>

<success_criteria>
- deductCredits() validates balance before deduction (uses WHERE credits >= amount)
- addCredits() atomically increases balance
- trade:buy, trade:sell client events have proper payloads
- trade:result, credits:update server events have proper payloads
- All packages compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/50-trading-system/50-01-SUMMARY.md`
</output>
