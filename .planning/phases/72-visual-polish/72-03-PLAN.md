---
phase: 72-visual-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/ui/panels/NpcInteractionModal.tsx
  - apps/web/src/ui/panels/NpcInteractionModal.css
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Modal windows fade in over 150ms with backdrop blur effect"
    - "Glassmorphism effect visible on NPC modal backdrop"
    - "Escape key and close button still close the modal"
  artifacts:
    - path: "apps/web/src/ui/panels/NpcInteractionModal.tsx"
      provides: "Overlay wrapper with visibility toggle"
      contains: "npc-modal-overlay"
    - path: "apps/web/src/ui/panels/NpcInteractionModal.css"
      provides: "Restored modal content fade animation"
      contains: "npc-modal-overlay--visible"
  key_links:
    - from: "apps/web/src/ui/panels/NpcInteractionModal.tsx"
      to: "apps/web/src/ui/panels/NpcInteractionModal.css"
      via: "className with npc-modal-overlay and --visible modifier"
      pattern: "npc-modal-overlay.*--visible"
---

<objective>
Wire glassmorphism modal overlay to React component

Purpose: Close gap from 72-VERIFICATION.md - the CSS for modal overlay with backdrop blur and fade transition exists but the React component doesn't render the overlay wrapper. This blocks VIS-03 (smooth modal transitions) and VIS-04 (glassmorphism effect).

Output: Working NPC modal with 150ms fade-in, backdrop blur, and glassmorphism effect on open
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-visual-polish/72-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overlay wrapper to NpcInteractionModal component</name>
  <files>apps/web/src/ui/panels/NpcInteractionModal.tsx</files>
  <action>
Modify the NpcInteractionModal component to wrap the modal in an overlay div:

1. The component currently returns null when `!interactingNpc` (line 235). Keep this behavior but move the return earlier or restructure.

2. When `interactingNpc` exists, wrap the entire modal in an overlay div:
```tsx
<div className="npc-modal-overlay npc-modal-overlay--visible">
  <div
    className="npc-modal ui-panel"
    style={{ ... }}
  >
    {/* existing modal content */}
  </div>
</div>
```

3. The overlay needs `npc-modal-overlay--visible` class to show (CSS has opacity:0 by default, opacity:1 when --visible).

4. Update the close button click and overlay background click to close:
   - Close button already calls `closeInteraction()` - keep that
   - Add onClick on overlay that closes IF clicking the overlay itself (not the modal content). Use `e.target === e.currentTarget` check to only close when clicking overlay background, not modal content.

5. Escape key handler already exists (lines 211-222) and calls `closeInteraction()` - no change needed.

6. Remove the `position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%)` from the modal div style since the overlay now handles centering via flexbox (CSS lines 6-8). Keep only the drag offset transform: `transform: translate(${position.x}px, ${position.y}px)`.

Key structural change:
- Before: `<div className="npc-modal">...</div>` returned directly
- After: `<div className="npc-modal-overlay npc-modal-overlay--visible"><div className="npc-modal">...</div></div>`
  </action>
  <verify>
grep -n "npc-modal-overlay" apps/web/src/ui/panels/NpcInteractionModal.tsx
# Should show: className containing "npc-modal-overlay npc-modal-overlay--visible"
  </verify>
  <done>
NpcInteractionModal renders overlay wrapper div with --visible class, modal content inside overlay, overlay click-to-close on background only
  </done>
</task>

<task type="auto">
  <name>Task 2: Update modal CSS for overlay-based centering</name>
  <files>apps/web/src/ui/panels/NpcInteractionModal.css</files>
  <action>
Update `.npc-modal` CSS (lines 36-47) to work with the overlay wrapper:

1. Remove `position: fixed` - overlay handles this now
2. Remove `top: 50%; left: 50%; transform: translate(-50%, -50%)` - overlay centers via flexbox
3. Keep: `width: 700px`, `max-height: 80vh`, `display: flex`, `flex-direction: column`, `z-index: 100`, `box-shadow: var(--shadow-modal)`

The updated `.npc-modal` should be:
```css
.npc-modal {
  width: 700px;
  max-height: 80vh;
  display: flex;
  flex-direction: column;
  z-index: 100;
  box-shadow: var(--shadow-modal);
}
```

The modal will now be centered by the overlay's flexbox (lines 6-8: `display: flex; align-items: center; justify-content: center`).

IMPORTANT: The drag functionality uses transform on the modal. After this change, the modal's transform will ONLY contain the drag offset, not the centering. This is correct because the overlay handles centering.
  </action>
  <verify>
grep -A 10 "^\.npc-modal {" apps/web/src/ui/panels/NpcInteractionModal.css
# Should NOT show: position: fixed, top: 50%, left: 50%
# Should show: width, max-height, display: flex, z-index, box-shadow
  </verify>
  <done>
.npc-modal CSS no longer has fixed positioning, relies on overlay flexbox for centering, drag transform works independently
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Type check passes:
```bash
npx tsc --noEmit -p apps/web/tsconfig.json
```

2. Visual verification (requires human):
   - Start dev server: `pnpm dev`
   - Go to game, find trader NPC, interact
   - Observe: Modal should fade in over 150ms with blurred backdrop showing game world
   - Observe: Clicking outside modal (on backdrop) should close it
   - Observe: Escape key should close it
   - Observe: Modal should be draggable
   - Observe: Trade, quests, dialogue tabs should work normally
</verification>

<success_criteria>
- NpcInteractionModal.tsx renders `.npc-modal-overlay` wrapper with `--visible` class
- `.npc-modal` CSS no longer uses fixed positioning for centering
- Modal appears with 150ms fade-in and backdrop blur (glassmorphism)
- Clicking overlay background closes modal
- Escape key still closes modal
- Close button still closes modal
- Modal remains draggable
- All existing functionality (tabs, trade, quests) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/72-visual-polish/72-03-SUMMARY.md`
</output>
