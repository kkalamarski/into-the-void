---
phase: 49-npc-interaction-window
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - apps/web/src/ui/panels/NpcInteractionModal.tsx
  - apps/web/src/ui/panels/NpcInteractionModal.css
  - apps/web/src/ui/GameUI.tsx
autonomous: true

must_haves:
  truths:
    - "Modal opens when npcStore.interactingNpc is non-null"
    - "Modal displays NPC portrait placeholder, name, type label, and dialogue"
    - "Dialogue text is drawn from NPC definition (not hardcoded)"
  artifacts:
    - path: "apps/web/src/ui/panels/NpcInteractionModal.tsx"
      provides: "NPC interaction modal React component"
      exports: ["NpcInteractionModal"]
    - path: "apps/web/src/ui/panels/NpcInteractionModal.css"
      provides: "Modal styling matching existing panel patterns"
      min_lines: 50
  key_links:
    - from: "apps/web/src/ui/panels/NpcInteractionModal.tsx"
      to: "useNpcStore"
      via: "Zustand hook subscription"
      pattern: "useNpcStore"
    - from: "apps/web/src/ui/GameUI.tsx"
      to: "NpcInteractionModal"
      via: "conditional render"
      pattern: "<NpcInteractionModal"
---

<objective>
Create the NpcInteractionModal component that displays NPC portrait, name, type, and dialogue text when a player clicks an NPC.

Purpose: Visual feedback for NPC interaction — players need to see who they're talking to and what the NPC says.

Output: NpcInteractionModal.tsx with portrait, name, type label, dialogue display; wired into GameUI.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/src/store/npcStore.ts
@apps/web/src/ui/panels/EquipmentPanel.tsx
@apps/web/src/ui/panels/EquipmentPanel.css
@apps/web/src/ui/GameUI.tsx
@packages/npcs/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NpcInteractionModal component</name>
  <files>apps/web/src/ui/panels/NpcInteractionModal.tsx</files>
  <action>
Create NpcInteractionModal.tsx following EquipmentPanel pattern:

```typescript
import React, { useEffect } from 'react';
import { useNpcStore } from '../../store/npcStore';
import { useGameStore } from '../../store/gameStore';
import { useDraggablePanel } from '../../hooks/useDraggablePanel';
import './NpcInteractionModal.css';

// NPC type display labels
const NPC_TYPE_LABELS: Record<string, string> = {
  trader: 'Trader',
  guard: 'Guard',
  faction_rep: 'Faction Representative',
  ambient: 'Citizen',
  service: 'Service',
};

export const NpcInteractionModal: React.FC = () => {
  const { interactingNpc, closeInteraction } = useNpcStore();
  const { position, isDragging, handleMouseDown } = useDraggablePanel();

  // Disable Phaser keyboard when modal is open
  useEffect(() => {
    const game = useGameStore.getState().game;
    const worldScene = game?.getWorldScene();
    if (worldScene) {
      worldScene.setKeyboardEnabled(false);
    }

    return () => {
      const game = useGameStore.getState().game;
      const worldScene = game?.getWorldScene();
      if (worldScene) {
        worldScene.setKeyboardEnabled(true);
      }
    };
  }, []);

  if (!interactingNpc) return null;

  // Get greeting dialogue (condition: 'greeting' or first dialogue)
  const greetingLine = interactingNpc.dialogue.find(d => d.condition === 'greeting')
    ?? interactingNpc.dialogue[0];
  const dialogueText = greetingLine?.text ?? '...';

  // Portrait color from NPC definition
  const portraitColor = `#${interactingNpc.color.toString(16).padStart(6, '0')}`;

  return (
    <div
      className="npc-modal ui-panel"
      style={{ transform: `translate(-50%, -50%) translate(${position.x}px, ${position.y}px)` }}
    >
      <div
        className="npc-modal-header"
        onMouseDown={handleMouseDown}
        style={{ cursor: isDragging ? 'grabbing' : 'grab' }}
      >
        <span>{interactingNpc.displayName}</span>
        <button className="close-btn" onClick={closeInteraction}>&times;</button>
      </div>

      <div className="npc-modal-content">
        {/* Portrait section */}
        <div className="npc-portrait-section">
          <div className="npc-portrait" style={{ backgroundColor: portraitColor }}>
            {/* Future: actual portrait sprite. For now, colored placeholder */}
          </div>
          <div className="npc-identity">
            <span className="npc-name">{interactingNpc.displayName}</span>
            <span className="npc-type">{NPC_TYPE_LABELS[interactingNpc.npcType] ?? interactingNpc.npcType}</span>
            {interactingNpc.title && <span className="npc-title">{interactingNpc.title}</span>}
            {interactingNpc.role && <span className="npc-role">{interactingNpc.role}</span>}
          </div>
        </div>

        {/* Dialogue section */}
        <div className="npc-dialogue">
          <p className="npc-dialogue-text">"{dialogueText}"</p>
        </div>

        {/* Action buttons placeholder - wired in Plan 03 */}
        <div className="npc-actions">
          {/* Action buttons will be added in next plan */}
        </div>
      </div>
    </div>
  );
};
```

Key points:
- Uses useDraggablePanel hook for panel dragging (same as EquipmentPanel)
- Disables Phaser keyboard on mount, re-enables on unmount
- Reads interactingNpc from npcStore
- Shows portrait as colored div (placeholder until sprites exist)
- Displays greeting dialogue from definition
- Close button calls closeInteraction()
  </action>
  <verify>TypeScript compiles with `pnpm build --filter=web`</verify>
  <done>NpcInteractionModal.tsx exists with portrait, name, type, and dialogue display</done>
</task>

<task type="auto">
  <name>Task 2: Create NpcInteractionModal CSS</name>
  <files>apps/web/src/ui/panels/NpcInteractionModal.css</files>
  <action>
Create CSS following EquipmentPanel.css patterns:

```css
.npc-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 360px;
  max-height: 80vh;
  display: flex;
  flex-direction: column;
  z-index: 100;
}

.npc-modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-bottom: 10px;
  border-bottom: 1px solid var(--color-bg-tertiary);
  margin-bottom: 10px;
}

.npc-modal-header span {
  font-weight: 600;
  color: var(--color-text-primary);
}

.npc-modal-header .close-btn {
  background: none;
  border: none;
  color: var(--color-text-secondary);
  font-size: 20px;
  cursor: pointer;
  padding: 0 5px;
}

.npc-modal-header .close-btn:hover {
  color: var(--color-text-primary);
}

.npc-modal-content {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* Portrait section */
.npc-portrait-section {
  display: flex;
  gap: 12px;
  align-items: center;
}

.npc-portrait {
  width: 64px;
  height: 64px;
  border-radius: 8px;
  border: 2px solid var(--color-bg-tertiary);
  flex-shrink: 0;
}

.npc-identity {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.npc-name {
  font-size: 16px;
  font-weight: 600;
  color: var(--color-text-primary);
}

.npc-type {
  font-size: 12px;
  color: var(--color-accent);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.npc-title,
.npc-role {
  font-size: 11px;
  color: var(--color-text-secondary);
  font-style: italic;
}

/* Dialogue section */
.npc-dialogue {
  background: var(--color-bg-tertiary);
  border-radius: 6px;
  padding: 12px 14px;
}

.npc-dialogue-text {
  font-size: 13px;
  color: var(--color-text-primary);
  line-height: 1.5;
  margin: 0;
  font-style: italic;
}

/* Action buttons */
.npc-actions {
  display: flex;
  gap: 8px;
  justify-content: flex-end;
  margin-top: 4px;
}

.npc-action-btn {
  padding: 8px 16px;
  border: none;
  border-radius: 4px;
  font-size: 13px;
  font-weight: 500;
  cursor: pointer;
  transition: background-color 0.2s, transform 0.1s;
}

.npc-action-btn:hover {
  transform: translateY(-1px);
}

.npc-action-btn--primary {
  background: var(--color-accent);
  color: var(--color-bg-primary);
}

.npc-action-btn--primary:hover {
  background: var(--color-accent-hover, var(--color-accent));
}

.npc-action-btn--secondary {
  background: var(--color-bg-tertiary);
  color: var(--color-text-primary);
}

.npc-action-btn--secondary:hover {
  background: var(--color-bg-secondary);
}
```

Uses existing CSS variables for consistency with game theme.
  </action>
  <verify>CSS file exists and imports work</verify>
  <done>NpcInteractionModal.css exists with styling for portrait, dialogue, and action buttons</done>
</task>

<task type="auto">
  <name>Task 3: Wire NpcInteractionModal into GameUI</name>
  <files>apps/web/src/ui/GameUI.tsx</files>
  <action>
1. Import useNpcStore and NpcInteractionModal:
```typescript
import { useNpcStore } from '../store/npcStore';
import { NpcInteractionModal } from './panels/NpcInteractionModal';
```

2. Add to component:
```typescript
const { interactingNpc } = useNpcStore();
```

3. Render modal conditionally (after DeathScreen, before minimap-border):
```tsx
{interactingNpc && <NpcInteractionModal />}
```

The modal only renders when interactingNpc is non-null (set by npc:interact:response handler).
  </action>
  <verify>TypeScript compiles; GameUI renders NpcInteractionModal when npcStore has interactingNpc</verify>
  <done>GameUI conditionally renders NpcInteractionModal based on npcStore state</done>
</task>

</tasks>

<verification>
1. Build succeeds: `pnpm build` passes without errors
2. Modal renders: With interactingNpc set, modal appears centered on screen
3. Content displays: Portrait color, name, type label, and dialogue text all visible
4. Keyboard disabled: Phaser keyboard input disabled while modal open
</verification>

<success_criteria>
- NpcInteractionModal.tsx renders when interactingNpc is non-null
- Portrait shows colored placeholder (NPC color from definition)
- Name and type label displayed prominently
- Dialogue text shows greeting line from NPC definition
- Close button visible in header
- Modal is draggable (useDraggablePanel hook)
- Phaser keyboard disabled while modal open
</success_criteria>

<output>
After completion, create `.planning/phases/49-npc-interaction-window/49-02-SUMMARY.md`
</output>
