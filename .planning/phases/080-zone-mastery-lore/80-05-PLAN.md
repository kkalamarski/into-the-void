---
phase: 80-zone-mastery-lore
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/discovery.service.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "POI discovery triggers zone mastery objective tracking"
    - "Zone mastery Bronze tier can be completed (requires POI discoveries)"
  artifacts:
    - path: "apps/game-server/src/game/discovery.service.ts"
      provides: "POI discovery event emission"
      contains: "eventEmitter.emit('poi.discovered'"
  key_links:
    - from: "apps/game-server/src/game/discovery.service.ts"
      to: "apps/game-server/src/game/zone-mastery.service.ts"
      via: "EventEmitter2 poi.discovered event"
      pattern: "eventEmitter\\.emit\\('poi\\.discovered'"
---

<objective>
Wire POI discovery events to zone mastery tracking by adding EventEmitter2 emission to DiscoveryService.

Purpose: Close the gap identified in VERIFICATION.md where POI discoveries are not being tracked for zone mastery objectives. The ZoneMasteryService listener exists but DiscoveryService never emits the event.

Output: DiscoveryService emits 'poi.discovered' event after successful POI discovery, completing the event-driven architecture for all three objective types.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/080-zone-mastery-lore/80-VERIFICATION.md
@apps/game-server/src/game/discovery.service.ts
@apps/game-server/src/game/gathering.service.ts (reference pattern, lines 2, 70, 294-299)
@apps/game-server/src/game/zone-mastery.service.ts (listener at line 60-67)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EventEmitter2 injection and emit poi.discovered event</name>
  <files>apps/game-server/src/game/discovery.service.ts</files>
  <action>
Add EventEmitter2 to DiscoveryService following the established codebase pattern:

1. Add import at line 2:
   `import { EventEmitter2 } from '@nestjs/event-emitter';`

2. Update constructor (line 33) to inject EventEmitter2:
   ```typescript
   constructor(
     private readonly databaseService: DatabaseService,
     private readonly eventEmitter: EventEmitter2,
   ) {}
   ```

3. After the db.insert() on line 69 (inside attemptDiscovery method), emit the event:
   ```typescript
   // Emit event for zone mastery tracking
   this.eventEmitter.emit('poi.discovered', {
     characterId,
     poiId,
     biome,
   });
   ```

The payload shape matches the PoiDiscoveredPayload interface expected by ZoneMasteryService:
- characterId: string (already available as parameter)
- poiId: string (already available as parameter)
- biome: string (already available as parameter)

Do NOT modify any other methods or add any other functionality.
  </action>
  <verify>
1. `pnpm build` succeeds with no TypeScript errors
2. `grep -n "eventEmitter.emit('poi.discovered'" apps/game-server/src/game/discovery.service.ts` returns the new emission line
3. `grep -n "EventEmitter2" apps/game-server/src/game/discovery.service.ts` shows both import and constructor injection
  </verify>
  <done>
DiscoveryService emits 'poi.discovered' event with { characterId, poiId, biome } payload after every successful POI discovery. The event is emitted after the database insert but before reward calculation, matching the anti-exploit pattern (discovery recorded first).
  </done>
</task>

</tasks>

<verification>
1. Build verification: `pnpm build` completes without errors
2. Pattern verification: Event emission follows exact same pattern as gathering.service.ts line 294
3. Integration verification: ZoneMasteryService @OnEvent('poi.discovered') handler at line 60 will now receive events
4. Flow verification: POI discovery -> db.insert -> emit event -> ZoneMasteryService.handlePoiDiscovered -> updateObjective
</verification>

<success_criteria>
- DiscoveryService imports EventEmitter2 from @nestjs/event-emitter
- DiscoveryService constructor injects EventEmitter2
- attemptDiscovery() emits 'poi.discovered' event after successful db.insert
- Event payload includes { characterId, poiId, biome }
- Build passes with no errors
- Gap from VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/080-zone-mastery-lore/80-05-SUMMARY.md`
</output>
