---
phase: 126-procedural-terrain-cubes
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: []
autonomous: false
requirements: [TERR-01, TERR-02, TERR-03, TERR-04, TERR-05]

must_haves:
  truths:
    - "Every visible tile renders as a 3-shade isometric cube with no flat-color fallback"
    - "Each biome is visually distinct — identifiable by color alone"
    - "Accent details are visible and biome-specific at normal zoom"
    - "Elevation tinting is clearly visible — high tiles bright, low tiles dark"
    - "Frame rate is stable (no regression from v1.25)"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the procedural terrain cube system. Start the dev server, navigate through multiple biomes, and verify that all success criteria for Phase 126 are met.

Purpose: Catch visual regressions, verify biome distinctness, and confirm elevation tinting before marking the phase complete.
Output: Verification report confirming or flagging issues.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/126-procedural-terrain-cubes/126-CONTEXT.md
@.planning/phases/126-procedural-terrain-cubes/126-01-SUMMARY.md
@.planning/phases/126-procedural-terrain-cubes/126-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and fix any compilation errors</name>
  <files>
    apps/web/src/game/rendering/ProceduralTileGenerator.ts
    apps/web/src/game/scenes/PreloadScene.ts
    apps/web/src/game/rendering/TileRenderer.ts
  </files>
  <action>
Run the full build to catch any compilation issues from Plans 01 and 02:

```bash
cd /Users/krzysztof.kalamarski/Projects/into-the-void
pnpm build 2>&1 | tail -50
```

If there are TypeScript errors:
1. Read the error messages carefully
2. Fix type errors in the files modified by Plans 01 and 02
3. Common issues to watch for:
   - Missing Phaser type imports in ProceduralTileGenerator.ts
   - Texture key type mismatches in TileRenderer.ts
   - Import path issues

Run build again after fixes to confirm clean compilation.

Also run lint:
```bash
pnpm lint 2>&1 | tail -30
```

Fix any lint errors in the new/modified files.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Full build passes with zero errors. Lint passes on all modified files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Procedural terrain cube system for all 16 biomes (30 tile types). Every tile now renders as a 3-shade isometric cube with biome-specific accent details, baked to GPU textures during preload. Elevation tinting preserved.
  </what-built>
  <how-to-verify>
1. Start the dev server: `pnpm dev`
2. Open http://localhost:5173 and log in
3. **Check void_plains (starting biome):**
   - Tiles should be deep indigo cubes with 3 distinct faces (top, lit south, shadow east)
   - Small pebble/crack accent details visible on top face
   - NO flat-color diamonds — every tile is a cube

4. **Check elevation tinting:**
   - Look for elevated terrain (walls, formations)
   - Higher tiles should appear noticeably brighter
   - Lower tiles should be darker
   - Shadow darkening near cliff edges should be visible

5. **Check biome variety (navigate to different biomes):**
   - Each biome should have a distinct color palette
   - Crystal biomes: cyan/blue tones with reflective accents
   - Toxic biomes: olive-brown/yellow-green
   - Volcanic: dark red-brown with orange accents
   - Ice: pale blue-white
   - Exotic biomes (void_rift, crystalline, bioluminescent): vivid alien colors

6. **Check accent detail visibility:**
   - Details should be noticeable at normal zoom level
   - Natural biomes: identifiable shapes (grass tufts, cracks, ripples)
   - Exotic biomes: abstract patterns (glitch blocks, energy veins, glow dots)
   - All 3 faces should have details (top most, sides less)

7. **Check variant variety:**
   - Same biome should show subtle tile-to-tile variation (not all identical)
   - Same tile position should look the same after page refresh (deterministic)

8. **Check performance:**
   - No visible frame drops when moving through dense biomes
   - Chunk loading should not stutter

Report any issues with specific biomes or visual problems.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues (which biome, what's wrong)</resume-signal>
</task>

</tasks>

<verification>
- [ ] Full build passes with zero TypeScript errors
- [ ] Lint passes on all modified files
- [ ] Every tile renders as a 3-shade cube (no flat fallback)
- [ ] Biomes are visually distinct by color
- [ ] Accent details visible at normal zoom
- [ ] Elevation tinting works (bright highs, dark lows)
- [ ] Variant selection is deterministic (same tile = same look across refreshes)
- [ ] No frame rate regression
</verification>

<success_criteria>
All Phase 126 success criteria from ROADMAP.md are visually confirmed:
1. Every visible tile is a 3-shade isometric cube
2. Each biome is identifiable by tile color alone
3. Deterministic variant selection per position
4. Elevation tinting clearly visible
5. No frame rate degradation
</success_criteria>

<output>
After completion, create `.planning/phases/126-procedural-terrain-cubes/126-03-SUMMARY.md`
</output>
