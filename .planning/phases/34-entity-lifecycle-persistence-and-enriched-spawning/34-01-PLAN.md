---
phase: 34-entity-lifecycle-persistence-and-enriched-spawning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/database/src/schema/entity-lifecycle.ts
  - packages/database/src/schema/index.ts
  - apps/game-server/src/zones/zones.module.ts
  - apps/game-server/src/zones/zones.service.ts
  - apps/game-server/package.json
autonomous: true

must_haves:
  truths:
    - "Entities spawning in a zone have correct maxHealth derived from EntityRegistry"
    - "Killed entities do not reappear when zone is re-entered until respawnAt elapses"
    - "Respawn timers survive server restart via DB persistence"
  artifacts:
    - path: "packages/database/src/schema/entity-lifecycle.ts"
      provides: "entity_lifecycle Drizzle table with entityId PK, zoneId, killedAt, respawnAt"
      contains: "export const entityLifecycle"
    - path: "apps/game-server/src/zones/zones.service.ts"
      provides: "enriched createEntityFromSpawn() using EntityRegistry"
      contains: "EntityRegistry.get"
  key_links:
    - from: "apps/game-server/src/zones/zones.service.ts"
      to: "@into-the-void/entities"
      via: "EntityRegistry import"
      pattern: "import.*EntityRegistry.*from.*@into-the-void/entities"
    - from: "apps/game-server/src/zones/zones.service.ts"
      to: "entity_lifecycle table"
      via: "loadZone() query"
      pattern: "entityLifecycle"
---

<objective>
Create entity_lifecycle DB table and enrich createEntityFromSpawn() with EntityRegistry data

Purpose: Entities must carry full typed data (health, speciesId, behavior) from the registry, and their death/respawn state must persist across zone eviction and server restarts via DB records.

Output: entity_lifecycle schema, enriched entity creation, lifecycle-aware zone loading
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-entity-lifecycle-persistence-and-enriched-spawning/34-RESEARCH.md
@packages/database/src/schema/discoveries.ts
@apps/game-server/src/zones/zones.service.ts
@apps/game-server/src/zones/zones.module.ts
@packages/entities/src/index.ts
@packages/entities/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create entity_lifecycle DB table and enrich createEntityFromSpawn</name>
  <files>
    packages/database/src/schema/entity-lifecycle.ts
    packages/database/src/schema/index.ts
    apps/game-server/src/zones/zones.module.ts
    apps/game-server/src/zones/zones.service.ts
    apps/game-server/package.json
  </files>
  <action>
    1. Create packages/database/src/schema/entity-lifecycle.ts:
       - Use entityId (varchar 200) as PRIMARY KEY (not composite) — the entity ID is globally unique (format: zoneId_spawnId_x_y)
       - Add zoneId varchar(50) for query filtering
       - Add killedAt and respawnAt timestamps (withTimezone: true)
       - Export EntityLifecycle and NewEntityLifecycle types
       - Pattern: Follow discoveries.ts for pgTable structure

    2. Export from packages/database/src/schema/index.ts:
       - Add `export * from './entity-lifecycle';`

    3. Add @into-the-void/entities dependency to apps/game-server/package.json:
       - Add `"@into-the-void/entities": "workspace:*"` to dependencies

    4. Update apps/game-server/src/zones/zones.module.ts:
       - Import DatabaseModule from '../database/database.module'
       - Add DatabaseModule to imports array

    5. Rewrite apps/game-server/src/zones/zones.service.ts:
       a. Add imports at top:
          - `import { EntityRegistry } from '@into-the-void/entities';`
          - `import type { CreatureDefinition, PlantDefinition, MineralDefinition, ArtifactDefinition } from '@into-the-void/entities';`
          - `import { DatabaseService } from '../database/database.service';`
          - `import { entityLifecycle } from '@into-the-void/database';`
          - `import { eq } from 'drizzle-orm';`

       b. Inject DatabaseService in constructor:
          ```typescript
          constructor(
            private readonly configService: ConfigService,
            private readonly databaseService: DatabaseService,
          ) { ... }
          ```

       c. Make loadZone() async:
          - Change signature to `private async loadZone(zoneId: string): Promise<ZoneState>`
          - Query entity_lifecycle for this zone: `await db.select().from(entityLifecycle).where(eq(entityLifecycle.zoneId, zoneId))`
          - Build suppressed Set of entityIds where respawnAt > now
          - Skip entity creation for suppressed spawn points
          - Update all callers (getChunk, getZoneEntities, spawnEntity) to await loadZone

       d. Make onModuleInit() async and await loadZone('z_0_0')

       e. Rewrite createEntityFromSpawn(spawn, zoneId):
          - Get definition: `const def = EntityRegistry.get(spawn.spawnId);`
          - For creatures (spawn.entityType === 'creature' && def.entityClass === 'creature'):
            * Cast to CreatureDefinition
            * Generate level deterministically from seed (worldSeed + entityId)
            * Return Creature with speciesId, health: baseHealth, maxHealth: baseHealth, level, behavior
          - For minerals (def.entityClass === 'mineral'):
            * Cast to MineralDefinition
            * Return Mineral with resourceId, yield: 5, maxYield: 5, requiredTier
          - For plants (def.entityClass === 'plant'):
            * Cast to PlantDefinition
            * Return Plant with speciesId, yield: 5, maxYield: 5
          - For artifacts (def.entityClass === 'artifact'):
            * Cast to ArtifactDefinition
            * Return Artifact with artifactId, rarity
          - Default: return base Entity shape (fallback for unknown)

       f. Add recordEntityKill(entityId, zoneId, respawnSeconds) method:
          - Calculate respawnAt = now + respawnSeconds * 1000
          - For artifacts, use FAR_FUTURE date (2100-01-01) since they never respawn
          - Use onConflictDoUpdate for upsert on entityId

       g. Add getEntitiesAtPosition(zoneId, x, y) method for later use by entity blocking:
          - Get zone state, filter entities by position
          - Return Entity[]

    6. Run `pnpm db:generate` to generate migration for entity_lifecycle table

    7. Run `pnpm db:migrate` to apply migration
  </action>
  <verify>
    - pnpm build succeeds
    - pnpm db:generate creates migration file
    - pnpm db:migrate applies without error
    - Entity types (Creature, Mineral, Plant, Artifact) are now populated with registry data
  </verify>
  <done>
    - entity_lifecycle table exists in database schema
    - EntityRegistry is imported and used in ZonesService
    - loadZone() is async and queries lifecycle records
    - createEntityFromSpawn() returns fully typed entities with health, speciesId, behavior
    - recordEntityKill() method exists for Phase 35 to use
  </done>
</task>

</tasks>

<verification>
1. Build check: `pnpm build` passes without errors
2. Database check: `pnpm db:generate && pnpm db:migrate` completes successfully
3. Import check: EntityRegistry is imported from @into-the-void/entities in zones.service.ts
4. Type check: Creature entities have health, maxHealth, speciesId, behavior fields
</verification>

<success_criteria>
- entity_lifecycle table created and migrated
- ZonesService.loadZone() is async and queries lifecycle records
- createEntityFromSpawn() uses EntityRegistry.get() to populate entity fields
- Creatures spawn with correct maxHealth from definition, not hardcoded values
- recordEntityKill() method ready for Phase 35 integration
</success_criteria>

<output>
After completion, create `.planning/phases/34-entity-lifecycle-persistence-and-enriched-spawning/34-01-SUMMARY.md`
</output>
