---
phase: 109-faction-identity-design-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/items/src/utils.ts
  - packages/items/src/definitions/suits.ts
  - packages/items/src/definitions/modules.ts
  - packages/items/src/definitions/tools.ts
autonomous: true
requirements:
  - SUIT-01

must_haves:
  truths:
    - "The scavenger archetype exists in ARCHETYPE_PROFILES with stat percentages summing to 100"
    - "generateSuitStats('scavenger', 'common', 1) returns a valid stat distribution"
    - "Item definition files reference the design document so future contributors have a single source of truth"
    - "All existing tests still pass after the changes"
  artifacts:
    - packages/items/src/utils.ts (modified -- scavenger archetype added)
    - packages/items/src/definitions/suits.ts (modified -- design reference comment)
    - packages/items/src/definitions/modules.ts (modified -- design reference comment)
    - packages/items/src/definitions/tools.ts (modified -- design reference comment)
  key_links:
    - "utils.ts ARCHETYPE_PROFILES includes scavenger alongside existing 7 archetypes"
    - "Definition files link to packages/items/FACTION-IDENTITY.md"
---

<objective>
Add the scavenger archetype to the codebase and reference the design document from item definition files.

Purpose: The scavenger archetype enables Unaffiliated faction gear in Phase 112. The design document references ensure discoverability for future contributors.
Output: Modified utils.ts with scavenger archetype, reference comments in 3 definition files.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@packages/items/src/utils.ts
@packages/items/src/definitions/suits.ts
@packages/items/src/definitions/modules.ts
@packages/items/src/definitions/tools.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/items/src/utils.ts:
```typescript
export const ARCHETYPE_PROFILES = {
  tank: { durability: 35, toughness: 30, resilience: 15, recovery: 10, vigor: 10 },
  scout: { haste: 30, perception: 25, vigor: 25, recovery: 10, durability: 10 },
  combat: { power: 30, haste: 20, toughness: 20, durability: 15, vigor: 15 },
  balanced: { durability: 15, toughness: 12, power: 10, haste: 10, vigor: 15, recovery: 8, perception: 15, resilience: 15 },
  hazmat: { resilience: 30, recovery: 25, durability: 25, vigor: 20 },
  assault: { power: 35, durability: 25, haste: 25, toughness: 15 },
  recon: { perception: 35, haste: 30, vigor: 25, recovery: 10 },
} as const;

export function generateSuitStats(
  archetype: keyof typeof ARCHETYPE_PROFILES,
  rarity: ItemRarity,
  tier: 1 | 2 | 3 | 4 | 5,
  baseBudget: number = 77
): Partial<Record<StatKey, number>>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scavenger archetype to ARCHETYPE_PROFILES</name>
  <files>packages/items/src/utils.ts</files>
  <action>
Add the scavenger archetype to the ARCHETYPE_PROFILES object in `packages/items/src/utils.ts`.

The scavenger archetype represents Unaffiliated faction identity -- survival instincts from scrounging, no combat specialization. Stats must sum to 100:
- vigor: 30 (primary -- stamina/endurance from constant scavenging)
- recovery: 25 (secondary -- self-reliance, healing without faction support)
- perception: 25 (tertiary -- scavenger's eye for finding useful materials)
- durability: 12 (minor -- improvised protection)
- resilience: 8 (minor -- environmental adaptability)

Add after the `recon` entry:

```typescript
scavenger: { vigor: 30, recovery: 25, perception: 25, durability: 12, resilience: 8 },
```

Also update the JSDoc comment for `generateSuitStats` to include `scavenger` in the archetype list:
Change: `@param archetype - Build identity (tank/scout/combat/balanced/hazmat/assault/recon)`
To: `@param archetype - Build identity (tank/scout/combat/balanced/hazmat/assault/recon/scavenger)`

And update the archetype classifications comment at the top of `packages/items/src/definitions/suits.ts` to include:
```
 * scavenger: Survival versatility (vigor/recovery/perception) — Unaffiliated
```
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx nx run items:test 2>&1 | tail -20</automated>
    Additionally verify manually:
    - ARCHETYPE_PROFILES.scavenger exists
    - Stat percentages sum to 100: 30 + 25 + 25 + 12 + 8 = 100
    - generateSuitStats('scavenger', 'common', 1) would work (archetype key is valid)
  </verify>
  <done>
    - ARCHETYPE_PROFILES in utils.ts contains scavenger entry with stats summing to 100
    - JSDoc for generateSuitStats lists scavenger
    - Existing tests pass (no regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add design document reference comments to definition files</name>
  <files>
    packages/items/src/definitions/suits.ts
    packages/items/src/definitions/modules.ts
    packages/items/src/definitions/tools.ts
  </files>
  <action>
Add a reference comment block at the TOP of each file (before existing imports) pointing to the faction identity design document. This satisfies Success Criterion 4: "The design artifact is referenced in the item definition files so future contributors have a single source of truth."

**For suits.ts**, add before the first import:
```typescript
/**
 * Faction Suit Design Reference
 * @see packages/items/FACTION-IDENTITY.md for:
 *   - Stat archetypes per faction (primary/secondary/off-archetype)
 *   - Ability assignment matrix (which abilities at which tier)
 *   - Naming conventions ({type}_{faction}_{name}_{rarity})
 *   - Color palette anchors per faction
 *   - Tier progression table
 */
```

**For modules.ts**, add before the first import:
```typescript
/**
 * Faction Module Design Reference
 * @see packages/items/FACTION-IDENTITY.md for:
 *   - Module type assignments per faction (life_support, armor, sensor, etc.)
 *   - Naming conventions ({type}_{faction}_{name}_{rarity})
 *   - Color palette anchors per faction
 */
```

**For tools.ts**, add before the first import:
```typescript
/**
 * Faction Tool Design Reference
 * @see packages/items/FACTION-IDENTITY.md for:
 *   - Tool type assignments per faction (bio, mining, research, etc.)
 *   - Tool ability grant patterns per faction
 *   - Naming conventions ({type}_{faction}_{name}_{rarity})
 *   - Color palette anchors per faction
 */
```

Do NOT modify any executable code in these files. Only add documentation comments.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && grep -l "FACTION-IDENTITY.md" packages/items/src/definitions/suits.ts packages/items/src/definitions/modules.ts packages/items/src/definitions/tools.ts | wc -l | grep -q "3" && echo "PASS: All 3 files reference design doc" || echo "FAIL"</automated>
    Verify all 3 definition files contain a @see reference to FACTION-IDENTITY.md.
  </verify>
  <done>
    - suits.ts contains @see reference to FACTION-IDENTITY.md
    - modules.ts contains @see reference to FACTION-IDENTITY.md
    - tools.ts contains @see reference to FACTION-IDENTITY.md
    - No executable code was changed (only comments added)
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `npx nx run items:test` passes (no regression from archetype addition or comment changes)
- `grep "scavenger" packages/items/src/utils.ts` shows the new archetype
- `grep "FACTION-IDENTITY.md" packages/items/src/definitions/suits.ts packages/items/src/definitions/modules.ts packages/items/src/definitions/tools.ts` shows references in all 3 files
- Stat percentages for scavenger sum to 100 (vigor 30 + recovery 25 + perception 25 + durability 12 + resilience 8)
</verification>

<success_criteria>
- Scavenger archetype is functional in generateSuitStats (can be called like any other archetype)
- All 3 item definition files clearly reference the design document
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/109-faction-identity-design-gate/109-02-SUMMARY.md`
</output>
