---
phase: 71-quest-objective-tracker-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/ui/hud/QuestTracker.tsx
  - apps/web/src/ui/hud/QuestTracker.css
autonomous: true

must_haves:
  truths:
    - "Player sees up to 3 tracked quests in HUD with live progress counters"
    - "Primary quest (first tracked) has visual emphasis with accent border"
    - "Player can collapse/expand tracker via header toggle"
    - "Collapsed state persists across page refresh"
    - "Tracker positioned at top: 110px to avoid overlap with status indicators"
  artifacts:
    - path: "apps/web/src/ui/hud/QuestTracker.tsx"
      provides: "Enhanced tracker with collapse, 3-quest limit, primary styling"
      contains: "isCollapsed"
    - path: "apps/web/src/ui/hud/QuestTracker.css"
      provides: "Repositioned styles with collapse animation and primary quest emphasis"
      contains: "tracked-quest--primary"
  key_links:
    - from: "apps/web/src/ui/hud/QuestTracker.tsx"
      to: "localStorage"
      via: "collapse state persistence"
      pattern: "localStorage\\.getItem.*quest-tracker-collapsed"
    - from: "apps/web/src/ui/hud/QuestTracker.tsx"
      to: "questStore"
      via: "Zustand selector for activeQuests and trackedQuests"
      pattern: "useQuestStore"
---

<objective>
Enhance QuestTracker HUD with collapse/expand, 3-quest limit, and visual hierarchy

Purpose: Improve quest tracking UX with space-saving collapse, clear primary quest focus, and proper positioning that doesn't overlap other HUD elements

Output: Enhanced QuestTracker.tsx and QuestTracker.css with all Phase 71 requirements
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-quest-objective-tracker-hud/71-RESEARCH.md

@apps/web/src/ui/hud/QuestTracker.tsx
@apps/web/src/ui/hud/QuestTracker.css
@apps/web/src/store/questStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collapse state and 3-quest limit to QuestTracker.tsx</name>
  <files>apps/web/src/ui/hud/QuestTracker.tsx</files>
  <action>
Enhance QuestTracker component with:

1. **Collapse state with localStorage persistence:**
   - Add `useState` for `isCollapsed`, initialized from `localStorage.getItem('quest-tracker-collapsed') === 'true'`
   - Create `toggleCollapse` function that updates state and persists to localStorage

2. **Header with toggle:**
   - Add wrapper div with class `quest-tracker-header`
   - Include title "Quests" and toggle indicator (use unicode arrows: collapsed = ">" expanded = "v")
   - Header onClick triggers toggleCollapse
   - Always render header (even when collapsed)

3. **3-quest limit with primary quest determination:**
   - Limit displayed quests: `tracked.slice(0, 3)`
   - Determine primary quest: first quest in the sliced array (`displayQuests[0]?.questId`)
   - Apply conditional class: `tracked-quest--primary` for first quest, `tracked-quest--secondary` for others

4. **Overflow indicator:**
   - If `tracked.length > 3`, show text "+{n} more" below quest list

5. **Conditional content rendering:**
   - Only render quest content div when `!isCollapsed`

Structure:
```tsx
<div className="quest-tracker">
  <div className="quest-tracker-header" onClick={toggleCollapse}>
    <span className="quest-tracker-title">Quests ({tracked.length})</span>
    <span className="quest-tracker-toggle">{isCollapsed ? '>' : 'v'}</span>
  </div>
  {!isCollapsed && (
    <div className="quest-tracker-content">
      {displayQuests.map((quest, index) => (
        <div className={`tracked-quest ${index === 0 ? 'tracked-quest--primary' : 'tracked-quest--secondary'}`}>
          ...existing content...
        </div>
      ))}
      {tracked.length > 3 && <div className="quest-tracker-overflow">+{tracked.length - 3} more</div>}
    </div>
  )}
</div>
```

Keep existing functionality: onClick on quest opens quest log, objective display with progress counters.
  </action>
  <verify>TypeScript compiles: `npx nx run web:build --skip-nx-cache 2>&1 | head -50`</verify>
  <done>QuestTracker.tsx has collapse state, 3-quest limit, and primary/secondary class assignments</done>
</task>

<task type="auto">
  <name>Task 2: Update QuestTracker.css with repositioning and new styles</name>
  <files>apps/web/src/ui/hud/QuestTracker.css</files>
  <action>
Update styles for enhanced tracker:

1. **Reposition to avoid overlap:**
   - Change `.quest-tracker` from `top: 60px` to `top: 110px`

2. **Header styles:**
   ```css
   .quest-tracker-header {
     display: flex;
     justify-content: space-between;
     align-items: center;
     padding: 6px 10px;
     background: var(--color-bg-tertiary, #1e1e2e);
     border: 1px solid var(--color-border, #333);
     border-radius: 4px;
     cursor: pointer;
     margin-bottom: 4px;
   }

   .quest-tracker-header:hover {
     border-color: var(--color-accent, #7b68ee);
   }

   .quest-tracker-title {
     font-size: 12px;
     font-weight: 600;
     color: var(--color-text-primary, #fff);
   }

   .quest-tracker-toggle {
     font-size: 10px;
     color: var(--color-text-secondary, #888);
     font-family: monospace;
   }
   ```

3. **Content wrapper:**
   ```css
   .quest-tracker-content {
     display: flex;
     flex-direction: column;
     gap: 8px;
   }
   ```

4. **Primary quest emphasis:**
   ```css
   .tracked-quest--primary {
     border-left: 3px solid var(--color-accent, #7b68ee);
     background: rgba(123, 104, 238, 0.08);
   }
   ```

5. **Secondary quest styling:**
   ```css
   .tracked-quest--secondary {
     opacity: 0.85;
   }

   .tracked-quest--secondary .tracked-quest-name {
     font-size: 12px;
   }
   ```

6. **Overflow indicator:**
   ```css
   .quest-tracker-overflow {
     font-size: 10px;
     color: var(--color-text-secondary, #888);
     text-align: center;
     padding: 4px;
     font-style: italic;
   }
   ```

7. **Update .quest-tracker to remove gap (now handled by content wrapper):**
   - Remove `gap: 8px` from `.quest-tracker`

Keep all existing styles for `.tracked-quest`, `.tracked-quest-name`, `.tracked-quest-objectives`, `.tracked-objective`, etc.
  </action>
  <verify>Build succeeds: `npx nx run web:build --skip-nx-cache 2>&1 | tail -10`</verify>
  <done>QuestTracker.css repositioned to top: 110px with header, primary/secondary quest, and collapse styles</done>
</task>

</tasks>

<verification>
1. Build check: `npx nx run web:build --skip-nx-cache`
2. Lint check: `npx nx run web:lint`
3. Confirm CSS contains new classes:
   - `grep "quest-tracker-header" apps/web/src/ui/hud/QuestTracker.css`
   - `grep "tracked-quest--primary" apps/web/src/ui/hud/QuestTracker.css`
   - `grep "top: 110px" apps/web/src/ui/hud/QuestTracker.css`
4. Confirm TSX uses localStorage:
   - `grep "quest-tracker-collapsed" apps/web/src/ui/hud/QuestTracker.tsx`
</verification>

<success_criteria>
- QuestTracker renders at top: 110px (no overlap with status indicators)
- Clicking header toggles collapse/expand
- Collapsed state persists in localStorage across page refresh
- Maximum 3 quests displayed with "+N more" indicator if overflow
- First quest has accent border (primary visual hierarchy)
- Build and lint pass with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/71-quest-objective-tracker-hud/71-01-SUMMARY.md`
</output>
