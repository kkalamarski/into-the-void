---
phase: 32-client-display
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - apps/web/src/ui/panels/StatsPanel.tsx
  - apps/web/src/ui/panels/StatsPanel.css
  - apps/web/src/ui/GameUI.tsx
  - apps/web/src/components/LevelUpNotification.tsx
  - apps/web/src/components/LevelUpNotification.css
autonomous: true

must_haves:
  truths:
    - "Player can open stats panel by pressing P key"
    - "Stats panel shows all 8 stats with current values"
    - "Each stat row shows base + equipment breakdown"
    - "Level-up notification appears when base stats increase"
    - "Notification auto-dismisses after 3 seconds"
  artifacts:
    - path: "apps/web/src/ui/panels/StatsPanel.tsx"
      provides: "Draggable stats panel with 8 stat rows"
      exports: ["StatsPanel"]
      min_lines: 50
    - path: "apps/web/src/ui/panels/StatsPanel.css"
      provides: "Stats panel styling"
      contains: ".stats-panel"
    - path: "apps/web/src/components/LevelUpNotification.tsx"
      provides: "Auto-dismiss level-up overlay"
      exports: ["LevelUpNotification"]
    - path: "apps/web/src/components/LevelUpNotification.css"
      provides: "Notification overlay styling with fade animation"
      contains: "levelup-fade"
    - path: "apps/web/src/ui/GameUI.tsx"
      provides: "Conditional rendering for StatsPanel and LevelUpNotification"
      contains: "showStats"
  key_links:
    - from: "apps/web/src/ui/panels/StatsPanel.tsx"
      to: "apps/web/src/store/statsStore.ts"
      via: "useStatsStore hook"
      pattern: "useStatsStore"
    - from: "apps/web/src/ui/panels/StatsPanel.tsx"
      to: "apps/web/src/store/gameStore.ts"
      via: "toggleStats for close button"
      pattern: "toggleStats"
    - from: "apps/web/src/components/LevelUpNotification.tsx"
      to: "apps/web/src/store/statsStore.ts"
      via: "levelUpDeltas and clearLevelUpDeltas"
      pattern: "clearLevelUpDeltas"
---

<objective>
Build StatsPanel UI and LevelUpNotification overlay

Purpose: Players can view their 8 character stats with breakdown and receive level-up notifications (STAT-15, STAT-16, STAT-17)
Output: StatsPanel.tsx with draggable panel showing all 8 stats and base+equipment breakdown, LevelUpNotification.tsx with 3-second auto-dismiss
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-client-display/32-RESEARCH.md
@.planning/phases/32-client-display/32-01-SUMMARY.md
@apps/web/src/ui/panels/EquipmentPanel.tsx
@apps/web/src/components/ReconnectOverlay.tsx
@apps/web/src/store/statsStore.ts
@apps/web/src/store/gameStore.ts
@apps/web/src/ui/GameUI.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatsPanel component with draggable header and stat breakdown</name>
  <files>apps/web/src/ui/panels/StatsPanel.tsx, apps/web/src/ui/panels/StatsPanel.css</files>
  <action>
1. Create StatsPanel.tsx following EquipmentPanel.tsx pattern exactly:

```tsx
import React, { useEffect } from 'react';
import { useStatsStore } from '../../store/statsStore';
import { useGameStore } from '../../store/gameStore';
import { useDraggablePanel } from '../../hooks/useDraggablePanel';
import { STAT_DISPLAY_ORDER } from '../constants';
import {
  GiHearts,
  GiShield,
  GiSwordWound,
  GiSpeedometer,
  GiBattery100,
  GiHealing,
  GiRadarSweep,
  GiMagicShield,
} from 'react-icons/gi';
import type { IconType } from 'react-icons';
import './StatsPanel.css';

const STAT_ICONS: Record<string, IconType> = {
  durability: GiHearts,
  toughness: GiShield,
  power: GiSwordWound,
  haste: GiSpeedometer,
  vigor: GiBattery100,
  recovery: GiHealing,
  perception: GiRadarSweep,
  resilience: GiMagicShield,
};

interface StatRowProps {
  statKey: string;
  label: string;
  base: number;
  equipment: number;
  total: number;
}

function StatRow({ statKey, label, base, equipment, total }: StatRowProps) {
  const Icon = STAT_ICONS[statKey] ?? GiHearts;
  return (
    <div className="stats-panel-row">
      <Icon className="stats-panel-icon" />
      <span className="stats-panel-label">{label}</span>
      <span className="stats-panel-total">{total}</span>
      <span className="stats-panel-breakdown">
        {equipment !== 0 ? `(${base} + ${equipment})` : `(${base})`}
      </span>
    </div>
  );
}

export const StatsPanel: React.FC = () => {
  const { stats } = useStatsStore();
  const { toggleStats } = useGameStore();
  const { position, isDragging, handleMouseDown } = useDraggablePanel();

  // Disable Phaser keyboard when panel is open
  useEffect(() => {
    const game = useGameStore.getState().game;
    const worldScene = game?.getWorldScene();
    if (worldScene) worldScene.setKeyboardEnabled(false);
    return () => {
      const game = useGameStore.getState().game;
      const worldScene = game?.getWorldScene();
      if (worldScene) worldScene.setKeyboardEnabled(true);
    };
  }, []);

  if (!stats) return null;

  return (
    <div
      className="stats-panel ui-panel"
      style={{ transform: `translateY(-50%) translate(${position.x}px, ${position.y}px)` }}
    >
      <div
        className="stats-panel-header"
        onMouseDown={handleMouseDown}
        style={{ cursor: isDragging ? 'grabbing' : 'grab' }}
      >
        <span>Character Stats</span>
        <button className="close-btn" onClick={toggleStats}>&times;</button>
      </div>
      <div className="stats-panel-body">
        {STAT_DISPLAY_ORDER.map(({ key, label }) => (
          <StatRow
            key={key}
            statKey={key}
            label={label}
            base={stats.base[key]}
            equipment={stats.equipment[key]}
            total={stats.total[key]}
          />
        ))}
      </div>
    </div>
  );
};
```

2. Create StatsPanel.css with styling matching EquipmentPanel:

```css
.stats-panel {
  position: fixed;
  top: 50%;
  left: 50%;
  width: 280px;
  background: var(--color-bg-panel);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  pointer-events: auto;
  z-index: 200;
}

.stats-panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 12px;
  background: var(--color-bg-header);
  border-bottom: 1px solid var(--color-border);
  border-radius: 8px 8px 0 0;
  user-select: none;
}

.stats-panel-body {
  padding: 12px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.stats-panel-row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 4px 0;
}

.stats-panel-icon {
  width: 20px;
  height: 20px;
  color: var(--color-accent);
  flex-shrink: 0;
}

.stats-panel-label {
  flex: 1;
  font-size: 14px;
  color: var(--color-text);
}

.stats-panel-total {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text-bright);
  min-width: 32px;
  text-align: right;
}

.stats-panel-breakdown {
  font-size: 12px;
  color: var(--color-text-muted);
  min-width: 80px;
}
```
  </action>
  <verify>
Run `pnpm build` — web project compiles.
Verify StatsPanel.tsx exists and exports StatsPanel.
Verify StatsPanel.css exists with .stats-panel class.
  </verify>
  <done>StatsPanel component displays all 8 stats with base+equipment breakdown in a draggable panel following EquipmentPanel pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create LevelUpNotification component with auto-dismiss</name>
  <files>apps/web/src/components/LevelUpNotification.tsx, apps/web/src/components/LevelUpNotification.css</files>
  <action>
1. Create LevelUpNotification.tsx following ReconnectOverlay pattern:

```tsx
import React, { useEffect } from 'react';
import { useStatsStore } from '../store/statsStore';
import { STAT_DISPLAY_ORDER } from '../ui/constants';
import './LevelUpNotification.css';

export const LevelUpNotification: React.FC = () => {
  const { levelUpDeltas, clearLevelUpDeltas } = useStatsStore();

  useEffect(() => {
    if (!levelUpDeltas) return;
    const timer = setTimeout(() => clearLevelUpDeltas(), 3000);
    return () => clearTimeout(timer);
  }, [levelUpDeltas, clearLevelUpDeltas]);

  if (!levelUpDeltas) return null;

  return (
    <div className="levelup-overlay">
      <div className="levelup-notification">
        <div className="levelup-title">Level Up!</div>
        <div className="levelup-deltas">
          {STAT_DISPLAY_ORDER
            .filter(({ key }) => levelUpDeltas[key] !== undefined)
            .map(({ key, label }) => (
              <span key={key} className="levelup-delta">
                +{levelUpDeltas[key]} {label}
              </span>
            ))}
        </div>
      </div>
    </div>
  );
};
```

2. Create LevelUpNotification.css with fade animation:

```css
.levelup-overlay {
  position: fixed;
  top: 20%;
  left: 50%;
  transform: translateX(-50%);
  z-index: 500;
  pointer-events: none;
  animation: levelup-fade 3s ease-in-out forwards;
}

.levelup-notification {
  background: linear-gradient(135deg, var(--color-bg-panel) 0%, rgba(0, 0, 0, 0.9) 100%);
  border: 2px solid var(--color-accent);
  border-radius: 12px;
  padding: 16px 24px;
  text-align: center;
  box-shadow: 0 0 20px rgba(var(--color-accent-rgb), 0.4);
}

.levelup-title {
  font-size: 24px;
  font-weight: bold;
  color: var(--color-accent);
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 2px;
}

.levelup-deltas {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.levelup-delta {
  font-size: 16px;
  color: #4aff4a;
  font-weight: 500;
}

@keyframes levelup-fade {
  0% {
    opacity: 0;
    transform: translateX(-50%) translateY(-10px);
  }
  15% {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }
  75% {
    opacity: 1;
  }
  100% {
    opacity: 0;
  }
}
```

Note: The component manages its own visibility via levelUpDeltas state. It renders when levelUpDeltas is non-null and auto-clears after 3 seconds.
  </action>
  <verify>
Run `pnpm build` — web project compiles.
Verify LevelUpNotification.tsx exists and exports LevelUpNotification.
Verify LevelUpNotification.css exists with @keyframes levelup-fade.
  </verify>
  <done>LevelUpNotification component displays stat deltas and auto-dismisses after 3 seconds with fade animation</done>
</task>

<task type="auto">
  <name>Task 3: Wire StatsPanel and LevelUpNotification into GameUI</name>
  <files>apps/web/src/ui/GameUI.tsx</files>
  <action>
1. Import StatsPanel and LevelUpNotification:
```tsx
import { StatsPanel } from './panels/StatsPanel';
import { LevelUpNotification } from '../components/LevelUpNotification';
```

2. Add showStats to the useGameStore destructure (line 22):
```tsx
const { showChat, showInventory, showEquipment, showStats, player } = useGameStore();
```

3. Add conditional StatsPanel render after EquipmentPanel (around line 85):
```tsx
{showStats && <StatsPanel />}
```

4. Add LevelUpNotification unconditionally (it manages its own visibility) after the showStats conditional and before the minimap-border div:
```tsx
<LevelUpNotification />
```

The final panel section should look like:
```tsx
{showChat && <ChatPanel />}
{showInventory && <InventoryPanel />}
{showEquipment && <EquipmentPanel />}
{showStats && <StatsPanel />}
<LevelUpNotification />
<div className="minimap-border" />
```
  </action>
  <verify>
Run `pnpm build` — web project compiles.
Grep for "showStats" in GameUI.tsx — should find destructure and conditional.
Grep for "StatsPanel" in GameUI.tsx — should find import and usage.
Grep for "LevelUpNotification" in GameUI.tsx — should find import and usage.
  </verify>
  <done>GameUI conditionally renders StatsPanel when showStats is true and always mounts LevelUpNotification for auto-display of level-up deltas</done>
</task>

</tasks>

<verification>
```bash
# Full monorepo build
pnpm build

# Verify StatsPanel
grep -n "export const StatsPanel" apps/web/src/ui/panels/StatsPanel.tsx
grep -n ".stats-panel" apps/web/src/ui/panels/StatsPanel.css

# Verify LevelUpNotification
grep -n "export const LevelUpNotification" apps/web/src/components/LevelUpNotification.tsx
grep -n "levelup-fade" apps/web/src/components/LevelUpNotification.css

# Verify GameUI wiring
grep -n "showStats" apps/web/src/ui/GameUI.tsx
grep -n "StatsPanel" apps/web/src/ui/GameUI.tsx
grep -n "LevelUpNotification" apps/web/src/ui/GameUI.tsx
```
</verification>

<success_criteria>
- StatsPanel renders all 8 stats with correct lore names (Durability, Toughness, Power, Haste, Vigor, Recovery, Perception, Resilience)
- StatsPanel shows breakdown format: total value followed by "(base + equipment)" or "(base)" when equipment is 0
- StatsPanel is draggable via header and has close button calling toggleStats
- StatsPanel disables Phaser keyboard on mount and re-enables on unmount
- LevelUpNotification displays stat deltas when levelUpDeltas is non-null
- LevelUpNotification auto-clears after 3 seconds via setTimeout
- LevelUpNotification has fade-in/fade-out CSS animation
- GameUI renders StatsPanel when showStats is true
- GameUI always mounts LevelUpNotification (self-managing visibility)
- Full monorepo build passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-client-display/32-02-SUMMARY.md`
</output>
