---
phase: 148-proceduraltilegenerator-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/rendering/tile-strategies/types.ts
  - apps/web/src/game/rendering/tile-strategies/tile-palettes.ts
  - apps/web/src/game/rendering/tile-strategies/AbstractTileRenderStrategy.ts
  - apps/web/src/game/rendering/tile-strategies/index.ts
autonomous: true
requirements: [TILE-01, TILE-02, TILE-03]

must_haves:
  truths:
    - TileRenderStrategy interface defines drawTopAccents(), drawDecorationAccents(), drawSouthAccents(), drawEastAccents() methods matching ProceduralTileGenerator's current per-tile branching
    - AbstractTileRenderStrategy provides shared drawing primitives (topDiamondPoint, detailRandom, hashString) and default south/east accent implementations
    - tile-palettes.ts contains the full BIOME_PALETTES record and FLOOR_TILE_IDS set extracted verbatim from ProceduralTileGenerator
    - Registry in index.ts exports a Map<string, TileRenderStrategy> with getStrategyForTile() lookup and registerTileStrategy() registration
  artifacts:
    - apps/web/src/game/rendering/tile-strategies/types.ts
    - apps/web/src/game/rendering/tile-strategies/tile-palettes.ts
    - apps/web/src/game/rendering/tile-strategies/AbstractTileRenderStrategy.ts
    - apps/web/src/game/rendering/tile-strategies/index.ts
  key_links:
    - types.ts TileRenderStrategy interface is the contract all 6 strategy classes implement
    - tile-palettes.ts is consumed by all strategy classes for palette lookups
    - AbstractTileRenderStrategy provides topDiamondPoint(), detailRandom, and default south/east implementations reused by all 6 strategies
    - index.ts registry is consumed by ProceduralTileGenerator after refactor (Plan 02)
---

<objective>
Define the TileRenderStrategy interface, extract palette data and utility functions to shared files, create AbstractTileRenderStrategy base class with shared drawing logic, and create the strategy registry.

Purpose: Establish the tile strategy pattern foundation before implementing per-category strategies. All contracts, shared data, and drawing primitives are defined first so Plan 02 can implement against stable interfaces.
Output: 4 new files in apps/web/src/game/rendering/tile-strategies/
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/web/src/game/rendering/ProceduralTileGenerator.ts
@apps/web/src/game/rendering/strategies/types.ts
</context>

<interfaces>
<!-- Key types and geometry constants from ProceduralTileGenerator.ts -->
From ProceduralTileGenerator.ts — the TilePalette interface (line 6-17):
```typescript
export interface TilePalette {
  top: number;
  south: number;
  east: number;
  accent: number;
  accentAlt?: number;
}
```

From ProceduralTileGenerator.ts — geometry constants (lines 194-196):
```typescript
const HW = 128; // half width
const HH = 64;  // half height
const SH = 128; // side height
```

From ProceduralTileGenerator.ts — utility functions to extract (lines 22-63):
```typescript
function darkenColor(color: number, factor: number): number;
function brightenColor(color: number, amount: number): number;
function buildPalette(top: number, accent: number, accentAlt?: number): TilePalette;
function detailRandom(seed: number, index: number): number;
function hashString(s: string): number;
```

From ProceduralTileGenerator.ts — helper methods used by strategies (lines 1820-1841):
```typescript
private topDiamondPoint(seed: number, index: number): { x: number; y: number };
private isFloorTile(tileId: string): boolean;
private isHubTile(tileId: string): boolean;
```

From Phase 147 strategies/types.ts — pattern reference:
```typescript
// Phase 147 used RenderStrategy interface + AbstractRenderStrategy base class + registry Map
// Phase 148 follows identical pattern with TileRenderStrategy + AbstractTileRenderStrategy
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define TileRenderStrategy interface, types, and extract palette data</name>
  <files>
    apps/web/src/game/rendering/tile-strategies/types.ts
    apps/web/src/game/rendering/tile-strategies/tile-palettes.ts
  </files>
  <action>
Create the `apps/web/src/game/rendering/tile-strategies/` directory.

**types.ts:**
Create with:

1. Re-export the `TilePalette` interface (copy verbatim from ProceduralTileGenerator.ts lines 6-17):
```typescript
export interface TilePalette {
  top: number;
  south: number;
  east: number;
  accent: number;
  accentAlt?: number;
}
```

2. Export the `TileRenderStrategy` interface with these methods:
```typescript
import Phaser from 'phaser';
import type { TilePalette } from './types';

export interface TileRenderStrategy {
  /** Tile IDs this strategy handles. Used for registry auto-registration. */
  readonly handledTileIds: readonly string[];

  /** Draw accent details on the top diamond face */
  drawTopAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
  ): void;

  /** Draw decoration accents for floor tile variants 3-5 (decoIndex 0-2) */
  drawDecorationAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
    decoIndex: number,
  ): void;

  /** Draw accent details on the south (left, lit) face */
  drawSouthAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
  ): void;

  /** Draw accent details on the east (right, shadow) face */
  drawEastAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
  ): void;
}
```

3. Export a `TileCategory` type union:
```typescript
export type TileCategory = 'floor' | 'wall' | 'hazard' | 'water' | 'portal' | 'decorative';
```

**tile-palettes.ts:**
Extract from ProceduralTileGenerator.ts — copy VERBATIM with zero changes to values:

1. The `darkenColor()` function (lines 22-27)
2. The `brightenColor()` function (lines 30-34)
3. The `buildPalette()` function (lines 38-46)
4. The `detailRandom()` function (lines 49-54)
5. The `hashString()` function (lines 57-63)
6. The entire `BIOME_PALETTES` record (lines 72-174) — all entries exactly as-is
7. The `FLOOR_TILE_IDS` Set (lines 177-187) — all entries exactly as-is

Export all of them as named exports. Keep the JSDoc comments and section comments from the original. Also re-export `TilePalette` from types.ts for convenience.

Add a `isHubTile()` utility function (extracted from ProceduralTileGenerator line 1838-1841):
```typescript
export function isHubTile(tileId: string): boolean {
  return tileId.startsWith('canopy_') || tileId.startsWith('ironhold_') ||
         tileId.startsWith('meridian_') || tileId.startsWith('salvage_');
}
```

Add a `isFloorTile()` utility function:
```typescript
export function isFloorTile(tileId: string): boolean {
  return FLOOR_TILE_IDS.has(tileId);
}
```

IMPORTANT: Do NOT modify ProceduralTileGenerator.ts in this plan. Plan 02 handles the switchover.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit apps/web/src/game/rendering/tile-strategies/types.ts apps/web/src/game/rendering/tile-strategies/tile-palettes.ts 2>&1 | head -20</automated>
  </verify>
  <done>types.ts exports TileRenderStrategy interface, TilePalette interface, and TileCategory type; tile-palettes.ts exports all color helpers, BIOME_PALETTES record with all 50+ palette entries, FLOOR_TILE_IDS set, isHubTile() and isFloorTile() utilities — all values identical to ProceduralTileGenerator originals</done>
</task>

<task type="auto">
  <name>Task 2: Create AbstractTileRenderStrategy base class and registry</name>
  <files>
    apps/web/src/game/rendering/tile-strategies/AbstractTileRenderStrategy.ts
    apps/web/src/game/rendering/tile-strategies/index.ts
  </files>
  <action>
**AbstractTileRenderStrategy.ts:**
Create abstract base class implementing TileRenderStrategy with shared drawing primitives and default implementations:

```typescript
import Phaser from 'phaser';
import type { TileRenderStrategy, TilePalette } from './types';
import { darkenColor, isFloorTile, isHubTile, detailRandom } from './tile-palettes';

// ─── Isometric Cube Geometry (256x256 canvas) ───────────────────
export const HW = 128; // half width
export const HH = 64;  // half height
export const SH = 128; // side height

export abstract class AbstractTileRenderStrategy implements TileRenderStrategy {
  abstract readonly handledTileIds: readonly string[];

  abstract drawTopAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
  ): void;

  /** Default: generic decoration dots. Override per-category for specific patterns. */
  drawDecorationAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
    decoIndex: number,
  ): void {
    g.fillStyle(palette.accent, 0.3);
    for (let i = 0; i < 5; i++) {
      const p = this.topDiamondPoint(seed, i + 1400 + decoIndex * 20);
      g.fillCircle(p.x, p.y, 2 + detailRandom(seed, i + 1405) * 2);
    }
  }

  /** Default south face accents — floor tiles get horizontal strata, others get vertical texture */
  drawSouthAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
  ): void {
    const southAccent = darkenColor(palette.accent, 0.75);

    if (isFloorTile(tileId)) {
      g.lineStyle(1, southAccent, 0.2);
      for (let i = 0; i < 3; i++) {
        const y = HH + 12 + i * 36 + detailRandom(seed, i + 500) * 10;
        const x1 = 10 + detailRandom(seed, i + 505) * 20;
        const x2 = 90 + detailRandom(seed, i + 510) * 30;
        g.beginPath();
        g.moveTo(x1, y);
        g.lineTo(x2, y + (128 - 64) * (x2 - x1) / 128 * 0.5);
        g.strokePath();
      }
    } else {
      g.lineStyle(1, southAccent, 0.2);
      for (let i = 0; i < 3; i++) {
        const x = 15 + i * 35 + detailRandom(seed, i + 515) * 15;
        g.beginPath();
        g.moveTo(x, HH + 5);
        g.lineTo(x + 5, HH + SH - 5);
        g.strokePath();
      }
    }
  }

  /** Default east face accents — floor tiles get horizontal strata, others get vertical texture */
  drawEastAccents(
    g: Phaser.GameObjects.Graphics,
    tileId: string,
    palette: TilePalette,
    seed: number,
  ): void {
    const eastAccent = darkenColor(palette.accent, 0.5);

    if (isFloorTile(tileId)) {
      g.lineStyle(1, eastAccent, 0.15);
      for (let i = 0; i < 2; i++) {
        const y = HH + 15 + i * 50 + detailRandom(seed, i + 600) * 10;
        const x1 = 140 + detailRandom(seed, i + 605) * 20;
        const x2 = 230 + detailRandom(seed, i + 610) * 20;
        g.beginPath();
        g.moveTo(x1, y);
        g.lineTo(x2, y - (x2 - x1) * 0.3);
        g.strokePath();
      }
    } else {
      g.lineStyle(1, eastAccent, 0.15);
      for (let i = 0; i < 2; i++) {
        const x = 155 + i * 40 + detailRandom(seed, i + 615) * 15;
        g.beginPath();
        g.moveTo(x, HH + 5);
        g.lineTo(x - 3, HH + SH - 5);
        g.strokePath();
      }
    }
  }

  // ─── Shared Drawing Primitives ─────────────────────────────────

  /** Get a random point within the top diamond, biased inward by margin */
  protected topDiamondPoint(seed: number, index: number): { x: number; y: number } {
    const margin = 0.15;
    const u = margin + detailRandom(seed, index * 2) * (1 - 2 * margin);
    const v = margin + detailRandom(seed, index * 2 + 1) * (1 - 2 * margin);
    const x = HW + (u - v) * HW;
    const y = (u + v) * HH;
    return { x, y };
  }
}
```

The topDiamondPoint method is copied VERBATIM from ProceduralTileGenerator.ts (lines 1820-1830). The default drawSouthAccents and drawEastAccents implementations are copied VERBATIM from the non-hub floor/wall branches of ProceduralTileGenerator.ts (lines 1718-1739 and 1792-1814).

**index.ts:**
Create the tile strategy registry:

```typescript
import type { TileRenderStrategy } from './types';

export type { TileRenderStrategy } from './types';
export type { TilePalette, TileCategory } from './types';
export { AbstractTileRenderStrategy, HW, HH, SH } from './AbstractTileRenderStrategy';
export { BIOME_PALETTES, FLOOR_TILE_IDS, darkenColor, brightenColor, buildPalette, detailRandom, hashString, isHubTile, isFloorTile } from './tile-palettes';

const tileStrategyMap = new Map<string, TileRenderStrategy>();

/** Get the tile render strategy for a specific tileId. Returns undefined for unregistered tiles. */
export function getStrategyForTile(tileId: string): TileRenderStrategy | undefined {
  return tileStrategyMap.get(tileId);
}

/** Register a strategy for a specific tileId. */
export function registerTileStrategy(tileId: string, strategy: TileRenderStrategy): void {
  tileStrategyMap.set(tileId, strategy);
}

/** Register a strategy for all tile IDs it handles (reads handledTileIds). */
export function registerStrategy(strategy: TileRenderStrategy): void {
  for (const tileId of strategy.handledTileIds) {
    tileStrategyMap.set(tileId, strategy);
  }
}

/** Initialize all tile strategies. Call once before baking textures. */
export function initTileStrategies(): void {
  // Will be populated in Plan 02 when strategy classes are created
  // registerStrategy(new FloorTileStrategy());
  // registerStrategy(new WallTileStrategy());
  // registerStrategy(new HazardTileStrategy());
  // registerStrategy(new WaterTileStrategy());
  // registerStrategy(new PortalTileStrategy());
  // registerStrategy(new DecorativeTileStrategy());
}
```

IMPORTANT: Do NOT modify ProceduralTileGenerator.ts in this plan. Plan 02 handles the switchover.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit apps/web/src/game/rendering/tile-strategies/index.ts 2>&1 | head -20</automated>
  </verify>
  <done>AbstractTileRenderStrategy compiles and provides topDiamondPoint() shared primitive, default south/east accent implementations matching ProceduralTileGenerator originals; index.ts exports registry functions (getStrategyForTile, registerTileStrategy, registerStrategy, initTileStrategies) and re-exports all types and palette data</done>
</task>

</tasks>

<verification>
- All 4 files compile with `npx tsc --noEmit`
- tile-palettes.ts BIOME_PALETTES has identical entries to ProceduralTileGenerator.ts (diff the values)
- tile-palettes.ts FLOOR_TILE_IDS has identical entries to ProceduralTileGenerator.ts
- TileRenderStrategy interface covers all 4 accent-drawing methods (drawTopAccents, drawDecorationAccents, drawSouthAccents, drawEastAccents)
- AbstractTileRenderStrategy.topDiamondPoint() produces identical output to ProceduralTileGenerator.topDiamondPoint()
- Default south/east accents in AbstractTileRenderStrategy match the non-hub branches exactly
- No changes to ProceduralTileGenerator.ts (it still works as before)
</verification>

<success_criteria>
- tile-strategies/ directory exists with types.ts, tile-palettes.ts, AbstractTileRenderStrategy.ts, index.ts
- All files compile without errors
- TileRenderStrategy interface has methods for: drawTopAccents, drawDecorationAccents, drawSouthAccents, drawEastAccents
- tile-palettes.ts contains exact copies of all palette data, color helpers, and utility functions from ProceduralTileGenerator.ts
- AbstractTileRenderStrategy provides topDiamondPoint() and default south/east implementations
- ProceduralTileGenerator.ts is completely unchanged (zero-diff)
</success_criteria>

<output>
After completion, create `.planning/phases/148-proceduraltilegenerator-strategy/148-01-SUMMARY.md`
</output>
