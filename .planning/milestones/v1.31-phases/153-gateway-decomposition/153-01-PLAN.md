---
phase: 153-gateway-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game-server/src/game/handlers/ZoneHandler.ts
  - apps/game-server/src/game/handlers/InventoryHandler.ts
  - apps/game-server/src/game/handlers/CombatHandler.ts
  - apps/game-server/src/game/handlers/SocialHandler.ts
  - apps/game-server/src/game/handlers/AutomationHandler.ts
  - apps/game-server/src/game/handlers/index.ts
autonomous: true
requirements: [GW-01]

must_haves:
  truths:
    - Five @Injectable() handler classes created: ZoneHandler, InventoryHandler, CombatHandler, SocialHandler, AutomationHandler
    - Each handler receives required services via NestJS constructor injection (no service locator pattern)
    - Each handler receives Server instance via setServer() method (same pattern as existing services like CombatService, AiService)
    - Handler methods have identical signatures and return types to current gateway methods — they are direct lifts, not refactors
    - Private helper methods move to the handler that uses them most: resolveZoneBiome, updatePlayerRooms, emitNpcQuestMarkers → ZoneHandler; emitStats → InventoryHandler; checkRareNodeDiscovery → SocialHandler; canSendChat → SocialHandler
    - Shared helpers (used by multiple handlers) are exposed as public methods on the owning handler so the gateway or other handlers can call them
    - No business logic changes — every line of handler code is a direct copy from game.gateway.ts with only `this.` reference adjustments
    - @OnEvent('player.teleported') moves to ZoneHandler, @OnEvent('craft.masterwork') moves to AutomationHandler
  artifacts:
    - apps/game-server/src/game/handlers/ZoneHandler.ts
    - apps/game-server/src/game/handlers/InventoryHandler.ts
    - apps/game-server/src/game/handlers/CombatHandler.ts
    - apps/game-server/src/game/handlers/SocialHandler.ts
    - apps/game-server/src/game/handlers/AutomationHandler.ts
    - apps/game-server/src/game/handlers/index.ts
  key_links:
    - ZoneHandler owns zone:request, portal:use, hub:recall, hub:leave, expedition:start, @OnEvent player.teleported, updatePlayerRooms, resolveZoneBiome, emitNpcQuestMarkers
    - InventoryHandler owns inventory:pickup/drop/use/reorder, equipment:change, inventory:unequip, equipment:tool_swap, storage:open, entity:tool_use, emitStats
    - CombatHandler owns ability:use, cast:cancel, respawn:sos, respawn:reboot
    - SocialHandler owns chat:send, npc:interact, trade:buy, trade:sell, quest:complete/abandon/accept, poi:discover, gathering:start/complete, lore:collect, mastery:query, canSendChat, checkRareNodeDiscovery
    - AutomationHandler owns automation:deploy/interact/collect/refuel/dismantle/panel_request, crafting:start/collect/recipes, @OnEvent craft.masterwork
    - Plan 02 depends on these handlers to rewire game.gateway.ts as a thin router
---

<objective>
Create five domain-specific @Injectable() handler classes that contain all business logic currently in game.gateway.ts event handlers.

Purpose: Extract ~1600 lines of handler logic into domain-grouped classes. The gateway file is NOT modified in this plan — handlers are created alongside it. Plan 02 will rewire the gateway to delegate to these handlers.
Output: 5 handler files + barrel export in apps/game-server/src/game/handlers/
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-gateway-decomposition/153-CONTEXT.md
@apps/game-server/src/game/game.gateway.ts
@apps/game-server/src/game/game.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ZoneHandler</name>
  <files>apps/game-server/src/game/handlers/ZoneHandler.ts</files>
  <action>
Create `apps/game-server/src/game/handlers/ZoneHandler.ts` as an `@Injectable()` NestJS service.

**Constructor dependencies (inject via NestJS DI):**
- EventEmitter2
- GameService
- PlayerService
- ZonesService
- AiService
- QuestService
- HazardService

**Server reference:** Add `private server!: Server` property and `setServer(server: Server)` method.

**Public methods to extract from game.gateway.ts (direct copy with `this.` adjustments):**

1. `handleZoneRequest(client, data)` — lines 378-411 of gateway. Zone state loading + entity spawning.
2. `handlePortalUse(client)` — lines 811-894. Portal teleport to hub.
3. `handleHubRecall(client)` — lines 896-962. Recall to faction hub.
4. `handleHubLeave(client)` — lines 1000-1068. Leave hub to open world.
5. `handleExpeditionStart(client, data)` — lines 1503-1603. Start expedition by tier.
6. `handlePlayerTeleported(data)` — lines 964-998. @OnEvent handler (keep @OnEvent decorator on this method).

**Private helper methods to move here:**
7. `updatePlayerRooms(client, playerZoneId)` — lines 1688-1718. 3x3 room subscription management.
8. `resolveZoneBiome(zoneId)` — lines 1663-1681. Biome lookup for zone.
9. `emitNpcQuestMarkers(client, playerId, playerFaction, zoneEntities)` — lines 1949-1982. Quest marker emission.

**IMPORTANT:** These helpers are used by auth handler too (which stays in gateway). Make `updatePlayerRooms`, `resolveZoneBiome`, and `emitNpcQuestMarkers` PUBLIC so the gateway can call `this.zoneHandler.updatePlayerRooms(...)` etc.

**Cross-handler call:** `handlePortalUse` calls `handleHubLeave` internally (line 819) — keep this as `this.handleHubLeave(client)` since both are in the same handler now.

Copy all logic verbatim. The only changes should be:
- Adding `@Injectable()` decorator
- Constructor injection of services
- `this.server` instead of gateway's `this.server`
- Helper method calls use `this.` (they're in the same class now)
  </action>
</task>

<task type="auto">
  <name>Task 2: Create InventoryHandler</name>
  <files>apps/game-server/src/game/handlers/InventoryHandler.ts</files>
  <action>
Create `apps/game-server/src/game/handlers/InventoryHandler.ts` as an `@Injectable()` NestJS service.

**Constructor dependencies:**
- GameService
- PlayerService
- InventoryService
- StorageService
- EntityService
- HazardService

**Server reference:** `private server!: Server` + `setServer(server: Server)`.

**Public methods to extract:**

1. `handleInventoryPickup(client, data)` — lines 413-445
2. `handleInventoryDrop(client, data)` — lines 447-479
3. `handleInventoryUse(client, data)` — lines 481-509
4. `handleEquipmentChange(client, data)` — lines 511-540
5. `handleInventoryUnequip(client, data)` — lines 542-571
6. `handleToolSwap(client)` — lines 573-598
7. `handleInventoryReorder(client, data)` — lines 600-619
8. `handleStorageOpen(client)` — lines 621-630
9. `handleToolUse(client, payload)` — lines 632-667

**Public helper method:**
10. `emitStats(client, playerId)` — lines 1609-1657. Must be PUBLIC because the auth handler in gateway calls it, and equipment handlers call it internally.

Copy all logic verbatim. Equipment handlers (change, unequip, tool_swap) call `this.emitStats(client, player.id)` and `this.hazardService.onPlayerEquipmentChanged(player.id)` — these stay identical since both services are injected.
  </action>
</task>

<task type="auto">
  <name>Task 3: Create CombatHandler</name>
  <files>apps/game-server/src/game/handlers/CombatHandler.ts</files>
  <action>
Create `apps/game-server/src/game/handlers/CombatHandler.ts` as an `@Injectable()` NestJS service.

**Constructor dependencies:**
- PlayerService
- InventoryService
- AbilityService
- AiService

**Server reference:** `private server!: Server` + `setServer(server: Server)`.

**Public methods to extract:**

1. `handleAbilityUse(client, data)` — lines 669-703. Ability use with cast/cooldown handling.
2. `handleCastCancel(client)` — lines 705-712. Cast interruption.
3. `handleRespawnSOS(client)` — lines 714-745. S.O.S. respawn to hub.
4. `handleRespawnReboot(client, payload)` — lines 747-809. Reboot kit respawn.

**Cross-handler dependency:** `handleRespawnSOS` calls `this.updatePlayerRooms(client, ...)` and `this.aiService.activateZone(...)`. Since updatePlayerRooms lives in ZoneHandler, inject ZoneHandler and call `this.zoneHandler.updatePlayerRooms(...)`.

**IMPORTANT:** Use `forwardRef(() => ZoneHandler)` with `@Inject(forwardRef(...))` for the ZoneHandler injection to avoid circular dependency issues, since ZoneHandler might also need references resolved at init time.

Copy logic verbatim from gateway. The `handleAbilityUse` method is also called from SocialHandler's gathering:start — make it PUBLIC.
  </action>
</task>

<task type="auto">
  <name>Task 4: Create SocialHandler</name>
  <files>apps/game-server/src/game/handlers/SocialHandler.ts</files>
  <action>
Create `apps/game-server/src/game/handlers/SocialHandler.ts` as an `@Injectable()` NestJS service.

**Constructor dependencies:**
- PlayerService
- InventoryService
- ZonesService
- TradeService
- QuestService
- DiscoveryService
- GatheringService
- LoreService
- ZoneMasteryService
- ExpeditionService
- ChatService
- AbilityService
- GameService

**Server reference:** `private server!: Server` + `setServer(server: Server)`.

**Private state + methods to move:**
- `chatBurstWindow: Map<string, number[]>` and `CHAT_BURST_LIMIT`, `CHAT_BURST_WINDOW_MS` constants
- `canSendChat(playerId)` — lines 72-80
- `checkRareNodeDiscovery(characterId, playerX, playerY, zoneId, socket)` — lines 1724-1776

**Public methods to extract:**

1. `handleChat(client, data)` — lines 342-370
2. `handleNpcInteract(client, data)` — lines 1070-1167
3. `handleTradeBuy(client, data)` — lines 1169-1216
4. `handleTradeSell(client, data)` — lines 1218-1263
5. `handleQuestComplete(client, data)` — lines 1265-1299
6. `handleQuestAbandon(client, data)` — lines 1301-1325
7. `handleQuestAccept(client, data)` — lines 1327-1352
8. `handlePoiDiscover(client, data)` — lines 1354-1415
9. `handleGatheringStart(client, data)` — lines 1417-1464
10. `handleGatheringComplete(client, data)` — lines 1466-1474
11. `handleLoreCollect(client, data)` — lines 1476-1485
12. `handleMasteryQuery(client, data)` — lines 1487-1501

**Cross-handler call:** `handleGatheringStart` (line 1460) calls `this.handleAbilityUse(client, {...})`. Inject CombatHandler (use `forwardRef`) and call `this.combatHandler.handleAbilityUse(...)`.

Copy all logic verbatim.
  </action>
</task>

<task type="auto">
  <name>Task 5: Create AutomationHandler</name>
  <files>apps/game-server/src/game/handlers/AutomationHandler.ts</files>
  <action>
Create `apps/game-server/src/game/handlers/AutomationHandler.ts` as an `@Injectable()` NestJS service.

**Constructor dependencies:**
- PlayerService
- InventoryService
- AutomationService
- CraftingService

**Server reference:** `private server!: Server` + `setServer(server: Server)`.

**Public methods to extract:**

1. `handleAutomationDeploy(client, data)` — lines 1780-1813
2. `handleAutomationInteract(client, data)` — lines 1815-1833
3. `handleAutomationCollect(client, data)` — lines 1835-1862
4. `handleAutomationRefuel(client, data)` — lines 1864-1892
5. `handleAutomationDismantle(client, data)` — lines 1894-1926
6. `handleAutomationPanelRequest(client)` — lines 1928-1941
7. `handleCraftingStart(client, data)` — lines 1988-2014
8. `handleCraftingCollect(client)` — lines 2016-2047
9. `handleCraftingRecipes(client)` — lines 2049-2064
10. `handleMasterworkBroadcast(data)` — lines 2070-2091. Keep `@OnEvent('craft.masterwork')` decorator.

Copy all logic verbatim.
  </action>
</task>

<task type="auto">
  <name>Task 6: Create barrel export</name>
  <files>apps/game-server/src/game/handlers/index.ts</files>
  <action>
Create `apps/game-server/src/game/handlers/index.ts`:

```typescript
export { ZoneHandler } from './ZoneHandler';
export { InventoryHandler } from './InventoryHandler';
export { CombatHandler } from './CombatHandler';
export { SocialHandler } from './SocialHandler';
export { AutomationHandler } from './AutomationHandler';
```
  </action>
</task>

</tasks>

<verification>
1. All five handler files exist in apps/game-server/src/game/handlers/
2. Each handler is decorated with @Injectable()
3. Each handler has a setServer(server: Server) method
4. Method signatures match the original gateway methods exactly
5. @OnEvent decorators preserved on handlePlayerTeleported and handleMasterworkBroadcast
6. No import errors — run `npx nx run game-server:build` to verify TypeScript compilation
7. Handler files compile independently (they don't import from game.gateway.ts)
8. Cross-handler dependencies use forwardRef to avoid circular DI issues
</verification>
