---
phase: 152-worldscene-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/game/scenes/controllers/CameraController.ts
  - apps/web/src/game/scenes/controllers/InputController.ts
  - apps/web/src/game/scenes/controllers/index.ts
  - apps/web/src/game/scenes/WorldScene.ts
autonomous: true
requirements: [SCENE-01, SCENE-02]

must_haves:
  truths:
    - CameraController owns camera follow, zoom setup, minimap creation/follow/ignore, and weather emitter ignore updates
    - InputController owns WASD/arrow key setup, keyboard shortcut bindings (Q/I/E/K/P/H), mouse button tracking, context menu disabling, and resolveDirection()
    - InputController captures key state and passes to existing PixelMovementController (stays independent) — InputController does NOT own pixel movement logic
    - Both controllers receive the Phaser scene reference in constructor and access scene.events, scene.cameras, scene.input, scene.add
    - WorldScene.create() instantiates both controllers; WorldScene.update() calls their update methods
    - All existing keybindings work identically after extraction — no behavior change
    - Camera follow, zoom, and minimap behavior is identical after extraction
  artifacts:
    - apps/web/src/game/scenes/controllers/CameraController.ts
    - apps/web/src/game/scenes/controllers/InputController.ts
    - apps/web/src/game/scenes/controllers/index.ts
  key_links:
    - CameraController.create() called from WorldScene.create() after tileLayer/zoneHUD init
    - CameraController.startFollowPlayer(sprite) called when local player created
    - CameraController.updateMinimapWeatherIgnore(weatherSystem) called after biome transitions
    - InputController.create() called from WorldScene.create() to set up all key bindings
    - InputController.getMovementKeys() returns { W, A, S, D } booleans for handleInput to pass to PixelMovementController
    - Plan 02 depends on this plan for reduced WorldScene size
---

<objective>
Extract camera management and input handling from WorldScene.ts into CameraController and InputController classes.

Purpose: Reduce WorldScene by ~400 lines by moving self-contained camera and input subsystems into dedicated controllers. These are the two most independent subsystems — no entity or interaction dependencies.
Output: 3 new files in apps/web/src/game/scenes/controllers/, WorldScene.ts delegates to both controllers.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-worldscene-decomposition/152-CONTEXT.md
@apps/web/src/game/scenes/WorldScene.ts
@apps/web/src/game/rendering/MinimapCamera.ts
@apps/web/src/game/systems/PixelMovementController.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CameraController</name>
  <files>apps/web/src/game/scenes/controllers/CameraController.ts</files>
  <action>
Create `apps/web/src/game/scenes/controllers/CameraController.ts`.

This controller owns all camera-related logic currently in WorldScene:

1. **Properties to move from WorldScene:**
   - `minimapCamera: MinimapCamera | null`

2. **create() method** — called from WorldScene.create():
   - Set fixed zoom: `scene.cameras.main.setZoom(0.5)`
   - Create MinimapCamera: `this.minimapCamera = new MinimapCamera(scene); this.minimapCamera.create()`
   - Accept zoneHUD game objects to make minimap ignore them: `this.minimapCamera.ignore(zoneHUD.getGameObjects())`

3. **startFollowPlayer(sprite: Phaser.GameObjects.Sprite):**
   - `scene.cameras.main.startFollow(sprite, true, 1.0, 1.0)` (center-locked, no lerp)
   - `this.minimapCamera.startFollow(sprite)`

4. **updateMinimapWeatherIgnore(weatherSystem: WeatherSystem):**
   - Move the `updateMinimapWeatherIgnore()` private method from WorldScene (lines 1338-1345)
   - Gets active emitters from weatherSystem and passes to `this.minimapCamera.ignore()`

5. **ignore(gameObjects):** Proxy to `this.minimapCamera.ignore()` for any future ignore needs.

6. **getMinimapCamera():** Returns the MinimapCamera instance (needed by WorldScene for edge cases).

Constructor signature:
```typescript
constructor(private scene: Phaser.Scene) {}
```

Import MinimapCamera and WeatherSystem types. The controller is a plain TypeScript class, NOT a Phaser scene or plugin.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>CameraController.ts exists with create(), startFollowPlayer(), updateMinimapWeatherIgnore(), getMinimapCamera()</done>
</task>

<task type="auto">
  <name>Task 2: Create InputController and barrel export</name>
  <files>
    apps/web/src/game/scenes/controllers/InputController.ts
    apps/web/src/game/scenes/controllers/index.ts
  </files>
  <action>
**InputController.ts:**

Create `apps/web/src/game/scenes/controllers/InputController.ts`.

This controller owns all input setup and key state reading currently in WorldScene:

1. **Properties to move from WorldScene:**
   - `cursors: Phaser.Types.Input.Keyboard.CursorKeys | null`
   - `wasd: WASDKeys | null` (move the WASDKeys type definition here too)
   - `leftMouseDown: boolean`
   - `rightMouseDown: boolean`

2. **Move the `resolveDirection()` free function** (lines 61-80) into this file. Keep it as a module-level function or a static method.

3. **create() method** — called from WorldScene.create(). Move all input setup from WorldScene.create():
   - WASD key creation (lines 257-262)
   - All keyboard shortcut bindings: Q (tool swap), I (inventory), E (equipment), K (abilities), P (equipment alias), H (hub recall) — lines 265-304
   - Arrow key creation via `createCursorKeys()` (line 256)
   - Mouse button tracking (pointerdown/pointerup for leftMouseDown/rightMouseDown) — lines 392-408
   - Context menu disable — line 411

4. **IMPORTANT: Do NOT move these to InputController** (they stay in WorldScene for now — Plan 02 moves them to InteractionController):
   - `pointerup` ground-click handler (lines 375-389) — depends on targetHighlight and combatStore
   - `gameobjectdown` entity click handler (lines 414-474) — depends on entity types and combat
   - Tile info popup on both-buttons-pressed (line 398) — depends on tile data

   Instead, InputController should emit Phaser scene events that WorldScene/InteractionController can listen to:
   - On both-mouse-buttons pressed: `scene.events.emit('input:both-buttons', pointer)` — WorldScene listens and calls showTileInfo
   - Expose `isLeftMouseDown()` and `isRightMouseDown()` getters

5. **getMovementKeys() method:** Returns current movement key state as `{ W: boolean, A: boolean, S: boolean, D: boolean }`:
   ```typescript
   getMovementKeys(): { W: boolean; A: boolean; S: boolean; D: boolean } {
     const keys = {
       W: this.wasd?.W.isDown ?? false,
       A: this.wasd?.A.isDown ?? false,
       S: this.wasd?.S.isDown ?? false,
       D: this.wasd?.D.isDown ?? false,
     };
     if (this.cursors) {
       if (this.cursors.up.isDown) keys.W = true;
       if (this.cursors.down.isDown) keys.S = true;
       if (this.cursors.left.isDown) keys.A = true;
       if (this.cursors.right.isDown) keys.D = true;
     }
     return keys;
   }
   ```
   This is called from WorldScene.handleInput() instead of reading keys directly.

6. **isKeyboardEnabled() method:** Returns `this.scene.input.keyboard?.enabled ?? false`. Used by WorldScene for guards.

Constructor signature:
```typescript
constructor(private scene: Phaser.Scene) {}
```

Import gameSocket and useGameStore for the shortcut bindings.

**index.ts:**

Create barrel export:
```typescript
export { CameraController } from './CameraController';
export { InputController } from './InputController';
```
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>InputController.ts exists with create(), getMovementKeys(), resolveDirection; index.ts barrel exports both controllers</done>
</task>

<task type="auto">
  <name>Task 3: Wire controllers into WorldScene, remove migrated code</name>
  <files>apps/web/src/game/scenes/WorldScene.ts</files>
  <action>
Update WorldScene.ts to delegate to CameraController and InputController:

1. **Add imports:**
   ```typescript
   import { CameraController, InputController } from './controllers';
   ```

2. **Add controller properties:**
   ```typescript
   private cameraController: CameraController | null = null;
   private inputController: InputController | null = null;
   ```

3. **Remove migrated properties from WorldScene:**
   - Remove `minimapCamera` property (now in CameraController)
   - Remove `cursors` property (now in InputController)
   - Remove `wasd` property (now in InputController)
   - Remove `leftMouseDown` and `rightMouseDown` properties (now in InputController)
   - Remove `WASDKeys` type definition (moved to InputController)
   - Remove `resolveDirection()` free function (moved to InputController)

4. **Update WorldScene.create():**
   - Replace minimap creation block (lines 207-213) with:
     ```typescript
     this.cameraController = new CameraController(this);
     this.cameraController.create(this.zoneHUD);
     ```
   - Replace camera zoom setup (line 359) — already handled by CameraController.create()
   - Replace all input setup (lines 255-411) with:
     ```typescript
     this.inputController = new InputController(this);
     this.inputController.create();
     ```
   - Keep the `pointerup` ground-click handler (lines 375-389) in WorldScene — wire it to listen for scene events or keep inline
   - Keep the `gameobjectdown` entity click handler (lines 414-474) in WorldScene
   - Wire `input:both-buttons` event from InputController to `this.showTileInfo(pointer)`
   - Keep audioManager.startMusic() call in WorldScene.create()

5. **Update WorldScene.updateLocalPlayer():**
   - Replace `this.cameras.main.startFollow(...)` and minimap follow with:
     ```typescript
     this.cameraController?.startFollowPlayer(this.localPlayer!);
     ```

6. **Update WorldScene.handleInput():**
   - Replace direct key reading (lines 868-881) with:
     ```typescript
     const keys = this.inputController!.getMovementKeys();
     ```
   - Remove the arrow key merging block (already handled inside InputController.getMovementKeys)

7. **Update WorldScene.commitZoneTransition():**
   - Replace `this.updateMinimapWeatherIgnore()` call with:
     ```typescript
     if (this.cameraController && this.weatherSystem) {
       this.cameraController.updateMinimapWeatherIgnore(this.weatherSystem);
     }
     ```
   - Do the same in `renderChunk()` where `updateMinimapWeatherIgnore` is called

8. **Remove the `updateMinimapWeatherIgnore()` private method** from WorldScene (now in CameraController).

9. **Where WorldScene still references `this.minimapCamera` directly** (e.g., in zoneHUD ignore calls), replace with `this.cameraController?.getMinimapCamera()` or move that logic into CameraController.

10. **Verify:** The `pointerup` and `gameobjectdown` handlers that reference `this.lastClickedEntity`, `this.targetHighlight`, `useCombatStore`, etc. remain in WorldScene. They will be moved to InteractionController in Plan 02.

CRITICAL: This is a refactoring-only change. Every keybinding, camera behavior, and minimap feature must work identically. No new features, no behavior changes.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>WorldScene delegates to CameraController and InputController; removed ~200 lines of migrated code; all keybindings and camera behavior preserved</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project apps/web/tsconfig.json` compiles without errors
- CameraController.ts exists in apps/web/src/game/scenes/controllers/
- InputController.ts exists in apps/web/src/game/scenes/controllers/
- index.ts barrel exports both controllers
- WorldScene no longer contains: resolveDirection(), WASDKeys type, minimapCamera property, cursors/wasd properties, leftMouseDown/rightMouseDown properties, updateMinimapWeatherIgnore() method
- WorldScene.create() instantiates CameraController and InputController
- WorldScene.handleInput() reads keys via inputController.getMovementKeys()
- Camera follow setup uses cameraController.startFollowPlayer()
- All keyboard shortcuts (Q/I/E/K/P/H/WASD/arrows) still functional
- Minimap still follows player and ignores weather emitters
</verification>

<success_criteria>
- controllers/ directory exists with CameraController.ts, InputController.ts, index.ts
- All files compile without errors
- SCENE-01: Input handling extracted to InputController — WASD, mouse, keyboard shortcuts
- SCENE-02: Camera management extracted to CameraController — follow, zoom, minimap
- WorldScene reduced by ~200-300 lines from this extraction
- Zero behavior change — all features work identically
</success_criteria>

<output>
After completion, create `.planning/phases/152-worldscene-decomposition/152-01-SUMMARY.md`
</output>
