---
phase: 118-ability-rebalance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/src/game/ability.ts
  - packages/game-logic/src/ability/definitions.ts
autonomous: true
requirements: [ABIL-01, ABIL-02, ABIL-03, ABIL-04, ABIL-05, ABIL-06, ABIL-07, ABIL-08, ABIL-09, ABIL-10, ABIL-11, ABIL-12, ABIL-13]

must_haves:
  truths:
    - "AbilityEffect union includes stun, hazard_immunity, and reflect variants alongside existing shield and damage_reduction"
    - "Plasma Burst definition has baseDamage 28 (down from 35)"
    - "Thermal Lance and Cryo Blast definitions have damageType fields"
    - "Emergency Shield, Fortify Systems, and Energy Barrier definitions use new effect types instead of stat buffs"
    - "Concussive Strike definition includes a stun effect"
    - "Overload Pulse definition has range 2 and requiresTarget false"
    - "Void Drain heal is 25 (up from 15) and Regeneration Protocol HoT is 16/tick (up from 8)"
    - "All rebalanced abilities have updated description strings reflecting new behavior"
  artifacts:
    - path: "packages/shared-types/src/game/ability.ts"
      provides: "Extended AbilityEffect union with stun, hazard_immunity, reflect, conditionBonus on damage"
      contains: "type: 'stun'"
    - path: "packages/game-logic/src/ability/definitions.ts"
      provides: "All 13 rebalanced ability definitions"
      contains: "baseDamage: 28"
  key_links:
    - from: "packages/game-logic/src/ability/definitions.ts"
      to: "packages/shared-types/src/game/ability.ts"
      via: "AbilityEffect type import"
      pattern: "type.*stun|type.*shield|type.*damage_reduction|type.*hazard_immunity|type.*reflect"
---

<objective>
Extend the AbilityEffect discriminated union with new effect types and update all 13 ability definitions with rebalanced values, new effect types, damage type assignments, and updated tooltip descriptions.

Purpose: Establish the data layer foundation so that server-side handlers (Plan 02, 03) and client-side rendering (Plan 04) have correct definitions to implement against. No behavioral changes yet -- this is purely type and data authoring.

Output: Updated AbilityEffect union in shared-types and 13 rebalanced ability definitions in game-logic.
</objective>

<execution_context>
@/Users/krzysztof.kalamarski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/krzysztof.kalamarski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-ability-rebalance/118-RESEARCH.md

<interfaces>
<!-- Current AbilityEffect union in packages/shared-types/src/game/ability.ts -->
export type AbilityEffect =
  | { readonly type: 'damage'; readonly baseDamage: number; readonly scaling: number; readonly damageType?: DamageType }
  | { readonly type: 'heal'; readonly baseHeal: number; readonly scaling: number }
  | { readonly type: 'buff'; readonly stat: string; readonly amount: number; readonly duration: number }
  | { readonly type: 'debuff'; readonly stat: string; readonly amount: number; readonly duration: number }
  | { readonly type: 'dot'; readonly damagePerTick: number; readonly tickInterval: number; readonly duration: number }
  | { readonly type: 'hot'; readonly healPerTick: number; readonly tickInterval: number; readonly duration: number }
  | { readonly type: 'gather'; readonly gatherType: 'harvest' | 'mine' | 'universal'; readonly baseYield: number }
  | { readonly type: 'shield'; readonly absorbAmount: number; readonly durationMs: number }
  | { readonly type: 'damage_reduction'; readonly reductionPercent: number; readonly durationMs: number };

<!-- Current ability definitions that need changing (from definitions.ts): -->
ABILITY_PLASMA_BURST: baseDamage: 35, scaling: 1.2, no damageType
ABILITY_THERMAL_LANCE: baseDamage: 28, scaling: 1.0, no damageType
ABILITY_CRYO_BLAST: baseDamage: 22, scaling: 0.9, no damageType
ABILITY_ELECTROCUTE: damage 15/0.7 + dot 5/1000/4000, no spreadRadius
ABILITY_OVERLOAD_PULSE: range: 1, requiresTarget: true, baseDamage: 25
ABILITY_PRECISION_SHOT: baseDamage: 24, range: 4, no reveal effect
ABILITY_VOID_DRAIN: baseHeal: 15
ABILITY_CONCUSSIVE_STRIKE: damage only, no stun
ABILITY_EMERGENCY_SHIELD: buff toughness 12 for 8000ms
ABILITY_REGENERATION_PROTOCOL: hot healPerTick: 8
ABILITY_MAGNETIC_FIELD: buff toughness 8 for 12000ms
ABILITY_FORTIFY_SYSTEMS: buff durability 10 for 15000ms
ABILITY_ENERGY_BARRIER: buff resilience 7 for 12000ms
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AbilityEffect union with new effect types</name>
  <files>packages/shared-types/src/game/ability.ts</files>
  <action>
Add the following new variants to the AbilityEffect discriminated union in `packages/shared-types/src/game/ability.ts`:

1. **stun**: `{ readonly type: 'stun'; readonly durationMs: number; readonly maniacDurationMs?: number }` — for Concussive Strike's CC effect. The optional `maniacDurationMs` allows 3s vs maniacs without hardcoding ability IDs in handlers.

2. **hazard_immunity**: `{ readonly type: 'hazard_immunity'; readonly durationMs: number }` — for Energy Barrier's biome hazard immunity. Phase 120 HazardService will check this state.

3. **reflect**: `{ readonly type: 'reflect'; readonly reflectPercent: number; readonly durationMs: number }` — for Magnetic Field's 30% damage reflect.

4. **Add conditionBonus to existing damage variant**: Extend the damage variant to: `{ readonly type: 'damage'; readonly baseDamage: number; readonly scaling: number; readonly damageType?: DamageType; readonly conditionBonus?: { readonly hpThresholdAbove: number; readonly multiplier: number } }` — for Plasma Burst's +50% above 80% HP.

5. **Add spreadRadius to existing dot variant**: Extend dot to: `{ readonly type: 'dot'; readonly damagePerTick: number; readonly tickInterval: number; readonly duration: number; readonly spreadRadius?: number }` — for Electrocute's chain spread to creatures within 2 tiles.

6. **Add reveal to a new variant**: `{ readonly type: 'reveal'; readonly radiusTiles: number; readonly coneDegrees: number; readonly durationMs: number }` — for Precision Shot's 6-tile cone reveal of stealthed predators.

Keep all existing variants unchanged. All new fields use `readonly` modifier consistent with existing pattern.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit -p packages/shared-types/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>AbilityEffect union has stun, hazard_immunity, reflect, reveal variants plus conditionBonus on damage and spreadRadius on dot. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 13 ability definitions with rebalanced values</name>
  <files>packages/game-logic/src/ability/definitions.ts</files>
  <action>
Update the following ability definitions in `packages/game-logic/src/ability/definitions.ts`. For each ability, update BOTH the effects array AND the description string.

**ABIL-01 — Plasma Burst:**
- Change `baseDamage: 35` to `baseDamage: 28`
- Add `conditionBonus: { hpThresholdAbove: 0.80, multiplier: 1.5 }` to the damage effect
- Update description: `'Launch a superheated plasma projectile. Deals 28 base damage, +50% bonus against targets above 80% HP.'`

**ABIL-02 — Thermal Lance:**
- Add `damageType: 'Thermal'` to the damage effect (baseDamage stays 28, scaling stays 1.0)
- Update description: `'Project a focused beam of intense heat. Deals Thermal damage, effective against cold-adapted creatures.'`

**ABIL-03 — Cryo Blast:**
- Add `damageType: 'Cryo'` to the damage effect (baseDamage stays 22, scaling stays 0.9)
- Update description: `'Unleash a burst of cryogenic energy. Deals Cryo damage, effective against heat-adapted creatures.'`

**ABIL-04 — Electrocute:**
- Add `spreadRadius: 2` to the existing dot effect (the dot with damagePerTick: 5)
- Update description: `'Discharge high-voltage energy that arcs between targets. Shock damage spreads to creatures within 2 tiles.'`

**ABIL-05 — Overload Pulse:**
- Change `range: 1` to `range: 2`
- Change `requiresTarget: true` to `requiresTarget: false`
- Update description: `'Release an overwhelming energy surge, damaging all creatures within 2 tiles.'`

**ABIL-06 — Precision Shot:**
- Add a second effect to the effects array: `{ type: 'reveal', radiusTiles: 6, coneDegrees: 90, durationMs: 5000 }`
- Update description: `'Fire a precisely targeted shot at extreme range. Reveals stealthed predators in a 6-tile cone for 5 seconds.'`

**ABIL-07 — Void Drain:**
- Change `baseHeal: 15` to `baseHeal: 25` in the heal effect
- Update description: `'Siphon life force from the target, dealing damage and restoring 25 health.'`

**ABIL-08 — Concussive Strike:**
- Add a second effect to the effects array: `{ type: 'stun', durationMs: 1000, maniacDurationMs: 3000 }`
- Update description: `'Deliver a powerful strike that stuns the target for 1 second. Stuns maniacs for 3 seconds.'`

**ABIL-09 — Emergency Shield:**
- Replace the entire effects array: `[{ type: 'shield', absorbAmount: 80, durationMs: 8000 }]`
- Update description: `'Activate emergency shielding that absorbs up to 80 incoming damage within 8 seconds.'`

**ABIL-10 — Regeneration Protocol:**
- Change `healPerTick: 8` to `healPerTick: 16` in the hot effect (duration stays 10000, tickInterval stays 2000 = 5 ticks x 16 = 80 HP total)
- Update description: `'Initialize cellular regeneration protocols, restoring 80 HP over 10 seconds.'`

**ABIL-11 — Magnetic Field:**
- Replace the entire effects array: `[{ type: 'reflect', reflectPercent: 0.30, durationMs: 8000 }]`
- Update description: `'Generate a protective magnetic field that reflects 30% of incoming damage for 8 seconds.'`

**ABIL-12 — Fortify Systems:**
- Replace the entire effects array: `[{ type: 'damage_reduction', reductionPercent: 0.15, durationMs: 10000 }]`
- Update description: `'Reinforce exo-suit structural integrity, reducing all incoming damage by 15% for 10 seconds.'`

**ABIL-13 — Energy Barrier:**
- Replace the entire effects array: `[{ type: 'hazard_immunity', durationMs: 20000 }]`
- Update description: `'Project an energy barrier granting immunity to all biome hazard effects for 20 seconds.'`

Do NOT change: energyCost, cooldownMs, range (except Overload Pulse), requiresTarget (except Overload Pulse), iconKey, iconColor, category, or any ability ID.
  </action>
  <verify>
    <automated>cd /Users/krzysztof.kalamarski/Projects/into-the-void && npx tsc --noEmit -p packages/game-logic/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>All 13 ability definitions updated with correct values, damage types, new effect types, and description strings. TypeScript compiles clean for game-logic package.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/shared-types/tsconfig.json` passes
2. `npx tsc --noEmit -p packages/game-logic/tsconfig.json` passes
3. Plasma Burst baseDamage is 28 with conditionBonus
4. Thermal Lance has damageType: 'Thermal', Cryo Blast has damageType: 'Cryo'
5. Emergency Shield uses shield effect (not buff), Fortify uses damage_reduction, Energy Barrier uses hazard_immunity, Magnetic Field uses reflect
6. Concussive Strike has stun effect, Electrocute dot has spreadRadius, Overload Pulse is AoE (requiresTarget: false, range: 2)
</verification>

<success_criteria>
All 13 ability definitions match their ABIL-XX requirement specifications. New AbilityEffect variants compile cleanly. No existing ability behaviors are broken (handlers don't exist yet for new types, so no runtime impact). Descriptions accurately reflect new mechanics.
</success_criteria>

<output>
After completion, create `.planning/phases/118-ability-rebalance/118-01-SUMMARY.md`
</output>
